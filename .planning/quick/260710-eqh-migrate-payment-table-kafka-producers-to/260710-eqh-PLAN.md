---
phase: quick-260710-eqh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/payment_context/infrastructure/config/PaymentKafkaProducerConfig.java
  - src/main/java/com/example/feat1/DDD/table_context/infrastructure/config/TableKafkaProducerConfig.java
  - src/main/java/com/example/feat1/DDD/shared/outbox/repository/OutboxEventRepository.java
  - src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java
autonomous: true
requirements: [15-01, WR-01]
must_haves:
  truths:
    - "Payment and Table Kafka producers use the Jackson-3 JacksonJsonSerializer, so PaymentEvent/TableOperationEvent Instant fields serialize at runtime"
    - "OutboxEventRepository.claimPending Javadoc no longer claims cross-instance no-double-publish safety and correctly describes SKIP LOCKED semantics"
    - "OutboxRelay class Javadoc wording is consistent with the corrected repository Javadoc (no multi-instance safety implication)"
    - "Full Maven suite stays green (213 tests, 0 failures)"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/payment_context/infrastructure/config/PaymentKafkaProducerConfig.java"
      provides: "Payment producer wired to JacksonJsonSerializer"
      contains: "JacksonJsonSerializer"
    - path: "src/main/java/com/example/feat1/DDD/table_context/infrastructure/config/TableKafkaProducerConfig.java"
      provides: "Table producer wired to JacksonJsonSerializer"
      contains: "JacksonJsonSerializer"
    - path: "src/main/java/com/example/feat1/DDD/shared/outbox/repository/OutboxEventRepository.java"
      provides: "Corrected claimPending Javadoc"
      contains: "SKIP LOCKED"
    - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java"
      provides: "Consistent OutboxRelay class Javadoc"
      contains: "single-instance"
  key_links:
    - from: "PaymentKafkaProducerConfig.java"
      to: "org.springframework.kafka.support.serializer.JacksonJsonSerializer"
      via: "VALUE_SERIALIZER_CLASS_CONFIG"
      pattern: "JacksonJsonSerializer.class"
    - from: "TableKafkaProducerConfig.java"
      to: "org.springframework.kafka.support.serializer.JacksonJsonSerializer"
      via: "VALUE_SERIALIZER_CLASS_CONFIG"
      pattern: "JacksonJsonSerializer.class"
---

<objective>
Fix two independent, low-risk outstanding issues in a single atomic change:
- **15-01** (latent runtime bug): migrate Payment + Table Kafka producers from Jackson-2 `JsonSerializer` to Jackson-3 `JacksonJsonSerializer`, mirroring the already-correct KitchenSettleTriggerProducerConfig. On the Boot 4 / Jackson 3 classpath the Jackson-2 serializer cannot serialize `Instant` fields, causing runtime publish failure.
- **WR-01** (doc-only): correct stale Javadoc that falsely claims SKIP LOCKED gives cross-instance no-double-publish safety. After the WR-02 refactor, `poll()` is no longer `@Transactional`, so SKIP LOCKED row locks release the moment `claimPending()` returns and rows stay `PENDING`, allowing re-claim by another instance.

Purpose: Close the last two known issues without altering runtime behavior beyond the serializer swap.
Output: Two producer configs on the Jackson-3 serializer; two accurate Javadoc blocks; green Maven suite.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Reference: the already-correct Jackson-3 pattern to mirror exactly (do NOT edit this file). -->

From KitchenSettleTriggerProducerConfig.java:
- import: org.springframework.kafka.support.serializer.JacksonJsonSerializer
- config.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class)   // key serializer UNCHANGED
- config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, JacksonJsonSerializer.class)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply both fixes (serializer swap + Javadoc corrections)</name>
  <files>
    src/main/java/com/example/feat1/DDD/payment_context/infrastructure/config/PaymentKafkaProducerConfig.java,
    src/main/java/com/example/feat1/DDD/table_context/infrastructure/config/TableKafkaProducerConfig.java,
    src/main/java/com/example/feat1/DDD/shared/outbox/repository/OutboxEventRepository.java,
    src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java
  </files>
  <action>
Read each file's exact current source before editing (line numbers below are approximate).

**Fix 1 (15-01) — Payment + Table producers, per requirement 15-01:**
In BOTH PaymentKafkaProducerConfig.java (line ~14 import, line ~24 config) and TableKafkaProducerConfig.java (line ~14 import, line ~24 config):
- Replace the import `org.springframework.kafka.support.serializer.JsonSerializer` with `org.springframework.kafka.support.serializer.JacksonJsonSerializer`.
- Replace `config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, JsonSerializer.class)` with `config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, JacksonJsonSerializer.class)`.
- Do NOT change the KEY_SERIALIZER_CLASS_CONFIG (StringSerializer stays). Do NOT touch bean names, generics, or method signatures. Mirror KitchenSettleTriggerProducerConfig exactly.

**Fix 2 (WR-01) — Javadoc corrections only, per requirement WR-01. PURELY documentation edits — do NOT change the SKIP LOCKED query, schema, method signatures, or any runtime logic:**
In OutboxEventRepository.java, rewrite the `claimPending` Javadoc (lines ~14-19) so it: (a) stops asserting cross-instance no-double-publish safety; (b) explains SKIP LOCKED only reduces lock contention within a single poll; (c) states cross-instance dedup is NOT guaranteed and the current topology is single-instance (poll() serialized by @Scheduled(fixedDelay)); (d) notes at-least-once delivery plus the idempotent order_processed_events consumer ledger is what bounds duplicate-publish impact. Preserve the existing note that this native query is MySQL-8-only and must not run on H2 (why OutboxRelay is disabled in the test profile).
In OutboxRelay.java, review the class Javadoc (lines ~14-28) and adjust any wording implying multi-instance safety so it is consistent with the corrected repository Javadoc (single-instance topology, at-least-once, idempotent consumer ledger bounds impact). Keep the accurate existing WR-02 explanation of poll() being non-@Transactional and per-row commits.
  </action>
  <verify>
    <automated>grep -c 'JacksonJsonSerializer' src/main/java/com/example/feat1/DDD/payment_context/infrastructure/config/PaymentKafkaProducerConfig.java src/main/java/com/example/feat1/DDD/table_context/infrastructure/config/TableKafkaProducerConfig.java && ! grep -rn 'import org.springframework.kafka.support.serializer.JsonSerializer;' src/main/java/com/example/feat1/DDD/payment_context/infrastructure/config/PaymentKafkaProducerConfig.java src/main/java/com/example/feat1/DDD/table_context/infrastructure/config/TableKafkaProducerConfig.java</automated>
  </verify>
  <done>Both producer configs import and use JacksonJsonSerializer for the value serializer (key serializer unchanged); the Jackson-2 JsonSerializer import is gone from both. claimPending and OutboxRelay Javadocs no longer assert cross-instance no-double-publish safety and are mutually consistent. No runtime logic, queries, or signatures changed.</done>
</task>

<task type="auto">
  <name>Task 2: Compile and run full Maven suite</name>
  <files>(none — verification only)</files>
  <action>Run the full Maven test suite from the repo root to confirm compilation succeeds and no tests regress. No test changes are expected: Fix 1 is a serializer swap exercised only at runtime, Fix 2 is doc-only.</action>
  <verify>
    <automated>./mvnw -q test</automated>
  </verify>
  <done>Maven suite passes green (expected 213 tests, 0 failures, 0 errors). Compilation clean.</done>
</task>

</tasks>

<verification>
- Both producer configs reference JacksonJsonSerializer; no Jackson-2 JsonSerializer import remains in either.
- claimPending and OutboxRelay Javadocs are accurate (no cross-instance safety claim) and consistent.
- `./mvnw test` is green with no test file modifications.
</verification>

<success_criteria>
- Payment + Table producers serialize Instant fields via Jackson-3 at runtime (15-01 closed).
- Outbox Javadocs correctly describe SKIP LOCKED semantics and single-instance topology (WR-01 closed).
- Full Maven suite green (213 tests, 0 failures), no runtime logic changed.
</success_criteria>

<output>
Create `.planning/quick/260710-eqh-migrate-payment-table-kafka-producers-to/260710-eqh-SUMMARY.md` when done
</output>
