---
phase: quick
plan: 260710-e78
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java
  - src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRowPublisher.java
  - src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRetentionJob.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java
  - src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java
autonomous: true
requirements: [WR-02, WR-05, IN-01, WR-03]

must_haves:
  truths:
    - "A persistence-layer failure on one claimed outbox row no longer aborts publication of the remaining rows in the same poll batch"
    - "An InterruptedException during a Kafka send restores the thread interrupt flag instead of swallowing it"
    - "A misconfigured ttl-days < 1 skips the retention sweep instead of deleting all SENT rows"
    - "rejection_reason maps to a TEXT/CLOB column via @Lob, matching the truncate Javadoc"
    - "The full Maven test suite stays green after the edits"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java"
      provides: "Per-row try/catch isolation in poll() loop (WR-02)"
      contains: "catch (Exception ex)"
    - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRowPublisher.java"
      provides: "Explicit InterruptedException handling before broad catch (WR-05)"
      contains: "catch (InterruptedException"
    - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRetentionJob.java"
      provides: "ttlDays lower-bound guard (IN-01)"
      contains: "ttlDays < 1"
    - path: "src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java"
      provides: "@Lob mapping on rejectionReason (WR-03)"
      contains: "@Lob"
  key_links:
    - from: "OrderEntity.rejectionReason"
      to: "OrderConfirmationService.MAX_REASON_LEN"
      via: "REJECTION_REASON_MAX_LEN constant retained for app-level truncation bound"
      pattern: "REJECTION_REASON_MAX_LEN"
---

<objective>
Fix four minor code-review findings from 17.2-REVIEW.md in a single atomic change: per-row
batch isolation (WR-02), interrupt-flag restoration (WR-05), retention ttl-days validation
(IN-01), and the rejection_reason TEXT mapping (WR-03).

Purpose: Close remaining non-blocking review debt on the outbox durability + order-confirmation
paths without altering runtime behavior on the happy path.
Output: Four edited source files plus a green Maven test suite.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-REVIEW.md

<interfaces>
<!-- The @Lob TEXT-column pattern to mirror for WR-03, from the sibling long-text column. -->
From src/main/java/com/example/feat1/DDD/shared/outbox/entity/OutboxEventEntity.java:
```java
@Lob
@Column(name = "payload", nullable = false)
private String payload;
```

Current OrderEntity mapping (to change), line 48:
```java
@Column(name = "rejection_reason", length = REJECTION_REASON_MAX_LEN)
private String rejectionReason;
```
REJECTION_REASON_MAX_LEN (line 35) MUST be retained — OrderConfirmationService.MAX_REASON_LEN
derives from it for the app-level char-count truncation bound.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the four review fixes</name>
  <files>
    src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java,
    src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRowPublisher.java,
    src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRetentionJob.java,
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java,
    src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java
  </files>
  <action>
Read each file's current source before editing — line numbers below are approximate.

WR-02 (OutboxRelay.java, poll() for-loop, ~line 40-42): Wrap the per-row
outboxRowPublisher.publish(row) call in a try/catch. On catch of a broad Exception, log a warning
"Outbox row {} publish transaction failed; continuing batch" with row.getId() and the exception,
then continue the loop. This file currently has no Logger — add a slf4j Logger field
(LoggerFactory.getLogger(OutboxRelay.class)) and the two imports (org.slf4j.Logger,
org.slf4j.LoggerFactory), mirroring OutboxRowPublisher. Do not change the batch claim or scheduling.

WR-05 (OutboxRowPublisher.java, publish(), ~line 52): Add an explicit
catch (InterruptedException ie) branch BEFORE the existing broad catch (Exception ex). In the new
branch: call Thread.currentThread().interrupt(), increment attempts via
row.setAttempts(row.getAttempts() + 1), and log a warning
"Outbox row {} publish interrupted" with row.getId() and ie. Do NOT set the row to FAILED and do
NOT flip it to SENT — leave it PENDING so the next poll re-drives it. The trailing
outboxEventRepository.save(row) at ~line 75 stays outside/after all catch branches and still runs.

IN-01 (OutboxRetentionJob.java, sweepSentRetention(), ~line 49-51): At the top of
sweepSentRetention(), before computing cutoff, guard: if (ttlDays < 1) { log a warning that names
the misconfigured ttlDays value and states the sweep is skipped; return; }. This prevents a
negative/zero ttl-days from producing a future cutoff that deletes all SENT rows. Leave
surfaceFailedRows() unchanged.

WR-03 (OrderEntity.java, ~line 48): Change the rejectionReason mapping to mirror
OutboxEventEntity.payload — annotate with @Lob and drop the length attribute:
@Lob @Column(name = "rejection_reason") private String rejectionReason;
Add the jakarta.persistence.Lob import. KEEP the REJECTION_REASON_MAX_LEN constant (line 35) — it
is the source of truth for OrderConfirmationService.MAX_REASON_LEN and must not be removed.
Then in OrderConfirmationService.java (truncate Javadoc, ~line 150-153): the existing wording
"the column is widened to TEXT AND the application layer bounds the value defensively" is now
accurate with the @Lob mapping — leave it, or lightly adjust for consistency, but do NOT weaken or
remove the app-level char-count truncation logic in truncate(); the MAX_REASON_LEN bound must remain.
  </action>
  <verify>
    <automated>cd /home/chinhlq/IdeaProjects/feat1 && grep -q "catch (Exception ex)" src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java && grep -q "catch (InterruptedException" src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRowPublisher.java && grep -q "ttlDays < 1" src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRetentionJob.java && grep -q "@Lob" src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java && grep -q "REJECTION_REASON_MAX_LEN" src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java && echo ALL_FOUR_PRESENT</automated>
  </verify>
  <done>All four edits present: WR-02 per-row try/catch with logger in OutboxRelay, WR-05 explicit InterruptedException catch restoring the interrupt flag, IN-01 ttlDays < 1 guard-and-return, WR-03 @Lob on rejectionReason with REJECTION_REASON_MAX_LEN retained.</done>
</task>

<task type="auto">
  <name>Task 2: Compile and run the full Maven test suite</name>
  <files>(no source changes — verification only)</files>
  <action>
Run the project's Maven test suite from the repo root using the wrapper to confirm the four edits
compile and the suite stays green. Use ./mvnw so the pinned Maven version is used. If any test
fails, read the failure, correlate it to the specific edit, fix, and re-run until green.
  </action>
  <verify>
    <automated>cd /home/chinhlq/IdeaProjects/feat1 && ./mvnw -q test</automated>
  </verify>
  <done>./mvnw test completes with BUILD SUCCESS and zero test failures/errors.</done>
</task>

</tasks>

<verification>
- Four target files edited exactly per WR-02, WR-05, IN-01, WR-03 prescriptions.
- REJECTION_REASON_MAX_LEN constant and the app-level truncate() bound both retained.
- Full Maven suite green (./mvnw test → BUILD SUCCESS).
</verification>

<success_criteria>
- WR-02: One row's publish transaction failure is caught and logged in poll(); the batch continues.
- WR-05: InterruptedException restores the interrupt flag, increments attempts, logs, leaves row PENDING.
- IN-01: ttl-days < 1 logs a warning and skips the sweep.
- WR-03: rejection_reason maps to TEXT/CLOB via @Lob; Javadoc consistent; truncation bound intact.
- Test suite passes.
</success_criteria>

<output>
Create `.planning/quick/260710-e78-fix-4-minor-findings-from-17-2-review-md/260710-e78-SUMMARY.md` when done
</output>
