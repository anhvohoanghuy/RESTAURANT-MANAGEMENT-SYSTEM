---
phase: 17-kitchen-context
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenKafkaTopicConfig.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/OrderConfirmedKafkaConsumerConfig.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/adapter/OrderConfirmedListener.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/application/KitchenTicketCreationService.java
  - src/test/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/OrderConfirmedKafkaConsumerConfigTest.java
  - src/test/java/com/example/feat1/DDD/kitchen_context/application/KitchenTicketCreationServiceTest.java
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "Kitchen consumes OrderConfirmed from orders.confirmed and creates exactly one KitchenTicket per confirmed order, building all items in one pass from the manifest"
    - "Redelivery of the same OrderConfirmed event creates no duplicate ticket (idempotency ledger)"
    - "A poison-pill OrderConfirmed payload routes to the DLT instead of blocking the partition"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/application/KitchenTicketCreationService.java"
      provides: "Idempotent OrderConfirmed handler that builds the full ticket"
      contains: "existsByEventIdAndConsumerName"
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenKafkaTopicConfig.java"
      provides: "NewTopic beans for kitchen's own topics only"
      contains: "orders.confirmed"
  key_links:
    - from: "OrderConfirmedListener"
      to: "KitchenTicketCreationService.onOrderConfirmed"
      via: "one-line @KafkaListener delegate"
      pattern: "onOrderConfirmed"
---

<objective>
Wire kitchen's inbound side: declare kitchen's own Kafka topics, add a poison-pill-safe consumer config
and a thin `OrderConfirmedListener`, and implement the idempotent `KitchenTicketCreationService` that
turns one `OrderConfirmedEvent` into exactly one `KitchenTicket` with all items created up-front. This
is the consumer half of D-01.

Purpose: Kitchen must materialize a ticket per confirmed order without cross-context lookups, exactly
once, and survive replay/poison payloads.
Output: topic config, consumer config, listener, creation service, and their tests.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-kitchen-context/17-CONTEXT.md
@.planning/phases/17-kitchen-context/17-PATTERNS.md

<interfaces>
<!-- From plan 17-01 (already merged): OrderConfirmedEvent record in
     com.example.feat1.DDD.order_context.application.event with nested OrderConfirmedLine/OrderConfirmedTopping,
     TYPE = "OrderConfirmed", topic default orders.confirmed.
     From plan 17-02: KitchenTicketEntity/KitchenTicketItemEntity, KitchenTicketRepository (existsByOrderId,
     findByOrderId), KitchenProcessedEventEntity/Repository (existsByEventIdAndConsumerName). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kitchen topic config + poison-pill-safe OrderConfirmed consumer config</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenKafkaTopicConfig.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/OrderConfirmedKafkaConsumerConfig.java,
    src/test/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/OrderConfirmedKafkaConsumerConfigTest.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java (topic-bean analog + settle-trigger topic already owned there),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfig.java (consumer-config analog to clone),
    src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfigTest.java (broker-free config test analog)
  </read_first>
  <action>
    `KitchenKafkaTopicConfig`: declare `NewTopic` beans (TopicBuilder, 1 partition/1 replica) ONLY for
    kitchen's genuinely new topics — `orders.confirmed` (`${order.events.order-confirmed-topic:orders.confirmed}`)
    + its `.DLT`, and `kitchen.ticket-status-changed` (`${kitchen.events.ticket-status-changed-topic:kitchen.ticket-status-changed}`)
    + its `.DLT`. Do NOT redeclare `settleTriggerTopic()`/`settleTriggerDltTopic()` — those are owned by
    InventoryKafkaTopicConfig (Anti-pattern in RESEARCH.md). `OrderConfirmedKafkaConsumerConfig`: clone
    `OrderKafkaConsumerConfig` retyped to `OrderConfirmedEvent`, TRUSTED_PACKAGE =
    "com.example.feat1.DDD.order_context.application.event", VALUE_DEFAULT_TYPE = OrderConfirmedEvent,
    USE_TYPE_INFO_HEADERS=false, ErrorHandlingDeserializer, DefaultErrorHandler with
    `FixedBackOff(1000L, 3L)` + `addNotRetryableExceptions(DeserializationException.class)`, AckMode.RECORD.
    Name every bean with a kitchen-scoped prefix to avoid collisions (Pitfall 5): `kitchenDltKafkaTemplate`,
    `orderConfirmedConsumerFactory`, `orderConfirmedErrorHandler`, `orderConfirmedKafkaListenerContainerFactory`;
    group-id `${kitchen.order-confirmed.consumer.group-id:kitchen-order-confirmed}`. Test: instantiate the
    consumer config directly (no Spring context) and assert deserializer class, TRUSTED_PACKAGES,
    VALUE_DEFAULT_TYPE, USE_TYPE_INFO_HEADERS=false, AckMode.RECORD, and that DeserializationException is
    non-retryable — mirroring SettleTriggerKafkaConsumerConfigTest.
  </action>
  <verify>
    <automated>./mvnw -o test -Dtest=OrderConfirmedKafkaConsumerConfigTest</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenKafkaTopicConfig` declares NewTopic beans for orders.confirmed(+DLT) and kitchen.ticket-status-changed(+DLT) and NONE for settle-trigger
    - Consumer config uses ErrorHandlingDeserializer + TRUSTED_PACKAGES + VALUE_DEFAULT_TYPE=OrderConfirmedEvent + USE_TYPE_INFO_HEADERS=false, all beans kitchen-prefixed
    - `./mvnw -o test -Dtest=OrderConfirmedKafkaConsumerConfigTest` passes
  </acceptance_criteria>
  <done>Kitchen owns its two topics, consumes OrderConfirmed poison-pill-safely, and has no bean-name collisions with order/inventory templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent KitchenTicketCreationService + thin OrderConfirmedListener</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/application/KitchenTicketCreationService.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/adapter/OrderConfirmedListener.java,
    src/test/java/com/example/feat1/DDD/kitchen_context/application/KitchenTicketCreationServiceTest.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java (inline idempotency-ledger analog, lines 40-53),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderStockResultListener.java (thin listener analog),
    .planning/phases/17-kitchen-context/17-PATTERNS.md (KitchenTicketCreationService section)
  </read_first>
  <behavior>
    - First delivery of an OrderConfirmed event: creates one KitchenTicketEntity with orderId set and one KitchenTicketItemEntity per manifest line (dishId, dishName, quantity, toppings copied; status QUEUED); item count equals manifest line count (Pitfall 4)
    - Redelivery of the same eventId: ledger guard short-circuits, no second ticket created (verify save called once across two invocations)
    - Concurrent-duplicate ledger insert (DataIntegrityViolationException on saveAndFlush): swallowed, returns without creating a ticket
  </behavior>
  <action>
    `KitchenTicketCreationService` (`@Service @RequiredArgsConstructor`, `@Transactional` method
    `onOrderConfirmed(OrderConfirmedEvent event)`): constant `CONSUMER_NAME = "kitchen-order-confirmed"`.
    Follow the inline idempotency pattern verbatim — `existsByEventIdAndConsumerName` early-return, then
    build+`saveAndFlush` the `KitchenProcessedEventEntity` inside try/catch on
    `DataIntegrityViolationException` (return on duplicate). After the ledger write, build ONE
    `KitchenTicketEntity` (orderId, createdAt=Instant.now()) and, in a single pass over `event.lines()`,
    all `KitchenTicketItemEntity` rows (orderLineId=line.lineId(), dishId, dishName, quantity, mapped
    topping snapshots, status defaulting to QUEUED), wire the bidirectional `ticket`/`items` link, and
    `kitchenTicketRepository.save(ticket)`. Never add items after creation (Pitfall 4: totalLines depends
    on item count == line count). `OrderConfirmedListener` (`@Component @RequiredArgsConstructor`) is a
    one-line `@KafkaListener` delegate: topics `${order.events.order-confirmed-topic:orders.confirmed}`,
    groupId `${kitchen.order-confirmed.consumer.group-id:kitchen-order-confirmed}`, containerFactory
    `orderConfirmedKafkaListenerContainerFactory`, method body calls
    `creationService.onOrderConfirmed(event)` only (no business logic in the adapter).
  </action>
  <verify>
    <automated>./mvnw -o test -Dtest=KitchenTicketCreationServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -o test -Dtest=KitchenTicketCreationServiceTest` passes
    - Test asserts one ticket with item count == manifest line count on first delivery, and no duplicate on replay
    - `OrderConfirmedListener` body is a single delegate call (no repository/ledger logic)
  </acceptance_criteria>
  <done>Exactly one ticket per confirmed order, all items built up-front, replay-safe; listener is a thin delegate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orders.confirmed topic → kitchen consumer | Untrusted/replayed/forged payloads cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Spoofing / Tampering | OrderConfirmed deserialization | mitigate | ErrorHandlingDeserializer + fixed VALUE_DEFAULT_TYPE + TRUSTED_PACKAGES + USE_TYPE_INFO_HEADERS=false blocks __TypeId__ type-confusion; poison payload → DLT |
| T-17-06 | Tampering | Duplicate ticket creation on replay | mitigate | `(eventId, kitchen-order-confirmed)` ledger guard + unique orderId constraint → at most one ticket per order |
| T-17-07 | Denial of Service | Poison-pill blocking partition | mitigate | DefaultErrorHandler routes DeserializationException straight to `.DLT` (non-retryable) |
| T-17-SC | Tampering | dependency installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `./mvnw -o test -Dtest=OrderConfirmedKafkaConsumerConfigTest,KitchenTicketCreationServiceTest` green
- `grep -R "settleTriggerTopic" src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenKafkaTopicConfig.java` returns nothing (not redeclared)
- `grep -R "kitchenDltKafkaTemplate" src/main/java/com/example/feat1/DDD/kitchen_context` shows kitchen-prefixed DLT template
</verification>

<success_criteria>
- One confirmed order deterministically yields one ticket with all items, replay- and poison-pill-safe
- Kitchen declares only its own topics; no bean-name collisions
</success_criteria>

<output>
Create `.planning/phases/17-kitchen-context/17-03-SUMMARY.md` when done
</output>
