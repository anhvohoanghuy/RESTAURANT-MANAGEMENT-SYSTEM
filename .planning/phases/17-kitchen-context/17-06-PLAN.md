---
phase: 17-kitchen-context
plan: 06
type: execute
wave: 4
depends_on: [17-02, 17-05]
files_modified:
  - src/main/java/com/example/feat1/DDD/kitchen_context/application/dto/KitchenBoardItemResponse.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/application/KitchenBoardService.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/presentation/KitchenController.java
  - src/test/java/com/example/feat1/DDD/kitchen_context/integration/KitchenIntegrationTest.java
autonomous: true
requirements: [D-05]
must_haves:
  truths:
    - "ADMIN/STAFF can PATCH a single item's status via /admin/orders/{orderId}/items/{itemId}/status; an illegal transition returns a stable error"
    - "ADMIN/STAFF can GET the kitchen board listing all not-yet-COMPLETED items"
    - "A non-staff / unauthenticated caller is denied both endpoints (existing /admin/orders/** RBAC)"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/presentation/KitchenController.java"
      provides: "Staff advance + board endpoints under /admin/orders/**"
      contains: "/admin/orders/{orderId}/items/{itemId}/status"
  key_links:
    - from: "KitchenController.advanceItemStatus"
      to: "KitchenTicketAdvanceService.advance"
      via: "controller delegate passing principal.getId()"
      pattern: "advance("
---

<objective>
Expose the two staff endpoints of D-05 under the already-secured `/admin/orders/**` route: a PATCH that
advances one item's status (delegating to the plan-17-05 advance service) and a GET kitchen board listing
active (not-COMPLETED) items. A `@SpringBootTest` + MockMvc integration test proves the RBAC, the
happy-path advance, the stable illegal-transition error, and the board filter end-to-end on H2.

Purpose: Give kitchen staff the queue view and the single-item advance control.
Output: board DTO, board service, controller, and the integration test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-kitchen-context/17-CONTEXT.md
@.planning/phases/17-kitchen-context/17-PATTERNS.md

<interfaces>
<!-- From plan 17-05: KitchenTicketAdvanceService.advance(UUID orderId, UUID itemId,
     AdvanceItemStatusRequest request, UUID actorId) -> KitchenItemResponse; DTOs in
     kitchen_context.application.dto. From plan 17-02: KitchenTicketItemRepository.findByStatusNot(status),
     KitchenTicketItemEntity (id, orderLineId, dishName, quantity, status, ticket.orderId), KitchenItemStatus.
     SecurityConfig: /admin/orders/** already hasAnyRole("ADMIN","STAFF") — do NOT modify SecurityConfig. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kitchen board DTO + read-only KitchenBoardService</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/application/dto/KitchenBoardItemResponse.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/application/KitchenBoardService.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/application/OrderSubmissionService.java (read-only list-query analog, lines 85-90),
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenTicketItemRepository.java (findByStatusNot)
  </read_first>
  <action>
    `KitchenBoardItemResponse` (record): `UUID itemId, UUID orderId, UUID orderLineId, String dishName,
    int quantity, KitchenItemStatus status`. `KitchenBoardService` (`@Service @RequiredArgsConstructor`)
    with `@Transactional(readOnly = true) List<KitchenBoardItemResponse> board()` calling
    `itemRepository.findByStatusNot(KitchenItemStatus.COMPLETED)` and mapping each item to the DTO via
    `stream().map(...).toList()` (dishName/quantity/status from the item, orderId from `item.getTicket().getOrderId()`).
    Optionally sort by ticket createdAt then status for a stable queue order.
  </action>
  <verify>
    <automated>./mvnw -q -o test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenBoardService.board()` queries `findByStatusNot(COMPLETED)` and returns the DTO list
    - `./mvnw -o test-compile` succeeds
  </acceptance_criteria>
  <done>The board read model returns all active items, excluding COMPLETED.</done>
</task>

<task type="auto">
  <name>Task 2: KitchenController (PATCH advance + GET board) with MockMvc integration test</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/presentation/KitchenController.java,
    src/test/java/com/example/feat1/DDD/kitchen_context/integration/KitchenIntegrationTest.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/table_context/infrastructure/presentation/TableOperationController.java (PATCH .../status + GET controller analog),
    src/test/java/com/example/feat1/DDD/inventory_context/integration/InventoryStockIntegrationTest.java (@SpringBootTest + MockMvc + roles analog)
  </read_first>
  <action>
    `KitchenController` (`@RestController @RequiredArgsConstructor`, no class/method security annotation —
    `/admin/orders/**` is already secured): `@PatchMapping("/admin/orders/{orderId}/items/{itemId}/status")
    advanceItemStatus(@AuthenticationPrincipal CustomUserDetails principal, @PathVariable UUID orderId,
    @PathVariable UUID itemId, @RequestBody AdvanceItemStatusRequest request)` returning
    `ResponseEntity.ok(advanceService.advance(orderId, itemId, request, principal.getId()))`; and
    `@GetMapping("/admin/orders/kitchen-board") kitchenBoard()` returning
    `ResponseEntity.ok(boardService.board())`. Integration test (`@SpringBootTest @AutoConfigureMockMvc`,
    H2): seed a KitchenTicket with items directly via the repositories; assert (1) a STAFF/ADMIN user
    (`.with(user("staff").roles("STAFF"))`) PATCHing QUEUED→PREPARING returns 200 and PREPARING in the
    body; (2) an illegal transition (e.g. QUEUED→READY) returns the stable error status/code from
    `KitchenDomainException.transitionInvalid()` (BAD_REQUEST); (3) the GET board returns the seeded
    non-COMPLETED items; (4) an unauthenticated/USER-role request to either endpoint is denied (401/403).
    Keep `spring.kafka.listener.auto-startup=false` (test properties) so listeners never poll a broker.
  </action>
  <verify>
    <automated>./mvnw -o test -Dtest=KitchenIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -o test -Dtest=KitchenIntegrationTest` passes
    - Test asserts 200 + PREPARING on a valid STAFF advance, stable BAD_REQUEST on an illegal transition, board lists non-COMPLETED items, and denial for non-staff
    - No modification to `SecurityConfig.java`
  </acceptance_criteria>
  <done>Both staff endpoints work under existing RBAC with a stable illegal-transition error; integration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /admin/orders/** | Untrusted caller; must be ADMIN/STAFF |
| request body targetStatus → enum | Unknown/invalid status string must not 500 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Elevation of Privilege | Both endpoints | mitigate | Routes live under `/admin/orders/**` already `hasAnyRole("ADMIN","STAFF")`; integration test asserts non-staff denial (no SecurityConfig change) |
| T-17-15 | Tampering | Illegal transition via API | mitigate | Advance service rejects with `transitionInvalid()` → stable BAD_REQUEST via GlobalExceptionHandler; asserted in test |
| T-17-16 | Denial of Service | Malformed targetStatus | mitigate | Unknown enum value deserializes to a 400 (Jackson enum binding) not a 500; illegal-but-valid enum → stable domain error |
| T-17-SC | Tampering | dependency installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `./mvnw -o test -Dtest=KitchenIntegrationTest` green
- `grep -R "/admin/orders/{orderId}/items/{itemId}/status" src/main/java/com/example/feat1/DDD/kitchen_context` shows the PATCH route
- `git diff --name-only` does NOT include SecurityConfig.java
</verification>

<success_criteria>
- Staff can advance a single item and view the active board under the existing RBAC
- Illegal transitions return a stable error; non-staff are denied
</success_criteria>

<output>
Create `.planning/phases/17-kitchen-context/17-06-SUMMARY.md` when done
</output>
