---
phase: 17-kitchen-context
plan: 04
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - src/main/java/com/example/feat1/DDD/kitchen_context/application/event/KitchenTicketStatusChangedEvent.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/domain/port/KitchenSettleTriggerPublisher.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/domain/port/KitchenTicketStatusChangedPublisher.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenSettleTriggerProducerConfig.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenTicketStatusChangedProducerConfig.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/adapter/KafkaKitchenSettleTriggerPublisher.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/adapter/KafkaKitchenTicketStatusChangedPublisher.java
  - src/test/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenKafkaProducerConfigTest.java
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "D-03: Kitchen can publish the existing inventory SettleTriggerEvent (imported, not redeclared) to kitchen.settlement-trigger keyed by orderId"
    - "D-04: Kitchen can publish a KitchenTicketStatusChangedEvent carrying a full per-item status snapshot to kitchen.ticket-status-changed"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/application/event/KitchenTicketStatusChangedEvent.java"
      provides: "Full per-item snapshot event enabling order-side derivation"
      contains: "record KitchenTicketStatusChangedEvent"
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenSettleTriggerProducerConfig.java"
      provides: "Producer for the imported SettleTriggerEvent contract"
      contains: "SettleTriggerEvent"
  key_links:
    - from: "KafkaKitchenSettleTriggerPublisher"
      to: "com.example.feat1.DDD.inventory_context.application.event.SettleTriggerEvent"
      via: "cross-context import (not redeclared)"
      pattern: "inventory_context.application.event.SettleTriggerEvent"
---

<objective>
Build kitchen's two outbound Kafka producers: one for the EXISTING inventory `SettleTriggerEvent`
(imported across the package boundary, never redeclared) targeting `kitchen.settlement-trigger`, and one
for the NEW `KitchenTicketStatusChangedEvent` (full per-item snapshot) targeting
`kitchen.ticket-status-changed`. Provides the publisher ports the advance service (plan 17-05) calls
after commit. Covers the producer contract for D-03 and the event shape for D-04.

Purpose: Decouple the advance service from Kafka wiring behind small ports, and lock in the exactly-once
SettleTrigger contract + the self-contained status snapshot that lets order_context derive status.
Output: event record, two ports, two producer configs, two adapters, one broker-free producer test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-kitchen-context/17-CONTEXT.md
@.planning/phases/17-kitchen-context/17-PATTERNS.md

<interfaces>
<!-- Locked contract to import, NOT redeclare (inventory_context.application.event.SettleTriggerEvent):
     record SettleTriggerEvent(UUID eventId, String eventType, Instant occurredAt, UUID orderId,
     UUID orderLineId, int totalLines) { public static final String TYPE = "SettleTrigger"; }
     Topic property key MUST be ${kitchen.events.settle-trigger-topic:kitchen.settlement-trigger}
     (same key InventoryKafkaTopicConfig uses).
     From plan 17-02: KitchenItemStatus enum (kitchen_context.domain.model). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KitchenTicketStatusChangedEvent record + two publisher ports</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/application/event/KitchenTicketStatusChangedEvent.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/domain/port/KitchenSettleTriggerPublisher.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/domain/port/KitchenTicketStatusChangedPublisher.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/application/event/OrderCreatedEvent.java (nested-list record analog),
    src/main/java/com/example/feat1/DDD/inventory_context/application/event/SettleTriggerEvent.java (header-field + TYPE analog),
    src/main/java/com/example/feat1/DDD/order_context/domain/port/OrderEventPublisher.java (port shape analog)
  </read_first>
  <action>
    `KitchenTicketStatusChangedEvent` (record, package `...kitchen_context.application.event`): header
    fields `UUID eventId, String eventType, Instant occurredAt, UUID orderId, UUID ticketId`, plus
    `List<ItemStatus> items` where nested `record ItemStatus(UUID orderLineId, KitchenItemStatus status)`
    carries the FULL per-item snapshot (RESEARCH Open Question #1 resolved: full snapshot so order_context
    derives the aggregate with no cross-context lookup). Constant `public static final String TYPE =
    "KitchenTicketStatusChanged";`. Define two small ports: `KitchenSettleTriggerPublisher` with
    `void publishSettleTrigger(SettleTriggerEvent event)` (import inventory's SettleTriggerEvent — do NOT
    redeclare it), and `KitchenTicketStatusChangedPublisher` with
    `void publishTicketStatusChanged(KitchenTicketStatusChangedEvent event)`.
  </action>
  <verify>
    <automated>./mvnw -q -o test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenTicketStatusChangedEvent` declares nested `ItemStatus(UUID orderLineId, KitchenItemStatus status)` and a `List<ItemStatus> items` field
    - `KitchenSettleTriggerPublisher` imports `inventory_context.application.event.SettleTriggerEvent` (no local record copy exists in kitchen_context)
    - `./mvnw -o test-compile` succeeds
  </acceptance_criteria>
  <done>The status-snapshot event and both publisher ports compile; SettleTriggerEvent is imported, not cloned.</done>
</task>

<task type="auto">
  <name>Task 2: Two producer configs + two adapters (cross-context settle-trigger + status-changed)</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenSettleTriggerProducerConfig.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenTicketStatusChangedProducerConfig.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/adapter/KafkaKitchenSettleTriggerPublisher.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/adapter/KafkaKitchenTicketStatusChangedPublisher.java,
    src/test/java/com/example/feat1/DDD/kitchen_context/infrastructure/config/KitchenKafkaProducerConfigTest.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java (cross-context producer analog),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java (adapter one-liner analog),
    src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfigTest.java (broker-free config test style)
  </read_first>
  <action>
    `KitchenSettleTriggerProducerConfig`: structural clone of `InventoryKafkaProducerConfig` importing
    `com.example.feat1.DDD.inventory_context.application.event.SettleTriggerEvent`; beans
    `settleTriggerProducerFactory` + `settleTriggerKafkaTemplate` (StringSerializer key,
    JacksonJsonSerializer value, bootstrap-servers `@Value` default localhost:9092).
    `KitchenTicketStatusChangedProducerConfig`: same shape retyped to `KitchenTicketStatusChangedEvent`,
    beans `ticketStatusChangedProducerFactory` + `ticketStatusChangedKafkaTemplate`.
    `KafkaKitchenSettleTriggerPublisher implements KitchenSettleTriggerPublisher` (`@Component
    @RequiredArgsConstructor`): field `KafkaTemplate<String, SettleTriggerEvent> settleTriggerKafkaTemplate`,
    `@Value("${kitchen.events.settle-trigger-topic:kitchen.settlement-trigger}") String settleTriggerTopic`,
    body `settleTriggerKafkaTemplate.send(settleTriggerTopic, event.orderId().toString(), event)`.
    `KafkaKitchenTicketStatusChangedPublisher implements KitchenTicketStatusChangedPublisher`: field
    `KafkaTemplate<String, KitchenTicketStatusChangedEvent> ticketStatusChangedKafkaTemplate`,
    `@Value("${kitchen.events.ticket-status-changed-topic:kitchen.ticket-status-changed}") String topic`,
    body `.send(topic, event.orderId().toString(), event)`. Test: instantiate both producer configs
    directly (no Spring context) and assert each ProducerFactory's VALUE_SERIALIZER is JacksonJsonSerializer
    and KEY_SERIALIZER is StringSerializer, and that the KafkaTemplate beans are non-null — mirroring the
    broker-free config-test style.
  </action>
  <verify>
    <automated>./mvnw -o test -Dtest=KitchenKafkaProducerConfigTest</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenSettleTriggerProducerConfig` imports inventory's SettleTriggerEvent and defines `settleTriggerKafkaTemplate`
    - Both adapters key sends by `event.orderId().toString()` and use the two required topic-property defaults
    - `./mvnw -o test -Dtest=KitchenKafkaProducerConfigTest` passes
  </acceptance_criteria>
  <done>Kitchen can publish both events behind ports; settle-trigger targets the Phase-16 topic default keyed by orderId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kitchen producer → inventory/order consumers | Emitted events must match the locked contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | SettleTrigger contract drift | mitigate | Import the exact inventory `SettleTriggerEvent` record instead of redeclaring, guaranteeing field/type parity with the Phase-16 consumer |
| T-17-09 | Info Disclosure | Status event over-sharing | accept | Event carries only fulfillment identity (orderLineId + status); no PII or pricing, staff-internal topic |
| T-17-SC | Tampering | dependency installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `./mvnw -o test -Dtest=KitchenKafkaProducerConfigTest` green
- `grep -R "record SettleTriggerEvent" src/main/java/com/example/feat1/DDD/kitchen_context` returns nothing (imported, not redeclared)
- `grep -R "kitchen.settlement-trigger" src/main/java/com/example/feat1/DDD/kitchen_context` shows the correct topic default
</verification>

<success_criteria>
- Two working producers behind small ports; SettleTriggerEvent imported from inventory_context
- Status-changed event carries a full per-item snapshot for order-side derivation
</success_criteria>

<output>
Create `.planning/phases/17-kitchen-context/17-04-SUMMARY.md` when done
</output>
