---
phase: 17-kitchen-context
plan: 07
type: execute
wave: 3
depends_on: [17-01, 17-04]
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/TicketStatusChangedKafkaConsumerConfig.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/TicketStatusChangedListener.java
  - src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java
  - src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java
  - src/test/java/com/example/feat1/DDD/order_context/infrastructure/config/TicketStatusChangedKafkaConsumerConfigTest.java
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "order_context consumes KitchenTicketStatusChangedEvent and derives order status from the per-item snapshot: any item PREPARING→PREPARING, all READY→READY, all SERVED→SERVED, all COMPLETED→COMPLETED"
    - "The derived status is only applied if it advances the order forward (monotonic rank); replayed/out-of-order events never regress the order and never touch a REJECTED order"
    - "The consumer is idempotent (processed-events ledger) and a poison-pill payload routes to the DLT"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java"
      provides: "Idempotent forward-only order-status derivation"
      contains: "FULFILLMENT_RANK"
  key_links:
    - from: "TicketStatusChangedListener"
      to: "KitchenStatusProjectionService.onTicketStatusChanged"
      via: "one-line @KafkaListener delegate"
      pattern: "onTicketStatusChanged"
---

<objective>
Complete the fulfillment loop: order_context consumes kitchen's `KitchenTicketStatusChangedEvent`,
derives the aggregate order status from the full per-item snapshot, and applies it forward-only and
idempotently — never regressing, never mutated by kitchen directly. Delivers the consumer half of D-04
and closes the backward-transition landmine (Pitfall 3).

Purpose: Order status must reflect kitchen fulfillment purely by event, with a monotonic guard because
Kafka can redeliver/reorder.
Output: consumer config, thin listener, the projection service with a rank-based guard, and tests.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-kitchen-context/17-CONTEXT.md
@.planning/phases/17-kitchen-context/17-RESEARCH.md
@.planning/phases/17-kitchen-context/17-PATTERNS.md

<interfaces>
<!-- From plan 17-04: KitchenTicketStatusChangedEvent(UUID eventId, String eventType, Instant occurredAt,
     UUID orderId, UUID ticketId, List<ItemStatus> items); ItemStatus(UUID orderLineId, KitchenItemStatus status);
     KitchenItemStatus enum (kitchen_context.domain.model).
     From plan 17-01: OrderStatus with CONFIRMED, PREPARING, READY, SERVED, COMPLETED, REJECTED.
     Existing (reuse, no modification): OrderProcessedEventEntity/Repository (existsByEventIdAndConsumerName),
     OrderRepository (findById/save), OrderEntity.status. Consumer-config analog: OrderKafkaConsumerConfig. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TicketStatusChangedKafkaConsumerConfig (poison-pill-safe, order-side, cross-context trusted package)</name>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/TicketStatusChangedKafkaConsumerConfig.java,
    src/test/java/com/example/feat1/DDD/order_context/infrastructure/config/TicketStatusChangedKafkaConsumerConfigTest.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfig.java (consumer-config analog to clone),
    src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfigTest.java (broker-free config test analog)
  </read_first>
  <action>
    Clone `OrderKafkaConsumerConfig` retyped to `KitchenTicketStatusChangedEvent`. Because the event class
    lives in kitchen_context, set TRUSTED_PACKAGE =
    "com.example.feat1.DDD.kitchen_context.application.event" and VALUE_DEFAULT_TYPE =
    KitchenTicketStatusChangedEvent; USE_TYPE_INFO_HEADERS=false, ErrorHandlingDeserializer,
    DefaultErrorHandler `FixedBackOff(1000L, 3L)` + `addNotRetryableExceptions(DeserializationException.class)`,
    AckMode.RECORD. Name all beans with a distinct prefix to avoid collisions (Pitfall 5):
    `ticketStatusChangedDltKafkaTemplate`, `ticketStatusChangedConsumerFactory`,
    `ticketStatusChangedErrorHandler`, `ticketStatusChangedKafkaListenerContainerFactory`; group-id
    `${order.ticket-status-changed.consumer.group-id:order-ticket-status-changed}`, topic property
    `${kitchen.events.ticket-status-changed-topic:kitchen.ticket-status-changed}` (consumed later by the
    listener). Test: instantiate the config directly and assert deserializer class, TRUSTED_PACKAGES,
    VALUE_DEFAULT_TYPE, USE_TYPE_INFO_HEADERS=false, AckMode.RECORD, DeserializationException non-retryable.
  </action>
  <verify>
    <automated>./mvnw -o test -Dtest=TicketStatusChangedKafkaConsumerConfigTest</automated>
  </verify>
  <acceptance_criteria>
    - Config trusts `kitchen_context.application.event` and defaults to `KitchenTicketStatusChangedEvent`, all beans distinctly prefixed
    - `./mvnw -o test -Dtest=TicketStatusChangedKafkaConsumerConfigTest` passes
  </acceptance_criteria>
  <done>order_context can consume the kitchen status event poison-pill-safely with no bean-name collisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KitchenStatusProjectionService (idempotent, rank-based forward-only derivation) + thin listener</name>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java,
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/TicketStatusChangedListener.java,
    src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java (ledger + status-guard skeleton),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderStockResultListener.java (thin listener analog),
    .planning/phases/17-kitchen-context/17-PATTERNS.md (order-side projection / FULFILLMENT_RANK section)
  </read_first>
  <behavior>
    - Snapshot with any item PREPARING (not all further) → order set to PREPARING
    - Snapshot with all items READY → READY; all SERVED → SERVED; all COMPLETED → COMPLETED
    - Incoming derived status with rank <= current order rank → no change (out-of-order/replayed PREPARING after READY does not regress)
    - Order currently REJECTED → never modified
    - Duplicate eventId (ledger hit) → returns without change
    - Poison/replay does not create duplicate ledger rows or double-apply
  </behavior>
  <action>
    `KitchenStatusProjectionService` (`@Service @RequiredArgsConstructor`, `@Transactional
    onTicketStatusChanged(KitchenTicketStatusChangedEvent event)`): constant `CONSUMER_NAME =
    "kitchen-status-projection"`; reuse the existing `OrderProcessedEventRepository` inline idempotency
    guard (existsByEventIdAndConsumerName → early return; saveAndFlush ledger in try/catch on
    DataIntegrityViolationException). Load the order via `OrderRepository.findById(event.orderId())`
    (return if absent). Derive the target OrderStatus from `event.items()`: if any item status maps to
    PREPARING/READY/SERVED/COMPLETED apply the D-04 rule — any item still at PREPARING → PREPARING; else
    all READY → READY; all SERVED → SERVED; all COMPLETED → COMPLETED (map KitchenItemStatus→OrderStatus).
    Define `private static final Map<OrderStatus,Integer> FULFILLMENT_RANK = Map.of(CONFIRMED,0, PREPARING,1,
    READY,2, SERVED,3, COMPLETED,4)`. Guard: if order.getStatus()==REJECTED return; only
    `order.setStatus(target)` when `FULFILLMENT_RANK.getOrDefault(target,-1) > FULFILLMENT_RANK.getOrDefault(
    order.getStatus(),-1)` (strictly forward). Save only when changed. `TicketStatusChangedListener`
    (`@Component @RequiredArgsConstructor`): one-line `@KafkaListener` (topic
    `${kitchen.events.ticket-status-changed-topic:kitchen.ticket-status-changed}`, groupId
    `${order.ticket-status-changed.consumer.group-id:order-ticket-status-changed}`, containerFactory
    `ticketStatusChangedKafkaListenerContainerFactory`) delegating to the service. Test all behaviors with
    a mocked OrderRepository + OrderProcessedEventRepository.
  </action>
  <verify>
    <automated>./mvnw -o test -Dtest=KitchenStatusProjectionServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -o test -Dtest=KitchenStatusProjectionServiceTest` passes
    - Test asserts a replayed PREPARING after READY does NOT regress the order (rank guard)
    - Test asserts a REJECTED order is never modified and duplicate eventIds are idempotent
    - `TicketStatusChangedListener` body is a single delegate call
  </acceptance_criteria>
  <done>Order status is derived from kitchen's item snapshot, applied strictly forward and idempotently, never regressing or touching REJECTED orders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kitchen.ticket-status-changed topic → order consumer | Replayed/out-of-order/forged payloads cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Tampering | Backward status transition | mitigate | `FULFILLMENT_RANK` monotonic guard applies a derived status only if it strictly advances; REJECTED orders skipped (Pitfall 3) |
| T-17-18 | Spoofing / Tampering | Status event deserialization | mitigate | ErrorHandlingDeserializer + fixed VALUE_DEFAULT_TYPE + TRUSTED_PACKAGES (kitchen event pkg) + USE_TYPE_INFO_HEADERS=false; poison → DLT |
| T-17-19 | Repudiation | Duplicate application on replay | mitigate | `(eventId, kitchen-status-projection)` ledger guard on the existing order_processed_events table |
| T-17-SC | Tampering | dependency installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `./mvnw -o test -Dtest=TicketStatusChangedKafkaConsumerConfigTest,KitchenStatusProjectionServiceTest` green
- `grep -n "FULFILLMENT_RANK" KitchenStatusProjectionService.java` confirms the rank guard exists
- `grep -n "REJECTED" KitchenStatusProjectionService.java` confirms the terminal-state skip
</verification>

<success_criteria>
- Order status reflects fulfillment derived from kitchen's per-item snapshot, forward-only and idempotent
- No backward transition on replay/reorder; REJECTED orders untouched; kitchen never writes OrderEntity directly
</success_criteria>

<output>
Create `.planning/phases/17-kitchen-context/17-07-SUMMARY.md` when done
</output>
