---
phase: 17-kitchen-context
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/kitchen_context/domain/model/KitchenItemStatus.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/domain/model/KitchenDomainException.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketEntity.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketItemEntity.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketItemToppingSnapshot.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenProcessedEventEntity.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenTicketRepository.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenTicketItemRepository.java
  - src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenProcessedEventRepository.java
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "D-02: A KitchenTicket aggregate persists one row per confirmed order and one child KitchenTicketItem row per order line, each item independently lockable and URL-addressable"
    - "The per-item fulfillment lifecycle vocabulary QUEUED→PREPARING→READY→SERVED→COMPLETED exists as a documented monotonic enum"
    - "A KitchenTicketItem row can be pessimistically locked and looked up jointly by (orderId, itemId)"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketItemEntity.java"
      provides: "Independently lockable child entity with @ManyToOne to KitchenTicketEntity"
      contains: "@ManyToOne"
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenTicketItemRepository.java"
      provides: "Pessimistic lock + dual-key lookup"
      contains: "PESSIMISTIC_WRITE"
    - path: "src/main/java/com/example/feat1/DDD/kitchen_context/domain/model/KitchenItemStatus.java"
      provides: "Lifecycle enum"
      contains: "QUEUED"
  key_links:
    - from: "KitchenTicketItemEntity.status"
      to: "KitchenItemStatus"
      via: "@Enumerated(EnumType.STRING)"
      pattern: "KitchenItemStatus"
---

<objective>
Create the `kitchen_context` foundation: the fulfillment lifecycle enum, the stable domain exception,
the JPA aggregate (`KitchenTicketEntity` + full-entity `KitchenTicketItemEntity` + topping snapshot +
processed-events ledger), and the repositories — including the pessimistic-lock + dual-key lookup that
later closes the double-settle-trigger and IDOR landmines. This is the structural half of D-02; no
behavior yet.

Purpose: Every later kitchen plan (consumer, advance service, board) builds on these entities/repos.
Output: 2 domain-model files, 4 entity files, 3 repository files — a compiling `kitchen_context` skeleton.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-kitchen-context/17-CONTEXT.md
@.planning/phases/17-kitchen-context/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kitchen domain model — lifecycle enum and stable domain exception</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/domain/model/KitchenItemStatus.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/domain/model/KitchenDomainException.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/domain/model/OrderStatus.java (bare-enum analog),
    src/main/java/com/example/feat1/DDD/order_context/domain/model/OrderDomainException.java (exact exception analog),
    src/main/java/com/example/feat1/common/exception/AppException.java (base class)
  </read_first>
  <action>
    Create `KitchenItemStatus` in package `com.example.feat1.DDD.kitchen_context.domain.model` with
    values `QUEUED, PREPARING, READY, SERVED, COMPLETED` in that exact order, plus a comment stating the
    declaration order is semantically load-bearing (single-step forward-only transitions in plan 17-05
    depend on it). Create `KitchenDomainException extends AppException` mirroring `OrderDomainException`:
    `public static final String` code constants `KITCHEN_TICKET_NOT_FOUND`, `KITCHEN_ITEM_NOT_FOUND`,
    `KITCHEN_TRANSITION_INVALID`, and static factories `ticketNotFound()`, `itemNotFound()`
    (both `HttpStatus.NOT_FOUND`), and `transitionInvalid()` (`HttpStatus.BAD_REQUEST`, matching
    `TableDomainException.reservationStatusInvalid`'s status choice). Constructor delegates to
    `super(code, message, status)`.
  </action>
  <verify>
    <automated>./mvnw -q -o test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenItemStatus` has exactly the five values in QUEUED→COMPLETED order
    - `KitchenDomainException` extends `AppException` and exposes the three static factories with distinct KITCHEN_* codes
    - `./mvnw -o test-compile` succeeds
  </acceptance_criteria>
  <done>Lifecycle vocabulary and stable error codes exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: JPA aggregate — KitchenTicket, KitchenTicketItem (full entity), topping snapshot, processed-events ledger</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketEntity.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketItemEntity.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenTicketItemToppingSnapshot.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/entity/KitchenProcessedEventEntity.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java (aggregate-root analog),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderLineEntity.java (full-entity child analog),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderLineToppingSnapshot.java (@Embeddable analog),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderProcessedEventEntity.java (ledger analog)
  </read_first>
  <action>
    `KitchenTicketEntity` (`@Entity @Table(name = "kitchen_tickets")`, Lombok `@Getter/@Setter`): UUID
    `id` (`@GeneratedValue(strategy = GenerationType.UUID)`), `UUID orderId` (nullable=false, add a
    unique constraint so one confirmed order maps to one ticket), `Instant createdAt`, and
    `@OneToMany(mappedBy = "ticket", cascade = ALL, orphanRemoval = true, fetch = LAZY) List<KitchenTicketItemEntity> items`.
    `KitchenTicketItemEntity` (`@Entity @Table(name = "kitchen_ticket_items")`) modeled as a FULL entity
    like `OrderLineEntity` (Open Question #2 is resolved — do NOT use @Embeddable/@ElementCollection):
    UUID `id`, `@ManyToOne(fetch = LAZY, optional = false) @JoinColumn(name = "ticket_id") KitchenTicketEntity ticket`,
    `UUID orderLineId` (the source order line, used as SettleTrigger `orderLineId`), `UUID dishId`,
    `String dishName`, `int quantity`, `@Enumerated(EnumType.STRING) @Column(nullable=false)
    KitchenItemStatus status = KitchenItemStatus.QUEUED`, and an `@ElementCollection` of
    `KitchenTicketItemToppingSnapshot` (table `kitchen_ticket_item_toppings`, join column `item_id`,
    `@OrderColumn(name="sort_order")`). `KitchenTicketItemToppingSnapshot` is `@Embeddable` cloning
    `OrderLineToppingSnapshot`'s identity columns (toppingGroupId/Name, toppingOptionId/Name) — drop the
    price column kitchen does not need. `KitchenProcessedEventEntity` clones `OrderProcessedEventEntity`
    verbatim but `@Table(name = "kitchen_processed_events")` with unique constraint
    `uq_kitchen_processed_event` on `{event_id, consumer_name}`.
  </action>
  <verify>
    <automated>./mvnw -q -o test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenTicketItemEntity` is a full `@Entity` with `@ManyToOne` back to `KitchenTicketEntity` and an `orderLineId` column (not @Embeddable)
    - `KitchenTicketEntity` has a unique constraint on `orderId` and a cascading `@OneToMany` items list
    - `KitchenProcessedEventEntity` maps table `kitchen_processed_events` with a unique `{event_id, consumer_name}` constraint
    - `./mvnw -o test-compile` succeeds
  </acceptance_criteria>
  <done>The aggregate persists one ticket per order and one lockable, URL-addressable item per line; ledger table is distinct from order/inventory ledgers.</done>
</task>

<task type="auto">
  <name>Task 3: Repositories — ticket, item (pessimistic lock + dual-key lookup), processed-events</name>
  <files>
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenTicketRepository.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenTicketItemRepository.java,
    src/main/java/com/example/feat1/DDD/kitchen_context/infrastructure/repository/KitchenProcessedEventRepository.java
  </files>
  <read_first>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java (PESSIMISTIC_WRITE lock analog),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderProcessedEventRepository.java (ledger repo analog),
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapter.java (dual-key lookup analog)
  </read_first>
  <action>
    `KitchenTicketRepository extends JpaRepository<KitchenTicketEntity, UUID>` with
    `boolean existsByOrderId(UUID orderId)` and `Optional<KitchenTicketEntity> findByOrderId(UUID orderId)`.
    `KitchenTicketItemRepository extends JpaRepository<KitchenTicketItemEntity, UUID>` with a
    `@Lock(LockModeType.PESSIMISTIC_WRITE) @Query("select i from KitchenTicketItemEntity i where i.id = :id
    and i.ticket.orderId = :orderId") Optional<KitchenTicketItemEntity> lockByOrderIdAndItemId(UUID orderId,
    UUID id)` — the dual-key predicate both closes Pitfall 1 (double-settle race: lock acquired before the
    status check) and the IDOR threat (item must belong to the ticket for the path's orderId). Also add a
    read-only `List<KitchenTicketItemEntity> findByStatusNot(KitchenItemStatus status)` for the board query.
    `KitchenProcessedEventRepository extends JpaRepository<KitchenProcessedEventEntity, UUID>` with
    `boolean existsByEventIdAndConsumerName(UUID eventId, String consumerName)`.
  </action>
  <verify>
    <automated>./mvnw -q -o test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `KitchenTicketItemRepository` declares a `@Lock(LockModeType.PESSIMISTIC_WRITE)` method keyed by BOTH orderId and item id
    - `KitchenTicketItemRepository` exposes `findByStatusNot(...)` for the board
    - `KitchenProcessedEventRepository` exposes `existsByEventIdAndConsumerName`
    - `./mvnw -o test-compile` succeeds
  </acceptance_criteria>
  <done>Repositories provide the locked dual-key item fetch, board query, and idempotency existence check the later plans consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `{orderId}`/`{itemId}` path → item row | An item must be reachable only through its owning order |
| concurrent PATCH → same item row | Two transactions must not both read QUEUED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering / Info Disclosure | KitchenTicketItem lookup | mitigate | `lockByOrderIdAndItemId` requires both keys, preventing IDOR across orders (mirrors OrderLineLookupAdapter dual-key pattern) |
| T-17-04 | Tampering | Concurrent item transition | mitigate | `@Lock(PESSIMISTIC_WRITE)` on the item-row query serializes concurrent advances (the lock method the plan-05 landmine fix depends on) |
| T-17-SC | Tampering | dependency installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `./mvnw -o test-compile` green
- `grep -R "PESSIMISTIC_WRITE" src/main/java/com/example/feat1/DDD/kitchen_context` shows the item lock
- `grep -R "kitchen_processed_events" src/main/java/com/example/feat1/DDD/kitchen_context` shows a distinct ledger table
</verification>

<success_criteria>
- kitchen_context compiles with a full-entity aggregate, lockable/dual-key item repository, and a dedicated ledger table
- KitchenTicketItemEntity carries `orderLineId` (for the SettleTrigger) and a QUEUED-default status
</success_criteria>

<output>
Create `.planning/phases/17-kitchen-context/17-02-SUMMARY.md` when done
</output>
