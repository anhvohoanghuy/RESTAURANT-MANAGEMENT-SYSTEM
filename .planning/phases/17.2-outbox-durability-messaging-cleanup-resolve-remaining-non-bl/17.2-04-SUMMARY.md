---
phase: 17.2-outbox-durability-messaging-cleanup
plan: 04
subsystem: messaging
tags: [kafka, outbox, dead-code-removal, order-context, inventory-context]

# Dependency graph
requires:
  - phase: 17.1
    provides: transactional outbox cutover for order/inventory saga events (I-WR-02), making the two Kafka publisher classes dead code
provides:
  - Removal of the last two direct-publish Kafka adapters (order + inventory), their domain ports, and the four typed producer beans wired only for them
  - Grep-verified proof that src/main has zero references to KafkaOrderEventPublisher/OrderEventPublisher/KafkaInventoryStockResultPublisher/InventoryStockResultPublisher
affects: [17.2-05, 17.2-06, any future phase touching order_context or inventory_context Kafka wiring]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - src/test/java/com/example/feat1/DDD/messaging/KafkaPublisherWhenCompleteTest.java
    - src/test/java/com/example/feat1/DDD/payment_context/integration/PaymentCheckoutIntegrationTest.java

key-decisions:
  - "Deleted KafkaOrderEventPublisher, OrderEventPublisher port, and OrderKafkaProducerConfig (orderCreatedKafkaTemplate/orderConfirmedKafkaTemplate producer beans) — grep-proven zero non-comment callers in src/main before deletion."
  - "Deleted KafkaInventoryStockResultPublisher, InventoryStockResultPublisher port, and InventoryKafkaProducerConfig (orderStockResultKafkaTemplate producer bean) — grep-proven zero references in src/main and src/test before deletion."
  - "Left OutboxConfig, PaymentKafkaProducerConfig, TableKafkaProducerConfig, KitchenSettleTriggerProducerConfig, and KitchenTicketStatusChangedProducerConfig untouched — these back live producers/the outbox relay, not the dead publishers."

requirements-completed: [WR-03]

# Metrics
duration: 2min
completed: 2026-07-10
---

# Phase 17.2 Plan 04: Delete Dead Kafka Publishers Summary

**Deleted the two dead direct-publish Kafka adapters (order + inventory) left over from the pre-outbox saga, their domain ports, and the four now-unused producer beans, fixing the two tests that referenced them.**

## Performance

- **Duration:** ~2 min (commit-to-commit)
- **Started:** 2026-07-10T09:40:02+07:00
- **Completed:** 2026-07-10T09:41:48+07:00
- **Tasks:** 2 completed
- **Files modified:** 8 (6 deleted, 2 test files edited)

## Accomplishments
- Deleted `KafkaOrderEventPublisher`, `OrderEventPublisher` port, and `OrderKafkaProducerConfig` (4 producer beans: orderCreated/orderConfirmed factory + template pairs).
- Deleted `KafkaInventoryStockResultPublisher`, `InventoryStockResultPublisher` port, and `InventoryKafkaProducerConfig` (orderStockResult factory + template pair).
- Fixed `KafkaPublisherWhenCompleteTest` (removed 2 orderEventPublisher-* tests, the orderEventAppender field, and its setup/teardown; KafkaKitchenSettleTriggerPublisher tests unchanged) and `PaymentCheckoutIntegrationTest` (removed the now-superfluous `@MockitoBean OrderEventPublisher orderEventPublisher` field + import).
- Grep-verified `src/main` has zero non-comment references to any of the four deleted class names.
- Full Maven suite green: 205 tests, 0 failures/errors — no `@SpringBootTest` context-load regression from the removed beans.

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete order-side dead publisher stack + fix its test references** - `43aa4f2` (feat) + `38462d7` (test)
2. **Task 2: Delete inventory-side dead publisher stack + full-suite regression** - `fdc242c` (feat)

_Note: Task 1's `git add` on already-staged deletions failed mid-command on the first pathspec, which silently dropped the two test-file edits from that commit; they were staged and committed separately in `38462d7` immediately after. Both commits together represent the complete, verified Task 1 change set._

## Files Created/Modified
- `src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java` - deleted (dead direct-publish adapter)
- `src/main/java/com/example/feat1/DDD/order_context/domain/port/OrderEventPublisher.java` - deleted (port with zero remaining impls/callers)
- `src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java` - deleted (4 producer beans consumed only by the deleted adapter)
- `src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/KafkaInventoryStockResultPublisher.java` - deleted (dead direct-publish adapter)
- `src/main/java/com/example/feat1/DDD/inventory_context/domain/port/InventoryStockResultPublisher.java` - deleted (port with zero remaining impls/callers)
- `src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java` - deleted (producer bean consumed only by the deleted adapter)
- `src/test/java/com/example/feat1/DDD/messaging/KafkaPublisherWhenCompleteTest.java` - removed order-event-publisher tests/appender; settle-trigger tests intact
- `src/test/java/com/example/feat1/DDD/payment_context/integration/PaymentCheckoutIntegrationTest.java` - removed unused `@MockitoBean OrderEventPublisher` field/import

## Decisions Made
- Confirmed via grep gate before each deletion (per plan's `<action>` instructions and WR-03 locked decision) that no code in `src/main` or `src/test` referenced the target classes beyond their own impl/port files (order-side had 2 stray javadoc/comment mentions in the still-live inventory files, which is expected and harmless — those files are updated separately and don't count as functional references).
- Did not touch `OutboxConfig`, consumer configs, or Payment/Table producer configs — verified they remain present and unmodified after both deletions.

## Deviations from Plan

None - plan executed exactly as written. (One execution-mechanics note: the Task 1 test-file edits landed in a follow-up commit `38462d7` rather than being folded into `43aa4f2`, because a `git add` command with multiple pathspecs failed fast on an already-staged deleted-file pathspec before reaching the test files — not a deviation from plan content, just from single-commit-per-task mechanics. Both commits are tagged `17.2-04` and together deliver exactly Task 1's scope.)

## Issues Encountered
None beyond the commit-splitting note above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The outbox is now unambiguously the sole egress path for order-created, order-confirmed, and inventory stock-result events — no code in `src/main` implies a competing direct-publish path.
- Full suite green (205 tests) with no regressions; ready for the remaining 17.2 plans (WR-01, WR-02, IN-02, IN-03, WR-05, WR-06) which are independent of this change.

---
*Phase: 17.2-outbox-durability-messaging-cleanup*
*Completed: 2026-07-10*
