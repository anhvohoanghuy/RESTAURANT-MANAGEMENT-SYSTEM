---
phase: 17.2-outbox-durability-messaging-cleanup
plan: 06
subsystem: infra
tags: [spring-boot, application-properties, config-hardening, env-vars, secrets]

# Dependency graph
requires: []
provides:
  - "Env-driven DB credentials in application.properties (${DB_USERNAME:root} / ${DB_PASSWORD:123456})"
  - "Root .env.example documenting DB_USERNAME/DB_PASSWORD dev-only defaults"
  - ".gitignore entry for .env to prevent accidental secret commits"
affects: [deployment, config, security]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Placeholder-with-default env idiom (${VAR:default}) applied to datasource credentials, matching existing ${AUTH_...:default}/${JWT_SECRET:...}/${KAFKA_BOOTSTRAP_SERVERS:...} usages already in application.properties"

key-files:
  created:
    - .env.example
  modified:
    - src/main/resources/application.properties
    - .gitignore

key-decisions:
  - "Only spring.datasource.username/password were externalized; spring.datasource.url and driver-class-name left untouched per plan scope"
  - "Test profile (src/test/resources/application.properties) left unchanged — H2 in-memory sa/empty creds are not a secret-disclosure concern"
  - "Added .env to .gitignore since no such pattern existed previously, ensuring a real local .env (if created by a developer) is never tracked"

patterns-established:
  - "Datasource credentials now follow the same ${VAR:default} idiom as JWT/Kafka/Auth config in application.properties"

requirements-completed: [WR-06]

# Metrics
duration: 6min
completed: 2026-07-10
---

# Phase 17.2 Plan 06: Externalize DB Credentials Summary

**Replaced plaintext `spring.datasource.username=root` / `password=123456` in application.properties with `${DB_USERNAME:root}` / `${DB_PASSWORD:123456}` env placeholders, matching the existing `${AUTH_...:default}` idiom, plus a documenting `.env.example` and a `.gitignore` entry for `.env`.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-07-10T02:33:00Z (approx)
- **Completed:** 2026-07-10T02:39:48Z
- **Tasks:** 1 completed
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- `application.properties` no longer contains a bare plaintext DB password; both `spring.datasource.username` and `spring.datasource.password` are env-driven with safe local-dev defaults matching the pre-existing values (`root`/`123456`), so no behavior change for developers who don't set the env vars.
- Added root `.env.example` documenting `DB_USERNAME`/`DB_PASSWORD` with explicit "dev-only placeholder, not real credentials" framing.
- Added a `.env` pattern to `.gitignore` (none existed previously) so a real local `.env` file is never accidentally committed.
- Confirmed the app config still resolves placeholders correctly by running `OrderConfirmationServiceTest`, which boots the Spring context (H2 test profile is untouched and unaffected by this change) — passed with exit code 0.

## Task Commits

Each task was committed atomically:

1. **Task 1: Externalize DB credentials to env placeholders + .env.example** - `09a7550` (fix)

**Plan metadata:** (this commit, docs — created by orchestrator/self after SUMMARY)

_Note: single-task plan; no TDD multi-commit sequence._

## Files Created/Modified
- `src/main/resources/application.properties` - `spring.datasource.username`/`password` now use `${DB_USERNAME:root}`/`${DB_PASSWORD:123456}` placeholder-with-default syntax; `spring.datasource.url` and driver line untouched.
- `.env.example` - New root-level file documenting `DB_USERNAME`/`DB_PASSWORD` non-secret local-dev defaults.
- `.gitignore` - Added `.env` entry under a new `### Environment ###` section.

## Decisions Made
- Kept the exact same default values (`root`/`123456`) as before so local/dev developer experience is unchanged — only the *source* of the value changed (env var takes precedence over the compiled-in default).
- Did not touch `spring.datasource.url` or `driver-class-name` — plan explicitly scoped this to username/password only.
- Left `src/test/resources/application.properties` (H2 profile with `sa`/empty password) completely unchanged, per plan's explicit instruction and because H2 in-memory test credentials are not a secret-disclosure concern.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Local developers may optionally copy `.env.example` to `.env` and adjust values, but the existing defaults (`root`/`123456`) continue to work exactly as before if they take no action.

## Next Phase Readiness

WR-06 is closed. `application.properties` contains no bare plaintext DB password; production/staging environments can now override `DB_USERNAME`/`DB_PASSWORD` via real environment variables without any code change. Migrating to a full secret-manager solution remains explicitly out of scope (per 17.2-CONTEXT deferred items) and is not blocked by this change.

---
*Phase: 17.2-outbox-durability-messaging-cleanup*
*Completed: 2026-07-10*
