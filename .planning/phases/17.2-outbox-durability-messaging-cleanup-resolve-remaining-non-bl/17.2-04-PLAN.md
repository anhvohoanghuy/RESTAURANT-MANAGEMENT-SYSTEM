---
phase: 17.2-outbox-durability-messaging-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java
  - src/main/java/com/example/feat1/DDD/order_context/domain/port/OrderEventPublisher.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/KafkaInventoryStockResultPublisher.java
  - src/main/java/com/example/feat1/DDD/inventory_context/domain/port/InventoryStockResultPublisher.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java
  - src/test/java/com/example/feat1/DDD/messaging/KafkaPublisherWhenCompleteTest.java
  - src/test/java/com/example/feat1/DDD/payment_context/integration/PaymentCheckoutIntegrationTest.java
autonomous: true
requirements: [WR-03]
must_haves:
  truths:
    - "After the outbox cutover, no code in src/main references KafkaOrderEventPublisher, OrderEventPublisher, KafkaInventoryStockResultPublisher, or InventoryStockResultPublisher"
    - "The typed producer beans wired ONLY for the deleted publishers (orderCreated/orderConfirmed/orderStockResult KafkaTemplate) are removed"
    - "The full test suite stays green after deletion (tests that only exercised the dead publishers are updated/removed)"
  artifacts: []
  key_links:
    - from: "src/main"
      to: "(none)"
      via: "grep proves zero references to the deleted classes"
      pattern: "KafkaOrderEventPublisher|KafkaInventoryStockResultPublisher|OrderEventPublisher|InventoryStockResultPublisher"
---

<objective>
Close WR-03: after the transactional-outbox cutover, `KafkaOrderEventPublisher` and
`KafkaInventoryStockResultPublisher` (and their ports + the typed producer configs wired only for them)
have zero callers in `src/main` but still carry `whenComplete` logging — misleading dead code implying
a live direct-publish path. Delete them and the now-unused producer beans.

Purpose: Remove a false "direct publish" path so the outbox is unambiguously the only egress.
Output: Six deleted source files + two updated test files; grep-verified clean; suite green.

Verified pre-conditions (from planning grep):
- `OrderEventPublisher` has ZERO non-comment injectors in src/main (only the impl + port).
- `InventoryStockResultPublisher` has ZERO non-comment injectors in src/main (InventoryReservationService
  no longer depends on it post-cutover).
- `orderCreatedKafkaTemplate`, `orderConfirmedKafkaTemplate`, `orderStockResultKafkaTemplate` beans are
  consumed ONLY by the two dead publishers.
- Test refs: `KafkaPublisherWhenCompleteTest` exercises `KafkaOrderEventPublisher`;
  `PaymentCheckoutIntegrationTest` has `@MockitoBean private OrderEventPublisher orderEventPublisher`.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete order-side dead publisher stack + fix its test references</name>
  <files>src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java, src/main/java/com/example/feat1/DDD/order_context/domain/port/OrderEventPublisher.java, src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java, src/test/java/com/example/feat1/DDD/messaging/KafkaPublisherWhenCompleteTest.java, src/test/java/com/example/feat1/DDD/payment_context/integration/PaymentCheckoutIntegrationTest.java</files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java
    - src/main/java/com/example/feat1/DDD/order_context/domain/port/OrderEventPublisher.java
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java (defines orderCreatedProducerFactory/orderCreatedKafkaTemplate/orderConfirmedProducerFactory/orderConfirmedKafkaTemplate — all consumed only by the dead publisher)
    - src/test/java/com/example/feat1/DDD/messaging/KafkaPublisherWhenCompleteTest.java (two tests + orderEventAppender reference KafkaOrderEventPublisher; the KafkaKitchenSettleTriggerPublisher tests must remain)
    - src/test/java/com/example/feat1/DDD/payment_context/integration/PaymentCheckoutIntegrationTest.java (@MockitoBean OrderEventPublisher orderEventPublisher + its import)
    - .planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md (WR-03 locked decision: verify by grep before deletion; do NOT delete config still used by the outbox relay or Payment/Table producers)
  </read_first>
  <action>
    Re-run the grep gate first: confirm `grep -rn "OrderEventPublisher" src/main` (excluding comment/javadoc
    lines) returns only the impl + port, and that orderCreatedKafkaTemplate/orderConfirmedKafkaTemplate are
    referenced only by KafkaOrderEventPublisher. Then delete KafkaOrderEventPublisher.java, the
    OrderEventPublisher port, and OrderKafkaProducerConfig.java (all four producer beans). In
    KafkaPublisherWhenCompleteTest, remove the two orderEventPublisher-* @Test methods, the orderEventAppender
    field, and its attach/detach setup/teardown — leaving the KafkaKitchenSettleTriggerPublisher tests intact
    and compiling. In PaymentCheckoutIntegrationTest, remove the `@MockitoBean private OrderEventPublisher
    orderEventPublisher;` field and its import (no bean now requires it). Do NOT touch OutboxConfig, the
    consumer configs, or Payment/Table producer configs.
  </action>
  <acceptance_criteria>
    - <automated>bash -c 'grep -rn "OrderEventPublisher\|KafkaOrderEventPublisher" src/main | grep -v "^[^:]*:[0-9]*: *\(\*\|//\)" | grep -c . ; test $? -eq 1'</automated> — no non-comment references remain in src/main (command exits success when count is zero).
    - The three order-side source files are deleted; the two order-side tests no longer reference the deleted class.
    - <automated>./mvnw -q test -Dtest=KafkaPublisherWhenCompleteTest</automated> passes (settle-trigger tests retained).
  </acceptance_criteria>
  <verify>
    <automated>./mvnw -q test -Dtest=KafkaPublisherWhenCompleteTest</automated>
  </verify>
  <done>Order-side dead publisher, port, and producer config are gone; dependent tests updated and green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete inventory-side dead publisher stack + full-suite regression</name>
  <files>src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/KafkaInventoryStockResultPublisher.java, src/main/java/com/example/feat1/DDD/inventory_context/domain/port/InventoryStockResultPublisher.java, src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java</files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/KafkaInventoryStockResultPublisher.java
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/port/InventoryStockResultPublisher.java
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java (orderStockResultProducerFactory/orderStockResultKafkaTemplate — consumed only by the dead inventory publisher)
    - .planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md (WR-03)
  </read_first>
  <action>
    Grep-verify `grep -rn "InventoryStockResultPublisher" src/main` (excluding comments) returns only the impl +
    port, and that orderStockResultKafkaTemplate is referenced only by KafkaInventoryStockResultPublisher. Then
    delete KafkaInventoryStockResultPublisher.java, the InventoryStockResultPublisher port, and
    InventoryKafkaProducerConfig.java. Grep src/test for any remaining reference to either inventory class and
    remove/update it if present. Then run the FULL suite to confirm no context-load regression from the removed
    beans.
  </action>
  <acceptance_criteria>
    - <automated>bash -c 'grep -rn "InventoryStockResultPublisher\|KafkaInventoryStockResultPublisher" src/main | grep -v "^[^:]*:[0-9]*: *\(\*\|//\)" | grep -c . ; test $? -eq 1'</automated> — zero non-comment references remain in src/main.
    - The three inventory-side source files are deleted; no src/test reference to the deleted classes remains.
    - <automated>./mvnw test</automated> — full suite green (no @SpringBootTest context-load failure from removed producer beans).
  </acceptance_criteria>
  <verify>
    <automated>./mvnw test</automated>
  </verify>
  <done>Inventory-side dead publisher, port, and producer config are gone; full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code egress paths to Kafka | Dead direct-publish adapters imply a second, unaudited egress alongside the outbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-08 | Repudiation/Maintainability | dead publishers implying a live direct-publish path | mitigate | delete adapters, ports, and their producer beans; grep-verify zero references |
| T-17.2-09 | Integrity | deleting config still used by outbox relay or Payment/Table producers | accept/mitigate | delete ONLY beans grep-proven unique to the dead publishers; leave OutboxConfig/consumer/Payment/Table configs intact |
</threat_model>

<verification>
- `grep -rn "KafkaOrderEventPublisher\|KafkaInventoryStockResultPublisher\|OrderEventPublisher\|InventoryStockResultPublisher" src/main` returns no non-comment lines.
- `./mvnw test` full suite green.
</verification>

<success_criteria>
Grep of src/main returns no reference to either deleted publisher class (or their ports); the suite stays green.
</success_criteria>

<output>
Create `.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-04-SUMMARY.md` when done.
</output>
