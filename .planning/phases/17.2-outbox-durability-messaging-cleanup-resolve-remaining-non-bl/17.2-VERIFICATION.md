---
phase: 17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl
verified: 2026-07-10T02:57:39Z
status: gaps_found
score: 9/9 must-haves verified
has_blocking_gaps: false
overrides_applied: 0
gaps:
  - truth: "OutboxEventRepository.claimPending's documented cross-instance dedup guarantee ('lets concurrent app instances poll the same table without double-publishing a row') holds after the WR-02 per-row-transaction refactor"
    status: partial
    severity: minor
    reason: "De-transactionalizing OutboxRelay.poll() (required by WR-02) releases the FOR UPDATE SKIP LOCKED row locks as soon as claimPending() returns, before OutboxRowPublisher.publish(row) opens its own per-row transaction. claimPending does not transition rows to an intermediate claimed/processing status, so the rows remain PENDING and visible to a second poller. A second app instance's poll() could re-claim and re-publish the same rows. Impact is bounded: the outbox is at-least-once by design and every downstream consumer (OrderConfirmationService, KitchenStatusProjectionService, InventoryReservationService) is protected by the CR-01 idempotency ledger verified intact in this phase, so this is duplicate-publish amplification, not data loss or double business-effect. Single-instance deployments (the current @Scheduled(fixedDelay) topology) are unaffected. This is an inherent, foreseeable side effect of the WR-02 requirement itself (whole-batch transaction was the only thing holding the lock across the batch) rather than an implementation defect; the claimPending Javadoc's multi-instance claim is now stale and should be corrected or the claim should be made durable in a follow-up."
    artifacts:
      - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java"
        issue: "poll() no longer holds a transaction spanning claim+publish, so SKIP LOCKED protection does not extend to the publish step"
      - path: "src/main/java/com/example/feat1/DDD/shared/outbox/repository/OutboxEventRepository.java"
        issue: "claimPending Javadoc still asserts a multi-instance no-double-publish guarantee that the current architecture no longer provides"
    missing:
      - "Either transition claimed rows to an intermediate CLAIMING/PROCESSING status inside a short claim transaction, or update the Javadoc/design docs to stop asserting cross-instance dedup safety"
  - truth: "OutboxRelay.poll() isolates each row's failure from its siblings for ALL failure modes, not only Kafka send failures"
    status: partial
    severity: minor
    reason: "OutboxRowPublisher.publish()'s try/catch covers the Kafka send only; the trailing outboxEventRepository.save(row) (line 75) sits outside the catch. If save() throws (constraint violation, connection error), the exception propagates out of publish() into poll()'s for-loop, which has no try/catch, aborting the rest of the batch. The plan's literal, tested acceptance criterion — a mid-batch SEND failure does not roll back or re-drive already-SENT siblings — is verified true by OutboxRelayTest.siblingRowsAreNotRolledBackWhenMiddleRowFails (mock-based, send failure path). The narrower persistence-exception path is untested and unhandled."
    artifacts:
      - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRelay.java"
        issue: "for-loop calling outboxRowPublisher.publish(row) has no try/catch"
      - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRowPublisher.java"
        issue: "save(row) at line 75 is outside the try/catch that handles send failures"
    missing:
      - "Wrap the outboxRowPublisher.publish(row) call in poll()'s loop with a try/catch that logs and continues, per the review's suggested fix"
  - truth: "InterruptedException during a Kafka send wait is handled distinctly from an ordinary send failure"
    status: failed
    severity: minor
    reason: "OutboxRowPublisher.publish()'s catch (Exception ex) block absorbs InterruptedException (thrown by future.get(SEND_TIMEOUT_SECONDS, ...)) as an ordinary retryable failure and never calls Thread.currentThread().interrupt(), clearing the thread's interrupt status. This can mask a cancellation/shutdown signal. Narrow robustness gap, not part of any of the 7 phase requirements' stated acceptance criteria."
    artifacts:
      - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRowPublisher.java"
        issue: "catch (Exception ex) at line ~52 does not special-case InterruptedException"
    missing:
      - "Add a catch (InterruptedException ie) branch that calls Thread.currentThread().interrupt() before the generic catch (Exception ex)"
  - truth: "OutboxRetentionJob.sweepSentRetention guards against a misconfigured non-positive outbox.retention.ttl-days"
    status: failed
    severity: minor
    reason: "ttlDays has no lower-bound validation. A negative value makes the cutoff a future instant, deleting every SENT row (retention is defeated but PENDING/FAILED rows are still never touched, so no data-loss beyond intended SENT pruning). A value of 0 has a similar effect. Configuration-misuse edge case, not exercised by IN-02's stated acceptance criteria (which only requires correct behavior for a valid positive window, verified by OutboxRetentionJobTest)."
    artifacts:
      - path: "src/main/java/com/example/feat1/DDD/shared/outbox/infrastructure/OutboxRetentionJob.java"
        issue: "no `if (ttlDays < 1) { ... return; }` guard on the configured ttlDays value"
    missing:
      - "Clamp or reject ttlDays < 1 at startup or inside sweepSentRetention"
  - truth: "OrderEntity.rejectionReason's JPA mapping matches its documented TEXT-column intent"
    status: partial
    severity: minor
    reason: "@Column(length = REJECTION_REASON_MAX_LEN=60000) with no @Lob/columnDefinition maps to VARCHAR(60000) under Hibernate's MySQL dialect. On MySQL utf8mb4 the max declarable VARCHAR is 16383 characters, so this can fail DDL at schema-creation time (ddl-auto=update) even though app-level truncation caps the value at 60000 chars. This is explicitly a pre-existing condition carried over unchanged from Phase 17.1 (the same length value, just previously literal 65535 vs now derived 60000) — this phase's IN-03 plan only unified the constant, it did not introduce or worsen the VARCHAR/TEXT mismatch. Flagged by the 17.2 code review as newly re-surfaced because IN-03 touched this exact line, but the underlying DDL risk predates Phase 17.2."
    artifacts:
      - path: "src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java"
        issue: "rejection_reason column lacks @Lob / columnDefinition=\"TEXT\", unlike the sibling OutboxEventEntity.payload column which correctly uses @Lob for the same reason"
    missing:
      - "Add @Lob (or columnDefinition=\"TEXT\") to rejection_reason, mirroring OutboxEventEntity.payload"
deferred: []
---

# Phase 17.2: Outbox Durability + Messaging Cleanup Verification Report

**Phase Goal:** Resolve the 7 remaining non-blocking robustness/hygiene findings deferred from Phase 17.1 — WR-01 outbox wire-format round-trip coverage, WR-02 relay per-row transactions, WR-03 delete dead direct-publish adapters, IN-02 outbox retention + FAILED-row surfacing, IN-03 unified rejection-reason cap constant, WR-05 explicit ordinal-free kitchen fulfillment rank map, WR-06 externalized DB credentials. No new features/deps; test suite stays green; Phase 17.1's 21 verified truths (incl. CR-01) must NOT regress.
**Verified:** 2026-07-10T02:57:39Z
**Status:** gaps_found (all gaps are `severity: minor`, `has_blocking_gaps: false` — phase goal IS achieved; these route to backlog, not escalation)
**Re-verification:** No — initial verification

## IMPORTANT: Finding-ID collision notice

The 17.2 code-review report (`17.2-REVIEW.md`) reuses the labels `WR-01`..`WR-05`/`IN-01`..`IN-02` for **its own, newly-discovered findings against the phase's implementation** — these are DIFFERENT items from the phase's 7 requirement IDs (`WR-01, WR-02, WR-03, IN-02, IN-03, WR-05, WR-06`) that share some of the same numbers by coincidence. Below, "Requirement WR-0N" always refers to the phase's original 7 findings (from `17.2-CONTEXT.md`/`ROADMAP.md`); "Review finding WR-0N" always refers to the newly-discovered review-report items, disambiguated by file:line.

## Goal Achievement

### Observable Truths — 7 Phase Requirements

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | WR-01: `Instant`+`BigDecimal` survive OutboxWriter serialize → consumer `JacksonJsonDeserializer` deserialize losslessly | ✓ VERIFIED | `src/test/java/.../shared/outbox/OutboxWireFormatRoundTripTest.java` exists, uses the real `@Autowired ObjectMapper` + a `JacksonJsonDeserializer` built with production consumer properties (`USE_TYPE_INFO_HEADERS=false`, forced `VALUE_DEFAULT_TYPE=OrderCreatedEvent`), asserts `record.equals()` plus explicit `BigDecimal.compareTo()==0`+scale checks on every money field. Passes in the full suite run (`./mvnw test`, 213/213 green). |
| 2 | WR-02: A send failure on one claimed row does not roll back/re-drive sibling rows already marked SENT in the same poll batch; send-then-mark ordering preserved; claim semantics (`FOR UPDATE SKIP LOCKED`)/`attempts→FAILED` unchanged | ✓ VERIFIED (with a documented minor gap — see Gaps) | `OutboxRowPublisher.publish()` is `@Transactional`, invoked per-row through the injected bean from `OutboxRelay.poll()` (no self-invocation); `OutboxRelayTest.siblingRowsAreNotRolledBackWhenMiddleRowFails` proves a `[ok, fail, ok]` batch ends with both `ok` rows `SENT` and the failing row `PENDING`/`attempts=1`. `claimPending`'s SQL (`FOR UPDATE SKIP LOCKED`) is textually unchanged. Gap: isolation is proven only for Kafka-send failures, not persistence (`save()`) failures (Review finding WR-02 in `OutboxRelay.java`/`OutboxRowPublisher.java`), and the effective cross-instance claim guarantee is weakened as a side effect of the transaction split (Review finding WR-01). Both are minor/non-blocking — see Gaps section. |
| 3 | WR-03: After the outbox cutover, no code in `src/main` references `KafkaOrderEventPublisher`/`OrderEventPublisher`/`KafkaInventoryStockResultPublisher`/`InventoryStockResultPublisher`; producer beans wired only for them removed | ✓ VERIFIED | `find`/`grep -rn` for all four class names across `src/main` returns zero hits. The four source files (2 adapters, 2 ports) plus `OrderKafkaProducerConfig`/`InventoryKafkaProducerConfig` are absent from the working tree. `OutboxConfig`, `PaymentKafkaProducerConfig`, `TableKafkaProducerConfig`, kitchen producer configs untouched (still present). |
| 4 | IN-02: SENT outbox rows older than a configurable retention window are deleted; rows within the window and all PENDING/FAILED rows are retained; FAILED rows are surfaced to operators; job gated off in tests | ✓ VERIFIED | `OutboxRetentionJob` (`@ConditionalOnProperty(outbox.retention.enabled, matchIfMissing=true)`) has `sweepSentRetention` (deletes via `deleteByStatusAndCreatedAtBefore("SENT", cutoff)`, `@Modifying(clearAutomatically, flushAutomatically)`) and `surfaceFailedRows` (`countByStatus("FAILED")` + `log.warn`). `OutboxRetentionJobTest.deletesOnlyOldSentRowsAndRetainsWithinWindowPendingAndFailedRows` seeds old-SENT/new-SENT/old-PENDING/old-FAILED rows against the real H2 test DB and asserts only the old-SENT row is deleted. `outbox.retention.enabled=false` present in `src/test/resources/application.properties`. |
| 5 | IN-03: One named constant drives both the rejection-reason truncation cap and the column length; truncation behavior unchanged | ✓ VERIFIED | `OrderEntity.REJECTION_REASON_MAX_LEN = 60000` (public static final); `@Column(name="rejection_reason", length=REJECTION_REASON_MAX_LEN)`; `OrderConfirmationService.MAX_REASON_LEN` now aliases `OrderEntity.REJECTION_REASON_MAX_LEN` instead of a separate literal. `OrderConfirmationServiceTest` (6 tests) passes in the full suite. |
| 6 | WR-05: `KitchenStatusProjectionService.deriveTargetStatus` no longer compares `KitchenItemStatus.ordinal()`; uses an explicit rank map; a test pins the intended ordering independent of enum declaration order | ✓ VERIFIED | `grep -n "ordinal()"` on the file returns zero hits. `ITEM_RANK` map + private `itemRank()` helper used in all three tier comparisons in `deriveTargetStatus`. `KitchenStatusProjectionServiceTest.itemRankPinsTheIntendedOrderingIndependentOfEnumDeclarationOrder` reads `ITEM_RANK` via reflection and asserts strictly-increasing ranks QUEUED<PREPARING<READY<SERVED<COMPLETED, independent of `.ordinal()`. 13/13 tests pass in that class. |
| 7 | WR-06: `application.properties` contains no bare plaintext DB password; username/password env-driven with a documented local-dev default; app still resolves credentials with defaults | ✓ VERIFIED | `spring.datasource.username=${DB_USERNAME:root}` / `spring.datasource.password=${DB_PASSWORD:123456}` in `src/main/resources/application.properties` (matches the existing `${AUTH_...:default}` idiom). `.env.example` created documenting both vars as non-secret local-dev placeholders. `.gitignore` gained a `.env` entry. Test profile (H2, `sa`/empty) left untouched. |

### Observable Truths — CR-01 Non-Regression (Critical Guard)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 8 | `OrderConfirmationService.onStockResult`: `recordProcessed(...)` is the LAST statement in the single `@Transactional` handler; no `REQUIRES_NEW` pre-commit | ✓ VERIFIED | Read `OrderConfirmationService.java` lines 57–105: single `@Transactional public void onStockResult(...)`; `recordProcessed(event.eventId())` is the final statement (line 105 area); no `REQUIRES_NEW`/`Propagation` anywhere in the file. |
| 9 | `KitchenStatusProjectionService.onTicketStatusChanged`: same guard | ✓ VERIFIED | Read lines 77–139: single `@Transactional`; `recordProcessed(event.eventId())` is the literal last statement of the method (line 130); no real `REQUIRES_NEW`/`Propagation` code (only an explanatory prose comment mentioning the term, pre-existing from 17.1, confirmed via `git show af6e061:...`). |
| 10 | `InventoryReservationService.onOrderCreated`: same guard | ✓ VERIFIED | Read lines 71–160: single `@Transactional`; `recordProcessed(eventId)` is the literal last statement (line 159); outbox write happens before it in the same transaction; no `REQUIRES_NEW`. |

### Observable Truths — Test Suite

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 11 | Full Maven suite passes, no regression from Phase 17.1's 207 tests | ✓ VERIFIED | `./mvnw test` run directly by this verifier: `Tests run: 213, Failures: 0, Errors: 0, Skipped: 0` / `BUILD SUCCESS`. Matches the claimed post-merge state (213 tests) exactly; up from 207 pre-phase (6 new tests: wire-format round-trip, sibling-isolation, retention x5 minus overlaps, item-rank pin — net +6 after WR-03's test deletions offset new additions). |

**Score:** 11/11 core truths verified (7 requirements + 3 CR-01 sites + test suite). 5 minor, non-blocking gaps documented below from the 17.2 code review (0 critical/blocking).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/test/java/.../shared/outbox/OutboxWireFormatRoundTripTest.java` | Round-trip Instant+BigDecimal test | ✓ VERIFIED | Exists, substantive (real ObjectMapper + real consumer deserializer config), wired into suite (runs, passes) |
| `src/main/java/.../shared/outbox/infrastructure/OutboxRowPublisher.java` | Per-row `@Transactional` publish unit | ✓ VERIFIED | Exists, `@Transactional publish(OutboxEventEntity)`, injected into `OutboxRelay` (not self-invoked), exercised by `OutboxRelayTest` |
| `src/main/java/.../shared/outbox/infrastructure/OutboxRetentionJob.java` | Scheduled SENT retention + FAILED surfacing | ✓ VERIFIED | Exists, two `@Scheduled` + `@ConditionalOnProperty`-gated methods, exercised by `OutboxRetentionJobTest` against real H2 DB |
| `src/main/java/.../shared/outbox/repository/OutboxEventRepository.java` | `deleteByStatusAndCreatedAtBefore` + `countByStatus` | ✓ VERIFIED | Both queries present, `@Modifying(clearAutomatically, flushAutomatically)` on the delete |
| `src/main/java/.../order_context/application/KitchenStatusProjectionService.java` | `ITEM_RANK` ordinal-free rank map | ✓ VERIFIED | `ITEM_RANK` map present, `.ordinal()` fully removed, CR-01 site untouched |
| `src/main/resources/application.properties` | `${DB_USERNAME:...}`/`${DB_PASSWORD:...}` placeholders | ✓ VERIFIED | Both present, no bare plaintext credential |
| (deleted) `KafkaOrderEventPublisher.java`, `OrderEventPublisher.java`, `OrderKafkaProducerConfig.java`, `KafkaInventoryStockResultPublisher.java`, `InventoryStockResultPublisher.java`, `InventoryKafkaProducerConfig.java` | Absent from `src/main` | ✓ VERIFIED | All 6 files absent from working tree; zero references remain |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `OrderConfirmationService.MAX_REASON_LEN` | `OrderEntity.REJECTION_REASON_MAX_LEN` | direct constant reference | ✓ WIRED | `static final int MAX_REASON_LEN = OrderEntity.REJECTION_REASON_MAX_LEN;` |
| `OutboxRelay.poll()` | `OutboxRowPublisher.publish(row)` | injected bean call, no self-invocation | ✓ WIRED | Constructor-injected `OutboxRowPublisher`, called per-row in the `for` loop |
| `OutboxRetentionJob` | `OutboxEventRepository` | `deleteByStatusAndCreatedAtBefore` / `countByStatus` | ✓ WIRED | Both methods called from the two `@Scheduled` methods |
| `spring.datasource.username/password` | `DB_USERNAME`/`DB_PASSWORD` env vars | `${VAR:default}` | ✓ WIRED | Both properties use the placeholder-with-default syntax |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite is green post-merge | `./mvnw test` | `Tests run: 213, Failures: 0, Errors: 0, Skipped: 0` / `BUILD SUCCESS` | ✓ PASS |
| No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` debt markers in phase-modified files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` on all 10 phase-touched source files | No matches (one unrelated pre-existing `payment.qr.placeholder-base-url` property name, not a debt marker) | ✓ PASS |
| Zero `src/main` references to the 4 deleted dead-publisher classes | `grep -rn` across `src/main` | 0 hits | ✓ PASS |
| All 3 CR-01 sites end with `recordProcessed(...)` as the last statement, no `REQUIRES_NEW` | Manual read of all 3 method bodies | Confirmed for all 3 | ✓ PASS |
| Commits referenced in SUMMARY.md files exist | `git log --oneline` / `git show --stat` on sampled hashes (`1057e66`, `8025dc1`, `09a7550`) | All present | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` conventions or PLAN/SUMMARY-declared probes found for this phase (backend Maven project, not a migration/CLI-tooling phase). Skipped — Maven test suite serves as the equivalent executable verification and was run directly (see above).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| WR-01 | 17.2-01-PLAN.md | Outbox wire-format round-trip coverage | ✓ SATISFIED | Round-trip test present, green |
| WR-02 | 17.2-02-PLAN.md | Relay per-row transaction | ✓ SATISFIED (minor gaps noted) | `OutboxRowPublisher` + sibling-isolation test |
| WR-03 | 17.2-04-PLAN.md | Delete dead direct-publish adapters | ✓ SATISFIED | 6 files deleted, grep-clean |
| IN-02 | 17.2-03-PLAN.md | Outbox retention + FAILED-row surfacing | ✓ SATISFIED | `OutboxRetentionJob` + test |
| IN-03 | 17.2-05-PLAN.md | Unified rejection-reason cap constant | ✓ SATISFIED | `REJECTION_REASON_MAX_LEN` single source of truth |
| WR-05 | 17.2-05-PLAN.md | Explicit ordinal-free kitchen fulfillment rank map | ✓ SATISFIED | `ITEM_RANK` map, reflection-pinned test |
| WR-06 | 17.2-06-PLAN.md | Externalize DB credentials | ✓ SATISFIED | `${DB_USERNAME:...}`/`${DB_PASSWORD:...}` |

No orphaned requirements found — all 7 requirement IDs declared in `ROADMAP.md`'s Phase 17.2 entry are claimed and satisfied by exactly one plan each.

### Anti-Patterns Found

None (blocker or warning level) in phase-modified files. See Gaps section below for the 17.2 code-review's substantive findings, all judged non-blocking.

## Severity Judgment on 17.2-REVIEW.md Findings (0 critical / 5 warnings / 2 info)

Per the escalation prompt, each review finding was judged against whether it breaks the phase GOAL (7 requirements resolved, CR-01 intact, suite green) or is a minor/acceptable follow-up:

1. **Review WR-01 (cross-instance SKIP LOCKED weakening)** — **MINOR, non-blocking.** This is an inherent, foreseeable consequence of satisfying requirement WR-02 exactly as locked in `17.2-CONTEXT.md` ("Publish/flip-to-SENT each claimed row in its OWN transaction... rather than one transaction for the whole batch"). The whole-batch transaction was the *only* mechanism holding the SKIP LOCKED lock across claim+publish; splitting it necessarily shortens the lock window. Impact is bounded by (a) at-least-once delivery + CR-01-protected idempotent consumers (independently re-verified intact in this report), and (b) the current single-instance `@Scheduled(fixedDelay)` topology, which serializes `poll()` calls and is therefore entirely unaffected. The stale Javadoc claim on `claimPending` is a genuine documentation-accuracy gap worth a follow-up, not a functional regression against any of the 7 phase requirements.
2. **Review WR-02 (persistence-exception isolation gap)** — **MINOR, non-blocking.** The plan's literal, TDD-verified acceptance criterion (a Kafka *send* failure on one row does not roll back siblings) is proven true by test. The narrower, untested case (a `save()` persistence exception) is a real gap in defense-in-depth but does not contradict any stated must-have.
3. **Review WR-03 (`rejection_reason` VARCHAR/@Lob DDL risk)** — **MINOR, non-blocking, pre-existing.** Confirmed via `git show` that the same length-without-`@Lob` pattern existed before Phase 17.2 (17.1 had `length=65535`; this phase only unified the constant to 60000, same underlying mapping issue). Out of the locked IN-03 scope (constant unification only). Worth a follow-up but does not belong to this phase's delivered scope.
4. **Review WR-04 (`${DB_PASSWORD:123456}` weak default)** — **NOT A GAP.** This is exactly what WR-06's locked decision in `17.2-CONTEXT.md` specifies verbatim: `${DB_USERNAME:root}` / `${DB_PASSWORD:123456}`, matching the existing `${AUTH_...:default}` idiom, explicitly to preserve local-dev convenience. The review's suggestion (fail-fast with no default) is a valid *future* hardening idea but contradicts the phase's own locked acceptance criterion ("a documented local default"). No action needed to satisfy WR-06 as specified.
5. **Review WR-05 (`InterruptedException` swallowed)** — **MINOR, non-blocking.** Real robustness gap, unrelated to any of the 7 phase requirements' acceptance criteria (this label collides with the phase's own WR-05, but they are unrelated concerns — see collision notice above).
6. **Review IN-01 (unvalidated negative `ttlDays`)** — **MINOR/INFO, non-blocking.** Configuration-misuse edge case; IN-02's stated acceptance criterion (correct behavior for a valid window) is met and tested.
7. **Review IN-02 (repeated stream passes in `deriveTargetStatus`)** — **INFO only, not a gap.** Explicitly advisory in the review itself ("no change is required now").

**`has_blocking_gaps: false`** — the phase goal is achieved. All 7 requirements are delivered and test-verified, CR-01 is intact at all 3 sites, and the full suite is green (213/213). The 5 minor gaps above are legitimate backlog-worthy follow-ups surfaced by the code review, not phase-goal blockers.

### Human Verification Required

None. This phase is entirely backend/config (Java service logic, JPA queries, `application.properties`), fully covered by automated tests and static/grep evidence — no UI, visual, or manual-flow verification applies.

### Gaps Summary

All 7 phase requirements (WR-01, WR-02, WR-03, IN-02, IN-03, WR-05, WR-06) are implemented, tested, and verified against the actual codebase — not merely claimed in SUMMARY.md. Phase 17.1's CR-01 idempotency-ledger-last fix is confirmed intact and unmodified at all three consumer sites. The full Maven suite passes with the exact claimed count (213 tests, 0 failures, 0 errors), run directly by this verifier rather than trusted from SUMMARY claims.

Five minor, non-blocking gaps were surfaced by the phase's own code review and independently confirmed by this verifier by reading the actual source: (1) the WR-02 per-row-transaction refactor weakens `claimPending`'s documented cross-instance dedup guarantee (bounded by idempotent consumers + single-instance topology), (2) per-row isolation doesn't extend to persistence-layer exceptions, (3) `InterruptedException` is swallowed without restoring the interrupt flag, (4) `OutboxRetentionJob.ttlDays` has no lower-bound guard, and (5) `OrderEntity.rejectionReason`'s VARCHAR mapping (pre-existing since 17.1) risks a MySQL DDL failure. None of these contradict the phase's locked acceptance criteria; all are recommended as backlog items via `has_blocking_gaps: false` routing.

---

_Verified: 2026-07-10T02:57:39Z_
_Verifier: Claude (gsd-verifier)_
