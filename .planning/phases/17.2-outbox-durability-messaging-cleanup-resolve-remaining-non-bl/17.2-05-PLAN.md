---
phase: 17.2-outbox-durability-messaging-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java
  - src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java
  - src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java
autonomous: true
requirements: [IN-03, WR-05]
must_haves:
  truths:
    - "There is one named constant for the rejection-reason cap; both the truncation cap and the column length derive from it, and truncation behavior is unchanged (still caps at the same effective length)"
    - "KitchenStatusProjectionService.deriveTargetStatus no longer compares KitchenItemStatus.ordinal(); it uses an explicit rank map mirroring OrderStatus FULFILLMENT_RANK"
    - "A test pins the intended KitchenItemStatus ordering independent of enum declaration order"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java"
      provides: "Explicit KitchenItemStatus rank map + ordinal-free derivation"
      contains: "ITEM_RANK"
  key_links:
    - from: "OrderConfirmationService.MAX_REASON_LEN"
      to: "OrderEntity rejection_reason @Column length"
      via: "single shared named constant referenced by both"
      pattern: "REJECTION_REASON"
---

<objective>
Close two independent hygiene findings in order_context (disjoint files, pure-logic, behavior-preserving):

IN-03 — `OrderConfirmationService.MAX_REASON_LEN = 60000` and `OrderEntity.rejectionReason
@Column(length = 65535)` are unaligned magic numbers. Define ONE named source of truth and derive both.

WR-05 — `KitchenStatusProjectionService.deriveTargetStatus` compares `KitchenItemStatus.ordinal()`;
reordering the enum silently breaks the derivation. Replace with an explicit rank map (mirroring the
existing `OrderStatus` `FULFILLMENT_RANK` map) and pin the ordering with a test.

Purpose: Remove two silent-breakage traps (unaligned magic numbers; enum-ordinal coupling).
Output: A single rejection-reason cap constant; an ordinal-free rank map + a reorder-catching test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

IN-03 current state:
  OrderConfirmationService.MAX_REASON_LEN = 60000 (used by truncate(): caps at MAX_REASON_LEN, appends
    a truncation suffix). Existing test OrderConfirmationServiceTest asserts
    order.getRejectionReason().length() == OrderConfirmationService.MAX_REASON_LEN — this reference MUST
    keep compiling and the effective cap MUST stay 60000.
  OrderEntity: @Column(name="rejection_reason", length = 65535) String rejectionReason.
  Note: JPA @Column(length=...) needs a compile-time constant; keep the single constant referenceable
  from OrderEntity (e.g. a public static final int on OrderEntity) and have MAX_REASON_LEN alias it.

WR-05 current state (KitchenStatusProjectionService):
  Existing FULFILLMENT_RANK is Map.of(OrderStatus.CONFIRMED..COMPLETED -> 0..4) — mirror its SHAPE.
  deriveTargetStatus(List<ItemStatus>) currently uses item.status().ordinal() >=
    KitchenItemStatus.{SERVED,READY,PREPARING}.ordinal() comparisons + allMatch COMPLETED.
  KitchenItemStatus enum order: QUEUED, PREPARING, READY, SERVED, COMPLETED (declaration order is
    documented as load-bearing — the point of WR-05 is to STOP relying on it implicitly).
  KitchenStatusProjectionServiceTest already exists — extend it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single rejection-reason cap constant (IN-03)</name>
  <files>src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java, src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java</files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java (MAX_REASON_LEN + truncate())
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java (rejection_reason @Column length)
    - src/test/java/com/example/feat1/DDD/order_context/application/OrderConfirmationServiceTest.java (asserts length == OrderConfirmationService.MAX_REASON_LEN)
    - .planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md (IN-03: single named constant; behavior identical; existing truncation test still passes)
  </read_first>
  <action>
    Define ONE named constant as the source of truth for the rejection-reason cap. Preferred shape:
    add `public static final int REJECTION_REASON_MAX_LEN = 60000;` to OrderEntity and use it in the
    column mapping (`@Column(name="rejection_reason", length = OrderEntity.REJECTION_REASON_MAX_LEN)`),
    then make OrderConfirmationService.MAX_REASON_LEN reference that same constant
    (`static final int MAX_REASON_LEN = OrderEntity.REJECTION_REASON_MAX_LEN;`) so both derive from one
    place and the existing test's `OrderConfirmationService.MAX_REASON_LEN` reference still compiles.
    Effective cap stays 60000 (unify the column length DOWN to the constant, not up) so truncation
    behavior and the existing test are unchanged. If layering forbids referencing OrderEntity from the
    application service in this codebase, instead place the constant in a small shared holder both
    reference — but keep exactly ONE authoritative value. Do not change truncate() logic.
  </action>
  <acceptance_criteria>
    - Exactly one authoritative constant defines the cap; both the truncation cap and the @Column length derive from it.
    - Effective truncation cap remains 60000; truncate() logic unchanged.
    - <automated>./mvnw -q test -Dtest=OrderConfirmationServiceTest</automated> passes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>./mvnw -q test -Dtest=OrderConfirmationServiceTest</automated>
  </verify>
  <done>Changing the constant in one place consistently drives both truncation and column length; existing truncation test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Explicit KitchenItemStatus rank map + reorder-catching test (WR-05)</name>
  <files>src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java, src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java</files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java (FULFILLMENT_RANK shape + deriveTargetStatus ordinal usage)
    - src/main/java/com/example/feat1/DDD/kitchen_context/domain/model/KitchenItemStatus.java (enum values)
    - src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java (existing derivation tests to extend)
    - .planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md (WR-05: explicit rank map; behavior identical; test catches future enum reorder)
  </read_first>
  <behavior>
    - Derivation is byte-for-byte behavior-identical to today: all-COMPLETED -> COMPLETED; all >= SERVED -> SERVED; all >= READY -> READY; any >= PREPARING -> PREPARING; empty/none -> null.
    - The comparison uses an explicit ITEM_RANK map (QUEUED<PREPARING<READY<SERVED<COMPLETED), NOT .ordinal().
    - A test pins the intended ranking so a future reorder of the KitchenItemStatus enum declaration would fail the test rather than silently change derivation.
    - CR-01 NON-REGRESSION GUARD (hard invariant): this edit is confined to the private deriveTargetStatus helper and the new ITEM_RANK map. Do NOT touch the recordProcessed(event.eventId()) call or its position as the LAST statement of onTicketStatusChanged, and do NOT reintroduce any REQUIRES_NEW / pre-commit idempotency-ledger pattern (that would revert the Phase 17.1 CR-01 fix). Confirm the ledger insert remains the final statement of onTicketStatusChanged after this change.
  </behavior>
  <action>
    Add a private static final Map<KitchenItemStatus,Integer> ITEM_RANK to KitchenStatusProjectionService
    with explicit values (QUEUED=0, PREPARING=1, READY=2, SERVED=3, COMPLETED=4), mirroring the existing
    OrderStatus FULFILLMENT_RANK idiom. Rewrite deriveTargetStatus to compare ITEM_RANK.get(status)
    against the ITEM_RANK of the threshold statuses instead of item.status().ordinal(). Behavior must be
    identical. In KitchenStatusProjectionServiceTest, add a test that pins the ordering independent of enum
    declaration order (e.g. assert ITEM_RANK strictly increases across the intended sequence, or drive
    deriveTargetStatus with mixed snapshots whose expected outcome depends on the rank rather than the
    declaration index). Keep all existing projection tests passing.
  </action>
  <acceptance_criteria>
    - <automated>bash -c 'grep -n "ordinal()" src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java | grep -v "^[0-9]*: *\(\*\|//\)" | grep -c . ; test $? -eq 1'</automated> — no non-comment .ordinal() comparison remains in the derivation.
    - An explicit ITEM_RANK map exists mirroring FULFILLMENT_RANK.
    - A test pins the intended ordering independent of enum declaration order.
    - CR-01 guard: <automated>bash -c 'grep -q "recordProcessed(event.eventId())" src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java && ! grep -q "REQUIRES_NEW" src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java'</automated> — the transactional-ledger insert is preserved and no REQUIRES_NEW was reintroduced.
    - <automated>./mvnw -q test -Dtest=KitchenStatusProjectionServiceTest</automated> passes.
  </acceptance_criteria>
  <verify>
    <automated>./mvnw -q test -Dtest=KitchenStatusProjectionServiceTest</automated>
  </verify>
  <done>Derivation is ordinal-free via an explicit rank map; a reorder-catching test guards the ordering; behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) | Pure in-process logic hygiene; no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-10 | Tampering/Integrity | enum-ordinal coupling silently breaking status derivation on reorder | mitigate | explicit rank map + reorder-catching test |
| T-17.2-11 | Integrity | unaligned reason-length magic numbers causing truncation/column drift | mitigate | single named constant drives both cap and column length |
</threat_model>

<verification>
- `grep -n "ordinal()" KitchenStatusProjectionService.java` shows no derivation comparison using ordinal.
- `./mvnw -q test -Dtest=OrderConfirmationServiceTest,KitchenStatusProjectionServiceTest` passes.
- Full suite green at phase close: `./mvnw test`.
</verification>

<success_criteria>
One named rejection-reason constant drives both truncation and column length with identical behavior; the
kitchen-status derivation is ordinal-free with a reorder-catching test.
</success_criteria>

<output>
Create `.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-05-SUMMARY.md` when done.
</output>
