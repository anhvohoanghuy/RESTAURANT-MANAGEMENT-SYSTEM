---
phase: 17.2-outbox-durability-messaging-cleanup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/application.properties
  - .env.example
autonomous: true
requirements: [WR-06]
must_haves:
  truths:
    - "application.properties contains no bare plaintext DB password; username and password are env-driven with a documented safe local-dev default"
    - "The app still resolves datasource credentials with the placeholders (defaults apply when the env vars are unset)"
  artifacts:
    - path: "src/main/resources/application.properties"
      provides: "Env-placeholder DB credentials matching the ${AUTH_...:default} idiom"
      contains: "${DB_PASSWORD"
  key_links:
    - from: "spring.datasource.username/password"
      to: "DB_USERNAME/DB_PASSWORD env vars"
      via: "${DB_USERNAME:root} / ${DB_PASSWORD:123456} placeholder-with-default"
      pattern: "\\$\\{DB_(USERNAME|PASSWORD)"
---

<objective>
Close WR-06: `application.properties` hard-codes `spring.datasource.username=root` /
`password=123456` in plaintext. Move DB username/password to environment-variable placeholders with
safe local-dev defaults, matching the existing `${AUTH_...:default}` idiom already in the file.

Purpose: Remove a plaintext credential from source control and make production creds injectable via env.
Output: Env-placeholder datasource credentials + a documenting `.env.example` (no real secrets).
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — use directly. -->

src/main/resources/application.properties current lines:
  spring.datasource.username=root
  spring.datasource.password=123456
Existing placeholder-with-default idiom already present in the SAME file to match:
  jwt.secret=${JWT_SECRET:change-me-...}
  auth.action-token.password-reset-expiration=${AUTH_PASSWORD_RESET_EXPIRATION:900000}
  spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
Note: no README.md or pre-existing .env.example exists at repo root (top-level docs are
  ARCHITECTURE.md, DECISIONS.md, HELP.md, PROJECT_CONTEXT.md, TASKS.md). The test profile
  (src/test/resources/application.properties) uses H2 with its own sa/empty creds and MUST NOT change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize DB credentials to env placeholders + .env.example</name>
  <files>src/main/resources/application.properties, .env.example</files>
  <read_first>
    - src/main/resources/application.properties (lines 2-4 plaintext DB creds; the ${AUTH_...:default} idiom already present)
    - .planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-CONTEXT.md (WR-06 locked decision + acceptance; do NOT commit real secrets)
  </read_first>
  <action>
    In src/main/resources/application.properties, replace the bare values with placeholder-with-default:
    `spring.datasource.username=${DB_USERNAME:root}` and `spring.datasource.password=${DB_PASSWORD:123456}`,
    matching the existing ${AUTH_...:default} idiom. Do NOT touch spring.datasource.url or the driver line,
    and do NOT modify src/test/resources/application.properties (H2 profile stays as-is). Create a root
    `.env.example` documenting DB_USERNAME and DB_PASSWORD (with the same non-secret local defaults, clearly
    marked as dev-only placeholders, NOT real credentials); if a `.gitignore` exists, ensure a real `.env` is
    ignored (add the pattern only if not already present — do not create tooling beyond this). Confirm the app
    context can still resolve the datasource credentials via the placeholders.
  </action>
  <acceptance_criteria>
    - <automated>bash -c 'grep -Eq "spring.datasource.password=\\\$\\{DB_PASSWORD:" src/main/resources/application.properties && ! grep -Eq "^spring.datasource.password=123456$" src/main/resources/application.properties'</automated> — password is env-driven, no bare plaintext line remains.
    - `spring.datasource.username` uses `${DB_USERNAME:root}`.
    - `.env.example` exists documenting DB_USERNAME/DB_PASSWORD with dev-only defaults (no real secrets); test profile unchanged.
    - <automated>./mvnw -q test -Dtest=OrderConfirmationServiceTest</automated> passes (context/property resolution unaffected; representative smoke of the H2 test profile).
  </acceptance_criteria>
  <verify>
    <automated>./mvnw -q test -Dtest=OrderConfirmationServiceTest</automated>
  </verify>
  <done>DB credentials are env-driven with documented local defaults; no bare plaintext DB password remains in source; the app still starts with placeholders resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source control -> deployment config | DB credentials must be injectable per-environment, not baked into a committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.2-12 | Information Disclosure | plaintext DB password committed in application.properties | mitigate | externalize to ${DB_PASSWORD:...} env placeholder; document in .env.example; keep real .env untracked |
| T-17.2-13 | Elevation | reused dev default credential in production | accept | this phase only externalizes to env; real secret-manager migration is explicitly deferred (out of scope). Operators override via env in prod. |
</threat_model>

<verification>
- `grep -n "DB_USERNAME\|DB_PASSWORD" src/main/resources/application.properties` shows both placeholders with defaults.
- No `spring.datasource.password=123456` bare line remains.
- `.env.example` documents the two vars without real secrets.
- Full suite green at phase close: `./mvnw test`.
</verification>

<success_criteria>
`application.properties` contains no bare plaintext DB password; the value is env-driven with a documented
local default, and the app still resolves credentials via the placeholders.
</success_criteria>

<output>
Create `.planning/phases/17.2-outbox-durability-messaging-cleanup-resolve-remaining-non-bl/17.2-06-SUMMARY.md` when done.
</output>
