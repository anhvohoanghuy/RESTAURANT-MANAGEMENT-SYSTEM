---
phase: 17.2-outbox-durability-messaging-cleanup
plan: 01
subsystem: infra
tags: [kafka, jackson, outbox, testing]

# Dependency graph
requires:
  - phase: 17.1-kitchen-hardening
    provides: transactional outbox (OutboxWriter, OutboxRelay, OutboxConfig) and Jackson-3-native serde
provides:
  - Automated proof that the outbox wire format (OutboxWriter serialize -> Kafka -> consumer JacksonJsonDeserializer deserialize) round-trips Instant and BigDecimal losslessly
  - Regression guard against future producer/consumer ObjectMapper drift for the outbox subsystem
affects: [17.2-02, 17.2-03, 17.2-04, 17.2-05, 17.2-06, outbox subsystem, kitchen_context consumers, inventory_context consumers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wire-format round-trip test: serialize with the real Spring-injected ObjectMapper bean, deserialize with a JacksonJsonDeserializer constructed with the exact production consumer property set (TRUSTED_PACKAGES/VALUE_DEFAULT_TYPE/USE_TYPE_INFO_HEADERS), asserting both whole-object equals() and explicit BigDecimal compareTo+scale checks — broker-free, no EmbeddedKafka needed"

key-files:
  created:
    - src/test/java/com/example/feat1/DDD/shared/outbox/OutboxWireFormatRoundTripTest.java
  modified: []

key-decisions:
  - "WR-01 round-trip test uses OrderCreatedEvent as the representative event because it is the only saga event carrying both an Instant and a BigDecimal, and is exactly the type the Inventory consumer (InventoryKafkaConsumerConfig#orderCreatedConsumerFactory) forces via VALUE_DEFAULT_TYPE"
  - "Test passed GREEN on first run (no RED phase observed) — no producer/consumer Jackson mapper mismatch exists today; kept as a permanent regression guard per the plan's explicit instruction, and Task 2 (mapper alignment) made NO production code change since no mismatch was found"

patterns-established:
  - "Broker-free wire-format round-trip tests: exercise the real ObjectMapper bean + a hand-configured JacksonJsonDeserializer mirroring production consumer properties, instead of spinning up EmbeddedKafka"

requirements-completed: [WR-01]

# Metrics
duration: ~20min
completed: 2026-07-10
---

# Phase 17.2 Plan 01: Outbox Wire-Format Round-Trip Test Summary

**Added a broker-free round-trip test proving OutboxWriter's Jackson-3 ObjectMapper and the production JacksonJsonDeserializer already agree byte-for-byte on Instant and BigDecimal encoding — no mapper alignment fix was needed.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-07-10 (session start)
- **Completed:** 2026-07-10T02:40:33Z
- **Tasks:** 2 (Task 2 required no code change — see below)
- **Files modified:** 1 created, 0 modified

## Accomplishments
- Closed WR-01: the transactional-outbox wire format (OutboxWriter serialize -> Kafka `StringSerializer` republish -> consumer `JacksonJsonDeserializer` deserialize) is now test-covered for `Instant` and `BigDecimal` fields.
- Proved, using the REAL Spring-injected `tools.jackson.databind.ObjectMapper` (the same bean `OutboxWriter` uses — there is exactly one `ObjectMapper` bean in the app, autoconfigured, no custom `@Bean` overrides it) and a `JacksonJsonDeserializer` configured identically to `InventoryKafkaConsumerConfig#orderCreatedConsumerFactory` (`USE_TYPE_INFO_HEADERS=false`, forced `VALUE_DEFAULT_TYPE=OrderCreatedEvent`, `TRUSTED_PACKAGES=com.example.feat1.DDD.order_context.application.event`), that `OrderCreatedEvent` — the only saga event carrying both an `Instant` and multiple `BigDecimal` fields — survives the round trip with `record.equals()` equality AND explicit `BigDecimal.compareTo()==0` + scale-preservation checks on every money field (`total`, `basePrice`, `toppingsTotal`, `unitPrice`, `lineTotal`, topping `additionalPrice`).
- The test ran GREEN on the very first execution — no encoding mismatch exists in the current codebase, so Task 2 (mapper alignment) made no production change, exactly as the plan specified for the already-passing case.

## Task Commits

1. **Task 1: Failing round-trip test (RED) through the real OutboxWriter serializer + real consumer deserializer** - `1057e66` (test) — test was GREEN on first run (no mismatch to expose as RED); kept as the permanent regression guard the plan calls for in that case.
2. **Task 2: Align OutboxWriter/consumer mappers only if the round-trip fails (GREEN)** - no commit (no-op). Task 1's test proved no mismatch exists, so per the plan's explicit instruction ("If Task 1 passed, make NO production change and note it") `OutboxWriter.java` and `OutboxConfig.java` were left unmodified. Re-ran `OutboxWireFormatRoundTripTest`, `OutboxWriterPersistenceTest`, and `OutboxRelayTest` together (5 tests, 0 failures) to satisfy Task 2's acceptance criteria.

**Plan metadata:** (this commit) `docs(17.2-01): complete outbox wire-format round-trip plan`

## Files Created/Modified
- `src/test/java/com/example/feat1/DDD/shared/outbox/OutboxWireFormatRoundTripTest.java` - New broker-free JUnit5 `@SpringBootTest` proving the OutboxWriter->consumer Jackson round trip for `Instant` and `BigDecimal`.

## Decisions Made
- Chose `OrderCreatedEvent` (not `OrderConfirmedEvent`, which has no `BigDecimal`, or `OrderStockResultEvent`, whose `BigDecimal` fields are optional/list-nested) as the single representative event because it is the only event type carrying both problem types AND is the exact class the Inventory consumer forces as its `VALUE_DEFAULT_TYPE` — this is a direct, not synthetic, coverage of the real trust boundary.
- Obtained the ObjectMapper via `@Autowired ObjectMapper` in a `@SpringBootTest` rather than hand-constructing one, per the task's explicit requirement to exercise the ACTUAL OutboxWriter serialization mapper, not an ad-hoc one. Verified by grep that `OutboxWriter` and `SecurityConfig` are the only two consumers of the `ObjectMapper` type in `src/main` and neither declares a competing `@Bean`, so the single autoconfigured Spring Boot bean is unambiguously the same instance.
- No new dependencies were added, matching the plan's explicit constraint.

## Deviations from Plan

None - plan executed exactly as written. Task 1's RED phase produced a GREEN result immediately (the plan explicitly anticipates and permits this outcome: "If the round-trip already passes, keep the test as a green regression guard and record in the SUMMARY that no mapper mismatch existed"), and Task 2 correctly made no production change as instructed for that case.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- WR-01 is closed; the outbox subsystem now has a permanent regression guard against future `Instant`/`BigDecimal` encoding drift between `OutboxWriter`'s producer-side `ObjectMapper` and the kitchen/inventory consumers' `JacksonJsonDeserializer` configs.
- No blockers for the remaining 17.2 plans (WR-02/WR-03/IN-02/IN-03/WR-05/WR-06); this plan touched only a new test file and did not modify `OutboxWriter.java`, `OutboxConfig.java`, or either consumer config, so there is no file-overlap risk with sibling plans in this wave.

---
*Phase: 17.2-outbox-durability-messaging-cleanup*
*Completed: 2026-07-10*
