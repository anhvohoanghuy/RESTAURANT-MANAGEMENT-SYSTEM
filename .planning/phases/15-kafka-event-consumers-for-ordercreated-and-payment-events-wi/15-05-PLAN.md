---
phase: 15-kafka-event-consumers
plan: 05
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderProcessedEventEntity.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderProcessedEventRepository.java
  - src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java
  - src/test/java/com/example/feat1/DDD/order_context/application/OrderConfirmationServiceTest.java
autonomous: true
requirements: [D-03, D-10, D-11]
must_haves:
  truths:
    - "A CONFIRMED result transitions an order from PENDING_CONFIRMATION to CONFIRMED"
    - "A REJECTED result transitions an order from PENDING_CONFIRMATION to REJECTED and records the shortfall reason"
    - "A duplicate result event (same eventId) does not re-transition; a result for an order not in PENDING_CONFIRMATION is ignored"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java"
      provides: "Idempotent, status-guarded saga completion handler"
      min_lines: 30
      contains: "onStockResult"
    - path: "src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderProcessedEventEntity.java"
      provides: "Order-context idempotency ledger, unique (event_id, consumer_name)"
      contains: "processed_events"
  key_links:
    - from: "OrderConfirmationService.onStockResult"
      to: "OrderRepository.findById + OrderEntity.setStatus"
      via: "status transition guarded by PENDING_CONFIRMATION"
      pattern: "PENDING_CONFIRMATION"
---

<objective>
Implement the Order-side saga completion (D-03/D-10/D-11): an order-context idempotency ledger and an `OrderConfirmationService` that consumes an `OrderStockResultEvent` (via a plain method the listener will call in 15-06), idempotently transitions `PENDING_CONFIRMATION` -> `CONFIRMED`/`REJECTED`, and records a rejection reason from the shortfalls.

Purpose: This is the terminal half of the saga — Order reacts to Inventory's verdict. Isolated from the Kafka wiring so the transition/idempotency logic is directly unit-testable. Owns only order-context files (no overlap with the inventory-side Wave-2 plan), so it runs in parallel.
Output: `OrderProcessedEventEntity` + repository, `OrderConfirmationService`, and its unit test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-PATTERNS.md

<interfaces>
<!-- Contracts from 15-01 + existing code. Use directly. -->

OrderStockResultEvent (order_context.application.event, from 15-01): eventId, orderId, Result result (CONFIRMED/REJECTED), List<Shortfall> shortfalls; Shortfall(ingredientId, ingredientName, required, available).

OrderStatus (from 15-01): SUBMITTED, PENDING_CONFIRMATION, CONFIRMED, REJECTED.
OrderEntity (from 15-01): getStatus()/setStatus(OrderStatus); getRejectionReason()/setRejectionReason(String).
OrderRepository extends JpaRepository<OrderEntity, UUID>: findById(UUID) inherited; existing findByIdAndUserId, findByUserIdOrderBySubmittedAtDesc.

Idempotency ledger shape (mirror 15-02 InventoryProcessedEventEntity, order-context copy):
  @Table(name="processed_events", uniqueConstraints=@UniqueConstraint(name="uq_processed_event", columnNames={"event_id","consumer_name"})); UUID eventId; String consumerName; Instant processedAt.

Order-side transition (RESEARCH Code Examples): guard status==PENDING_CONFIRMATION before transitioning; set REJECTED reason from shortfalls.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order-context processed-events ledger entity + repository</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java (entity conventions in this context)
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md (Idempotency ledger entity code example)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderProcessedEventEntity.java,
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderProcessedEventRepository.java
  </files>
  <action>
    Per D-03: create `OrderProcessedEventEntity` in `order_context.infrastructure.entity` — one ledger table per context (RESEARCH Open Q2); this is the Order copy, structurally identical to the Inventory copy (15-02). Use `@Table(name="processed_events", uniqueConstraints=@UniqueConstraint(name="uq_processed_event", columnNames={"event_id","consumer_name"}))`, `@Id @GeneratedValue(strategy=GenerationType.UUID) UUID id`, `@Column(name="event_id", nullable=false) UUID eventId`, `@Column(name="consumer_name", nullable=false) String consumerName`, `@Column(name="processed_at", nullable=false) Instant processedAt`. Match this context's entity accessor style (Lombok `@Getter/@Setter` if OrderEntity uses it, else explicit accessors). Create `OrderProcessedEventRepository extends JpaRepository<OrderProcessedEventEntity, UUID>` with `boolean existsByEventIdAndConsumerName(UUID eventId, String consumerName)`.
    Note: both context ledgers map to a table literally named `processed_events`; they are separate JPA entities in separate H2/MySQL schemas per the app's single datasource — if the shared physical table name causes an H2 duplicate-table conflict in the combined test schema, disambiguate the Order copy's `name` (e.g. `order_processed_events`) and keep the same columns/constraint. Verify via the test in Task 3.
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - OrderProcessedEventEntity enforces unique (event_id, consumer_name); repository exposes existsByEventIdAndConsumerName; compiles.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OrderConfirmationService — idempotent, status-guarded transition</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/application/OrderSubmissionService.java (@Service @RequiredArgsConstructor, @Transactional, OrderRepository usage)
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md (Order-side transition code example)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java
  </files>
  <behavior>
    - CONFIRMED: order in PENDING_CONFIRMATION + CONFIRMED result -> status becomes CONFIRMED, rejectionReason stays null.
    - REJECTED: order in PENDING_CONFIRMATION + REJECTED result with a Shortfall -> status becomes REJECTED, rejectionReason describes the short ingredient(s).
    - Idempotent replay: when the ledger already has (eventId, "order-stock-result") -> return, no status change.
    - Status guard: order missing, or status already CONFIRMED/REJECTED/SUBMITTED (not PENDING_CONFIRMATION) -> return, no change.
  </behavior>
  <action>
    Per D-10/D-11: create `OrderConfirmationService` (`@Service @RequiredArgsConstructor`, inject `OrderProcessedEventRepository`, `OrderRepository`). Implement `@Transactional public void onStockResult(OrderStockResultEvent event)` per RESEARCH Code Examples: (1) idempotency — pre-check `existsByEventIdAndConsumerName(eventId, "order-stock-result")`, then insert the ledger row (catch `DataIntegrityViolationException` -> duplicate, return); (2) load `orderRepository.findById(event.orderId())`; if empty or `status != PENDING_CONFIRMATION`, return (guard against double-transition and legacy SUBMITTED rows); (3) set status to `CONFIRMED` when `result == Result.CONFIRMED`, else `REJECTED` and `order.setRejectionReason(describe(event.shortfalls()))` where `describe` joins short ingredient names/quantities into a stable human-readable reason. No publish, no cart-restore, no staff-review (D-11 — REJECTED is terminal).
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - onStockResult is @Transactional, idempotent, status-guarded, and sets CONFIRMED/REJECTED (+reason) only from PENDING_CONFIRMATION; compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Unit-test the confirmation transition (confirm / reject / idempotency / guard)</name>
  <read_first>
    - src/test/java/com/example/feat1/DDD/order_context/application/OrderSubmissionServiceTest.java (Mockito conventions in this context)
    - src/main/java/com/example/feat1/DDD/order_context/application/OrderConfirmationService.java (Task 2 output)
  </read_first>
  <files>
    src/test/java/com/example/feat1/DDD/order_context/application/OrderConfirmationServiceTest.java
  </files>
  <action>
    Create `OrderConfirmationServiceTest` (JUnit 5 + AssertJ + plain Mockito, no Spring context). Cover, per <behavior>: (a) CONFIRMED result on a PENDING_CONFIRMATION order -> status CONFIRMED, reason null; (b) REJECTED result with one Shortfall -> status REJECTED and a non-blank rejectionReason mentioning the short ingredient; (c) duplicate — `existsByEventIdAndConsumerName` true -> `findById` never invoked / status unchanged, no save side effect; (d) status guard — order already CONFIRMED (or SUBMITTED) -> no transition. Stub `OrderRepository.findById` to return an `OrderEntity` fixture with the desired status; verify the entity's status after the call.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=OrderConfirmationServiceTest test</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover confirm, reject-with-reason, duplicate-eventId no-op, and non-PENDING guard; all pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| OrderStockResultEvent payload -> confirmation handler | Untrusted result event drives a terminal status transition |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | replayed result re-transitions order | mitigate | Ledger insert (eventId, "order-stock-result") + status==PENDING_CONFIRMATION guard (D-03). |
| T-15-10 | Tampering | spurious transition of a non-pending/legacy order | mitigate | Guard ignores any order not in PENDING_CONFIRMATION (tolerates legacy SUBMITTED rows). |
| T-15-05 | Information Disclosure | rejectionReason exposure | accept | Reason lists ingredient names only; GET /orders remains owner-scoped by existing Spring Security. |
| T-15-SC | Tampering | package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `./mvnw -q -Dtest=OrderConfirmationServiceTest test` green.
- `./mvnw test` full suite green at wave merge (confirm no processed_events table-name collision in the H2 combined schema — see Task 1 note).
</verification>

<success_criteria>
- Order confirmation handler idempotently and safely completes the saga to CONFIRMED/REJECTED with reason.
- Unit tests prove D-03/D-10/D-11 behaviors.
</success_criteria>

<output>
Create `.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-05-SUMMARY.md` when done.
</output>
