---
phase: 15-kafka-event-consumers
plan: 04
type: execute
wave: 3
depends_on: [15-03]
files_modified:
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/KafkaInventoryStockResultPublisher.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfig.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/OrderCreatedListener.java
  - src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfigTest.java
autonomous: true
requirements: [D-04, D-05, D-10]
must_haves:
  truths:
    - "Inventory consumes OrderCreated from orders.created and delegates to InventoryReservationService"
    - "Inventory publishes OrderStockResultEvent to inventory.order-stock-results keyed by orderId"
    - "A deserialization failure routes to orders.created.DLT instead of blocking the partition; handler failures retry then DLT"
    - "The result topic and both DLT topics are declared as NewTopic beans so a broker without auto-create still works"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfig.java"
      provides: "@EnableKafka consumer factory + container factory + DefaultErrorHandler/DLT"
      contains: "ErrorHandlingDeserializer"
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/OrderCreatedListener.java"
      provides: "thin @KafkaListener delegating to the reservation service"
      contains: "@KafkaListener"
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java"
      provides: "NewTopic beans for result + DLT topics"
      contains: "NewTopic"
  key_links:
    - from: "OrderCreatedListener.onOrderCreated"
      to: "InventoryReservationService.onOrderCreated"
      via: "delegation"
      pattern: "reservationService.onOrderCreated"
    - from: "KafkaInventoryStockResultPublisher.publishStockResult"
      to: "inventory.order-stock-results topic"
      via: "kafkaTemplate.send(topic, orderId, event)"
      pattern: "kafkaTemplate.send"
---

<objective>
Wire the Inventory Kafka boundary (D-04/D-05/D-10): a producer config + `KafkaInventoryStockResultPublisher` adapter that emits the result event, a consumer config (`@EnableKafka`, typed `ConsumerFactory` with `ErrorHandlingDeserializer` -> `JsonDeserializer`, container factory `AckMode.RECORD`, `DefaultErrorHandler` + `DeadLetterPublishingRecoverer`), a thin `OrderCreatedListener` delegating to the reservation service from 15-03, and `NewTopic` beans so the result + DLT topics exist even on a broker without auto-create.

Purpose: Connects the reservation domain logic to Kafka. All beans are first-party Spring Kafka mirrored from the existing producer config — mechanical wiring isolated from the domain plan to preserve quality.
Output: Inventory producer config + publisher adapter, consumer config + listener, topic-provisioning config, and a config bean test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-PATTERNS.md

<interfaces>
<!-- Analog to copy + contracts from prior plans. Use directly. -->

OrderKafkaProducerConfig (analog — exact structural copy): @Configuration; @Bean ProducerFactory<String,T> (@Value("${spring.kafka.bootstrap-servers:localhost:9092}") String bootstrapServers, StringSerializer key, JsonSerializer value, DefaultKafkaProducerFactory); @Bean KafkaTemplate<String,T>.

KafkaOrderEventPublisher (analog — publisher adapter): @Component @RequiredArgsConstructor implements port; private final KafkaTemplate<String,T>; @Value("${...topic:default}") String topic; send(topic, event.orderId().toString(), event).

Ports/services from prior plans:
  InventoryStockResultPublisher (15-03): void publishStockResult(OrderStockResultEvent event).
  InventoryReservationService (15-03): void onOrderCreated(OrderCreatedEvent event).
  OrderStockResultEvent (15-01, order_context.application.event).
  OrderCreatedEvent (order_context.application.event).

Properties (added in 15-01): inventory.events.order-stock-results-topic (default inventory.order-stock-results); inventory.order-created.consumer.group-id (default inventory-order-created); order.events.order-created-topic (existing, default orders.created).

RESEARCH Pattern 1 (consumer factory) + Pattern 2 (DefaultErrorHandler + DLT) — build verbatim.
Topic provisioning (RESEARCH Open Q4 resolved -> NewTopic beans): use org.apache.kafka.clients.admin.NewTopic + Spring's TopicBuilder; a KafkaAdmin is auto-configured from spring.kafka.bootstrap-servers by Boot, so NewTopic @Beans are picked up automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory result-event producer config + publisher adapter + topic provisioning</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaProducerConfig.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/KafkaInventoryStockResultPublisher.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java
  </files>
  <action>
    Per D-05: copy `OrderKafkaProducerConfig` structure into `InventoryKafkaProducerConfig` in `inventory_context.infrastructure.config`, retyped to `OrderStockResultEvent` — two beans: `ProducerFactory<String, OrderStockResultEvent> orderStockResultProducerFactory(@Value("${spring.kafka.bootstrap-servers:localhost:9092}") String bootstrapServers)` (StringSerializer key, `org.springframework.kafka.support.serializer.JsonSerializer` value — keep Jackson-2 for producer/consumer symmetry, D-05) and `KafkaTemplate<String, OrderStockResultEvent> orderStockResultKafkaTemplate(...)`. Create `KafkaInventoryStockResultPublisher` in `inventory_context.infrastructure.adapter` (`@Component @RequiredArgsConstructor implements InventoryStockResultPublisher`) injecting `KafkaTemplate<String, OrderStockResultEvent>`, `@Value("${inventory.events.order-stock-results-topic:inventory.order-stock-results}") String topic`, implementing `publishStockResult` as `kafkaTemplate.send(topic, event.orderId().toString(), event)`.
    W-3 topic provisioning: create `InventoryKafkaTopicConfig` (`@Configuration`) with `NewTopic` `@Bean`s built via `TopicBuilder.name(...).partitions(1).replicas(1).build()` for: the result topic `${inventory.events.order-stock-results-topic:inventory.order-stock-results}`, and the two DLT topics `${order.events.order-created-topic:orders.created}` + ".DLT" and `${inventory.events.order-stock-results-topic:inventory.order-stock-results}` + ".DLT". (Boot auto-configures a `KafkaAdmin` from `spring.kafka.bootstrap-servers`, so these NewTopic beans are auto-declared when a broker is reachable and are harmless otherwise; this removes reliance on broker `auto.create.topics.enable`.) Resolve the topic names once (inject via `@Value` on the config or a small helper) so the DLT names match the actual topics.
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - InventoryKafkaProducerConfig exposes a typed ProducerFactory + KafkaTemplate for OrderStockResultEvent.
    - KafkaInventoryStockResultPublisher implements the port and sends keyed by orderId.
    - InventoryKafkaTopicConfig declares NewTopic beans for the result topic + orders.created.DLT + inventory.order-stock-results.DLT; compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Inventory OrderCreated consumer config (ErrorHandlingDeserializer + DLT) + listener</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java (bootstrap-servers @Value idiom)
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md (Pattern 1 consumer factory, Pattern 2 DefaultErrorHandler+DLT — build verbatim)
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-PATTERNS.md (OrderCreatedListener excerpt)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfig.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/OrderCreatedListener.java
  </files>
  <action>
    Per D-05/D-04: create `InventoryKafkaConsumerConfig` (`@Configuration @EnableKafka`) with: (1) `ConsumerFactory<String, OrderCreatedEvent>` — bootstrap-servers via the same `@Value` idiom, `GROUP_ID_CONFIG` from `${inventory.order-created.consumer.group-id:inventory-order-created}`, `AUTO_OFFSET_RESET_CONFIG=earliest`, `ENABLE_AUTO_COMMIT_CONFIG=false`, key `StringDeserializer`, value `ErrorHandlingDeserializer` delegating to `JsonDeserializer` with `TRUSTED_PACKAGES="com.example.feat1.DDD.order_context.application.event"`, `VALUE_DEFAULT_TYPE=OrderCreatedEvent`, `USE_TYPE_INFO_HEADERS=false` (ignore producer __TypeId__, force local type — RESEARCH Pattern 1); (2) a DLT `KafkaTemplate<String,Object>` bean named `inventoryDltKafkaTemplate` (W-4 — give it an explicit distinct bean name because multiple KafkaTemplate beans exist app-wide; StringSerializer/JsonSerializer, same bootstrap-servers); (3) `DefaultErrorHandler orderCreatedErrorHandler(@Qualifier("inventoryDltKafkaTemplate") KafkaTemplate<String,Object> dlt)` = `new DefaultErrorHandler(new DeadLetterPublishingRecoverer(dlt), new FixedBackOff(1000L, 3L))` with `DeserializationException` added as not-retryable (poison-pill -> straight to orders.created.DLT); (4) `ConcurrentKafkaListenerContainerFactory<String,OrderCreatedEvent> orderCreatedKafkaListenerContainerFactory` wiring the consumer factory + error handler, `AckMode.RECORD`. Create `OrderCreatedListener` (`@Component @RequiredArgsConstructor`) with a `@KafkaListener(topics="${order.events.order-created-topic:orders.created}", groupId="${inventory.order-created.consumer.group-id:inventory-order-created}", containerFactory="orderCreatedKafkaListenerContainerFactory")` method `onOrderCreated(OrderCreatedEvent event)` that does exactly `reservationService.onOrderCreated(event)` — no business logic (RESEARCH anti-pattern).
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - Consumer config defines factory + container factory (AckMode.RECORD) + DefaultErrorHandler with an explicitly-named inventoryDltKafkaTemplate DLT and not-retryable DeserializationException.
    - OrderCreatedListener is a one-line delegate; compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Config wiring test (beans load, error handler DLT + not-retryable deser)</name>
  <read_first>
    - src/test/resources/application.properties (auto-startup=false from 15-01)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfig.java (Task 2 output)
  </read_first>
  <files>
    src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfigTest.java
  </files>
  <action>
    Per D-04: create `InventoryKafkaConsumerConfigTest`. Keep it broker-free (RESEARCH: test the config, not a live consume). Instantiate `InventoryKafkaConsumerConfig` directly (or use a minimal `@SpringBootTest` relying on `spring.kafka.listener.auto-startup=false`) and assert: the `ConsumerFactory` bean is non-null and produces a consumer configured with `ENABLE_AUTO_COMMIT=false`; the `ConcurrentKafkaListenerContainerFactory` is non-null with `AckMode.RECORD`; the `DefaultErrorHandler` bean is non-null. If reachable via the handler API, assert a `DeserializationException` is classified not-retryable — otherwise assert the handler was constructed with a `DeadLetterPublishingRecoverer`. Prefer the direct-instantiation style to avoid a full context; if using `@SpringBootTest`, ensure no consumer container dials a broker (auto-startup=false).
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=InventoryKafkaConsumerConfigTest test</automated>
  </verify>
  <acceptance_criteria>
    - Test verifies the consumer factory, container factory (AckMode.RECORD), and DefaultErrorHandler/DLT beans wire correctly; passes without a live broker.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kafka bytes (orders.created) -> ErrorHandlingDeserializer | Untrusted payload + __TypeId__ header crosses into inventory |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Elevation of Privilege | malicious __TypeId__ -> arbitrary class instantiation | mitigate | USE_TYPE_INFO_HEADERS=false + VALUE_DEFAULT_TYPE + TRUSTED_PACKAGES allow-list (RESEARCH V5). |
| T-15-08 | Denial of Service | poison-pill blocks partition | mitigate | ErrorHandlingDeserializer + DefaultErrorHandler routes deser failures to orders.created.DLT; DeserializationException not-retryable (D-04). |
| T-15-09 | Repudiation | lost failed messages | mitigate | DeadLetterPublishingRecoverer preserves failed records + exception headers on .DLT; DLT topic pre-declared via NewTopic bean. |
| T-15-SC | Tampering | package installs | accept | No new dependencies (spring-kafka already present); EmbeddedKafka not added. |
</threat_model>

<verification>
- `./mvnw -q -Dtest=InventoryKafkaConsumerConfigTest test` green.
- `./mvnw test` full suite green at wave merge — with auto-startup=false, new @KafkaListener beans must not dial a broker; NewTopic beans are inert without a reachable KafkaAdmin/broker.
</verification>

<success_criteria>
- Inventory produces the result event and consumes OrderCreated through first-party Spring Kafka beans with DLT + poison-pill safety and pre-declared topics.
- Listener is a thin delegate; config test passes broker-free.
</success_criteria>

<output>
Create `.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-04-SUMMARY.md` when done.
</output>
