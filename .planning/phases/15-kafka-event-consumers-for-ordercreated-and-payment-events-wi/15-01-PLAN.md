---
phase: 15-kafka-event-consumers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/domain/model/OrderStatus.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java
  - src/main/java/com/example/feat1/DDD/order_context/application/OrderSubmissionService.java
  - src/main/java/com/example/feat1/DDD/order_context/application/event/OrderStockResultEvent.java
  - src/main/resources/application.properties
  - src/test/resources/application.properties
  - src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java
  - src/test/java/com/example/feat1/DDD/order_context/integration/OrderSubmissionIntegrationTest.java
autonomous: true
requirements: [D-01, D-08, D-10, D-11]
must_haves:
  truths:
    - "A newly submitted order is persisted with status PENDING_CONFIRMATION and POST /orders returns it synchronously"
    - "The OrderStockResultEvent contract exists so both contexts can serialize/deserialize the saga result"
    - "OrderCreatedEvent and OrderStockResultEvent survive a real JsonSerializer -> JsonDeserializer round-trip"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/order_context/domain/model/OrderStatus.java"
      provides: "PENDING_CONFIRMATION, CONFIRMED, REJECTED enum values"
      contains: "PENDING_CONFIRMATION"
    - path: "src/main/java/com/example/feat1/DDD/order_context/application/event/OrderStockResultEvent.java"
      provides: "Shared saga result event record with Result enum and Shortfall"
      contains: "record OrderStockResultEvent"
    - path: "src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java"
      provides: "Serde round-trip coverage closing Pitfall 5"
  key_links:
    - from: "OrderSubmissionService.submit"
      to: "OrderStatus.PENDING_CONFIRMATION"
      via: "order.setStatus"
      pattern: "setStatus\\(OrderStatus.PENDING_CONFIRMATION\\)"
---

<objective>
Establish the Order-side saga foundation: extend the order status lifecycle to `PENDING_CONFIRMATION`/`CONFIRMED`/`REJECTED`, flip order submission to create orders in `PENDING_CONFIRMATION` (D-01/D-08), define the shared `OrderStockResultEvent` contract consumed by both contexts (D-10/D-11), register saga Kafka properties, and close the untested-serializer gap (RESEARCH Pitfall 5) with a serde round-trip test.

Purpose: Every downstream plan (inventory reservation, order confirmation, both consumer wirings) depends on the status enum, the shared result-event record, and the test-side `auto-startup=false` property being in place first. This is the Wave-1 contract + config layer.
Output: Extended `OrderStatus`, `OrderEntity` (rejectionReason + default status), `OrderSubmissionService` change, new `OrderStockResultEvent`, saga config properties, serde test, extended integration test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-CONTEXT.md
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-PATTERNS.md

<interfaces>
<!-- Existing contracts. Use directly — no exploration. -->

OrderStatus.java (current — SUBMITTED only):
  public enum OrderStatus { SUBMITTED }

OrderCreatedEvent.java (order_context.application.event) — mirror its record + nested-record + TYPE-constant shape:
  record OrderCreatedEvent(UUID eventId, String eventType, Instant occurredAt, UUID orderId,
      UUID userId, OrderTable table, List<OrderLine> lines, BigDecimal total, Instant submittedAt)
      { public static final String TYPE = "OrderCreated"; ... nested records ... }

OrderEntity.java — status field at line 37: `private OrderStatus status = OrderStatus.SUBMITTED;` (@Enumerated(EnumType.STRING), @Column(nullable=false)).

OrderSubmissionService.submit (line 58): `order.setStatus(OrderStatus.SUBMITTED);`

application.properties (lines 24-30): spring.kafka.bootstrap-servers, spring.kafka.producer.*, order.events.order-created-topic=orders.created, payment.events.topic, table.events.topic.

Result-event schema (from RESEARCH Code Examples — build to this shape):
  record OrderStockResultEvent(UUID eventId, String eventType, Instant occurredAt, UUID orderId,
      Result result, List<Shortfall> shortfalls) {
    enum Result { CONFIRMED, REJECTED }
    record Shortfall(UUID ingredientId, String ingredientName, BigDecimal required, BigDecimal available) {}
    static final String CONFIRMED_TYPE = "OrderStockConfirmed";
    static final String REJECTED_TYPE  = "OrderStockRejected"; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend order status lifecycle and default new orders to PENDING_CONFIRMATION</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/domain/model/OrderStatus.java
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java (status field line 37, @Column style lines 42-60)
    - src/main/java/com/example/feat1/DDD/order_context/application/OrderSubmissionService.java (line 58)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/domain/model/OrderStatus.java,
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderEntity.java,
    src/main/java/com/example/feat1/DDD/order_context/application/OrderSubmissionService.java
  </files>
  <action>
    Per D-01: add `PENDING_CONFIRMATION`, `CONFIRMED`, `REJECTED` to the `OrderStatus` enum, keeping the existing `SUBMITTED` value so historical `EnumType.STRING` rows remain valid (RESEARCH Runtime State Inventory — do not remove or renumber). In `OrderEntity`, change the status field default from `OrderStatus.SUBMITTED` to `OrderStatus.PENDING_CONFIRMATION`, and add a nullable `rejectionReason` column mirroring the existing nullable `@Column` style (name `rejection_reason`, `String`, nullable, no length change needed). Add a Lombok-generated getter/setter is already implied by the entity's class-level annotations — confirm the entity uses Lombok `@Getter/@Setter`; if it uses explicit accessors, add `getRejectionReason`/`setRejectionReason`. In `OrderSubmissionService.submit`, change line 58 `order.setStatus(OrderStatus.SUBMITTED)` to `order.setStatus(OrderStatus.PENDING_CONFIRMATION)` (D-01/D-08). Do NOT touch the after-commit publish wiring (lines 81, 225-237) — it already starts the saga.
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - OrderStatus contains SUBMITTED, PENDING_CONFIRMATION, CONFIRMED, REJECTED.
    - OrderEntity default status is PENDING_CONFIRMATION and it has a nullable rejectionReason column + accessors.
    - OrderSubmissionService.submit sets PENDING_CONFIRMATION; project compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create shared OrderStockResultEvent contract and register saga Kafka properties</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/application/event/OrderCreatedEvent.java (record + TYPE-constant shape to mirror)
    - src/main/resources/application.properties (lines 24-30 — property idiom to mirror)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/application/event/OrderStockResultEvent.java,
    src/main/resources/application.properties,
    src/test/resources/application.properties
  </files>
  <action>
    Per D-10/D-11: create `OrderStockResultEvent` in package `com.example.feat1.DDD.order_context.application.event` as a record `(UUID eventId, String eventType, Instant occurredAt, UUID orderId, Result result, List<Shortfall> shortfalls)` with a nested `enum Result { CONFIRMED, REJECTED }`, a nested `record Shortfall(UUID ingredientId, String ingredientName, BigDecimal required, BigDecimal available)`, and string constants `CONFIRMED_TYPE = "OrderStockConfirmed"` / `REJECTED_TYPE = "OrderStockRejected"` — mirroring the `TYPE`-constant convention in `OrderCreatedEvent`. This class is shared: both inventory and order contexts import it (the consumer TRUSTED_PACKAGES must list this exact package). In `src/main/resources/application.properties`, add saga properties using the existing `${ENV:default}` idiom: `inventory.events.order-stock-results-topic=${INVENTORY_EVENTS_ORDER_STOCK_RESULTS_TOPIC:inventory.order-stock-results}`, `inventory.order-created.consumer.group-id=${INVENTORY_ORDER_CREATED_GROUP_ID:inventory-order-created}`, and `order.stock-result.consumer.group-id=${ORDER_STOCK_RESULT_GROUP_ID:order-stock-result}`. In `src/test/resources/application.properties`, add `spring.kafka.listener.auto-startup=false` (RESEARCH Pitfall 1 — prevents consumer containers dialing a non-existent broker during tests once listener beans exist).
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - OrderStockResultEvent record exists with Result enum, Shortfall record, and CONFIRMED_TYPE/REJECTED_TYPE constants in order_context.application.event.
    - application.properties defines the result topic + both consumer group-id properties.
    - Test application.properties sets spring.kafka.listener.auto-startup=false.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Serde round-trip test + extend order submission integration test for PENDING_CONFIRMATION</name>
  <read_first>
    - src/test/java/com/example/feat1/DDD/order_context/integration/OrderSubmissionIntegrationTest.java
    - src/main/java/com/example/feat1/DDD/order_context/application/event/OrderCreatedEvent.java
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md (Pitfall 5)
  </read_first>
  <files>
    src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java,
    src/test/java/com/example/feat1/DDD/order_context/integration/OrderSubmissionIntegrationTest.java
  </files>
  <action>
    Create `EventSerdeRoundTripTest` (plain JUnit 5, no Spring context) that closes RESEARCH Pitfall 5 / assumption A2: instantiate the real Jackson-2 `org.springframework.kafka.support.serializer.JsonSerializer` and `JsonDeserializer` (the same classes the producers use), serialize a fully-populated `OrderCreatedEvent` (with a table, at least one line with a selected topping) to bytes and deserialize back, asserting field-equality; do the same for `OrderStockResultEvent` (both a CONFIRMED result with empty shortfalls and a REJECTED result with one Shortfall). Configure the deserializer with the event's package trusted (or construct it typed to the target class) so it round-trips without a broker. Extend `OrderSubmissionIntegrationTest`: assert that after `POST /orders` the returned order and the persisted `OrderEntity` have status `PENDING_CONFIRMATION` (D-01/D-08) rather than `SUBMITTED`.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=EventSerdeRoundTripTest,OrderSubmissionIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - EventSerdeRoundTripTest round-trips OrderCreatedEvent and both OrderStockResultEvent variants through the real JsonSerializer/JsonDeserializer and passes.
    - OrderSubmissionIntegrationTest asserts POST /orders yields PENDING_CONFIRMATION and passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kafka payload -> consumer deserializer | Result/created event bytes are untrusted input at the deserialization boundary (validated in wiring plans 15-04/15-06) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | OrderStockResultEvent deserialization | mitigate | Fixed record shape + typed serde; trusted-package config enforced in consumer configs (15-04/15-06). Serde round-trip test proves the contract here. |
| T-15-05 | Information Disclosure | rejectionReason text on order | accept | Reason lists short ingredient names only; order read already owner-scoped by existing Spring Security (V4 unchanged). |
| T-15-SC | Tampering | package installs | accept | No new dependencies added this phase (spring-kafka already present); RESEARCH Package Legitimacy Audit found no new coordinates. |
</threat_model>

<verification>
- `./mvnw -q compile` succeeds after each code task.
- `./mvnw test` (full suite, 119+ tests) stays green at wave merge — existing SUBMITTED-dependent reads must tolerate the new default.
</verification>

<success_criteria>
- OrderStatus, OrderEntity, and OrderSubmissionService reflect D-01/D-08.
- OrderStockResultEvent contract and saga properties exist.
- Serde round-trip and PENDING_CONFIRMATION integration assertions pass.
</success_criteria>

<output>
Create `.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-01-SUMMARY.md` when done.
</output>
