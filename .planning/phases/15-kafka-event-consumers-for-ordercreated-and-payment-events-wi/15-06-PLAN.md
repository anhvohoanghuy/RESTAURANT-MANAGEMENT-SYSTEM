---
phase: 15-kafka-event-consumers
plan: 06
type: execute
wave: 3
depends_on: [15-05]
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfig.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderStockResultListener.java
  - src/test/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfigTest.java
autonomous: true
requirements: [D-04, D-05, D-10]
must_haves:
  truths:
    - "Order consumes OrderStockResultEvent from inventory.order-stock-results and delegates to OrderConfirmationService"
    - "A deserialization failure routes to inventory.order-stock-results.DLT instead of blocking the partition; handler failures retry then DLT"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfig.java"
      provides: "@EnableKafka consumer factory + container factory + DefaultErrorHandler/DLT for the result event"
      contains: "ErrorHandlingDeserializer"
    - path: "src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderStockResultListener.java"
      provides: "thin @KafkaListener delegating to OrderConfirmationService"
      contains: "@KafkaListener"
  key_links:
    - from: "OrderStockResultListener.onStockResult"
      to: "OrderConfirmationService.onStockResult"
      via: "delegation"
      pattern: "confirmationService.onStockResult"
---

<objective>
Wire the Order-side Kafka consumer (D-04/D-05/D-10): a consumer config typed to `OrderStockResultEvent` (`@EnableKafka`, `ErrorHandlingDeserializer` -> `JsonDeserializer`, container factory `AckMode.RECORD`, `DefaultErrorHandler` + `DeadLetterPublishingRecoverer`) and a thin `OrderStockResultListener` delegating to `OrderConfirmationService` from 15-05. This closes the saga loop end-to-end.

Purpose: The final connection — Order now consumes Inventory's verdict off the wire. Mechanical wiring mirrored from the existing producer config + the inventory consumer config (15-04), isolated from the transition logic to preserve quality.
Output: Order consumer config + listener + config bean test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md
@.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-PATTERNS.md

<interfaces>
<!-- Analog + prior-plan contracts. Use directly. -->

OrderKafkaProducerConfig (analog for bootstrap-servers @Value + factory idiom).
InventoryKafkaConsumerConfig (15-04) — identical consumer-config structure, retype OrderCreatedEvent -> OrderStockResultEvent and swap topic/group/trusted-package.

Contracts:
  OrderConfirmationService.onStockResult(OrderStockResultEvent event) (15-05).
  OrderStockResultEvent (order_context.application.event, from 15-01).
  Properties (from 15-01): inventory.events.order-stock-results-topic (default inventory.order-stock-results); order.stock-result.consumer.group-id (default order-stock-result).
  TRUSTED_PACKAGES for the result event = "com.example.feat1.DDD.order_context.application.event".

RESEARCH Pattern 1 (consumer factory) + Pattern 2 (DefaultErrorHandler + DLT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order result-event consumer config (ErrorHandlingDeserializer + DLT)</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaProducerConfig.java
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-RESEARCH.md (Pattern 1, Pattern 2 — build verbatim)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfig.java
  </files>
  <action>
    Per D-05/D-04: create `OrderKafkaConsumerConfig` (`@Configuration @EnableKafka`) mirroring the inventory consumer config (15-04) but typed to `OrderStockResultEvent`: (1) `ConsumerFactory<String, OrderStockResultEvent>` with bootstrap-servers via `@Value`, `GROUP_ID_CONFIG` from `${order.stock-result.consumer.group-id:order-stock-result}`, `AUTO_OFFSET_RESET_CONFIG=earliest`, `ENABLE_AUTO_COMMIT_CONFIG=false`, key `StringDeserializer`, value `ErrorHandlingDeserializer` delegating to `JsonDeserializer` with `TRUSTED_PACKAGES="com.example.feat1.DDD.order_context.application.event"`, `VALUE_DEFAULT_TYPE=OrderStockResultEvent`, `USE_TYPE_INFO_HEADERS=false`; (2) a DLT `KafkaTemplate<String,Object>` bean; (3) `DefaultErrorHandler orderStockResultErrorHandler` = `new DefaultErrorHandler(new DeadLetterPublishingRecoverer(dltTemplate), new FixedBackOff(1000L, 3L))` with `DeserializationException` not-retryable (routes to inventory.order-stock-results.DLT); (4) `ConcurrentKafkaListenerContainerFactory<String,OrderStockResultEvent> orderStockResultKafkaListenerContainerFactory` with `AckMode.RECORD`. Name beans distinctly from the inventory config to avoid collisions.
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - Consumer config defines a typed factory, container factory (AckMode.RECORD), and DefaultErrorHandler with DLT + not-retryable DeserializationException; compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: OrderStockResultListener thin delegate</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/KafkaOrderEventPublisher.java (adapter conventions)
    - .planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-PATTERNS.md (OrderStockResultListener assignment)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderStockResultListener.java
  </files>
  <action>
    Per D-10: create `OrderStockResultListener` (`@Component @RequiredArgsConstructor`) injecting `OrderConfirmationService`. Add a `@KafkaListener(topics="${inventory.events.order-stock-results-topic:inventory.order-stock-results}", groupId="${order.stock-result.consumer.group-id:order-stock-result}", containerFactory="orderStockResultKafkaListenerContainerFactory")` method `onStockResult(OrderStockResultEvent event)` that does exactly `confirmationService.onStockResult(event)` — no business logic (RESEARCH anti-pattern: listener stays a one-line delegate).
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - OrderStockResultListener is a one-line delegate on the result topic using the config's container factory; compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Config wiring test + full saga suite green</name>
  <read_first>
    - src/test/resources/application.properties (auto-startup=false from 15-01)
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfig.java (Task 1 output)
  </read_first>
  <files>
    src/test/java/com/example/feat1/DDD/order_context/infrastructure/config/OrderKafkaConsumerConfigTest.java
  </files>
  <action>
    Per D-04: create `OrderKafkaConsumerConfigTest` broker-free (mirror InventoryKafkaConsumerConfigTest from 15-04). Instantiate `OrderKafkaConsumerConfig` directly and assert: `ConsumerFactory` non-null with `ENABLE_AUTO_COMMIT=false`; `ConcurrentKafkaListenerContainerFactory` non-null with `AckMode.RECORD`; `DefaultErrorHandler` non-null and constructed with a `DeadLetterPublishingRecoverer` (and, if reachable, `DeserializationException` classified not-retryable). After this task, run the full suite to confirm the whole saga (both consumers + all services) coexists with `spring.kafka.listener.auto-startup=false` and no listener dials a broker.
  </action>
  <verify>
    <automated>./mvnw test</automated>
  </verify>
  <acceptance_criteria>
    - OrderKafkaConsumerConfigTest passes broker-free.
    - Full Maven suite (119+ existing tests plus all new Phase-15 tests) is green with both consumers wired and auto-startup disabled in tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kafka bytes (inventory.order-stock-results) -> ErrorHandlingDeserializer | Untrusted result payload + __TypeId__ header crosses into order context |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Elevation of Privilege | malicious __TypeId__ -> arbitrary class instantiation | mitigate | USE_TYPE_INFO_HEADERS=false + VALUE_DEFAULT_TYPE + TRUSTED_PACKAGES allow-list. |
| T-15-08 | Denial of Service | poison-pill blocks partition | mitigate | ErrorHandlingDeserializer + DefaultErrorHandler -> inventory.order-stock-results.DLT; DeserializationException not-retryable (D-04). |
| T-15-09 | Repudiation | lost failed results | mitigate | DeadLetterPublishingRecoverer preserves failed records + exception headers on .DLT. |
| T-15-SC | Tampering | package installs | accept | No new dependencies. |
</threat_model>

<verification>
- `./mvnw -q -Dtest=OrderKafkaConsumerConfigTest test` green.
- `./mvnw test` full suite green — the phase gate before `/gsd:verify-work`.
</verification>

<success_criteria>
- Order consumes the result event through first-party Spring Kafka beans with DLT + poison-pill safety and completes the saga.
- Full suite green with both consumers wired.
</success_criteria>

<output>
Create `.planning/phases/15-kafka-event-consumers-for-ordercreated-and-payment-events-wi/15-06-SUMMARY.md` when done.
</output>
