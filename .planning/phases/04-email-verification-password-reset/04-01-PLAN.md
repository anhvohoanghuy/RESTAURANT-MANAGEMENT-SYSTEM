---
phase: 04-email-verification-password-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/auth/application/AuthAccountRecoveryService.java
  - src/main/java/com/example/feat1/DDD/auth/application/notification/EmailNotificationPort.java
  - src/main/java/com/example/feat1/DDD/auth/domain/model/AuthActionTokenPurpose.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/entity/AuthActionTokenEntity.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/presentation/AuthController.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/repository/IAuthActionTokenRepository.java
  - src/main/java/com/example/feat1/DDD/identity_context/domain/model/aggregate/User.java
  - src/main/java/com/example/feat1/DDD/identity_context/infastructure/entity/UserEntity.java
  - src/test/java/com/example/feat1/DDD/auth/**
autonomous: true
requirements: [AUTH-012, AUTH-013, AUTH-014]
requirements_addressed: [AUTH-012, AUTH-013, AUTH-014]
user_setup:
  - No SMTP/provider setup is required in this phase.
must_haves:
  truths:
    - D-01: Verification request endpoint returns `202`.
    - D-02: Email verify endpoint returns `204`.
    - D-03: Forgot password endpoint returns generic `202`.
    - D-04: Password reset endpoint returns `204`.
    - D-05: Public APIs never return raw action tokens.
    - D-06: Action tokens are stored as hashes.
    - D-07: Raw tokens are emitted only through `EmailNotificationPort`.
    - D-08: Verification tokens default to 24h expiry.
    - D-09: Reset tokens default to 15m expiry.
    - D-10: Action tokens are single-use.
    - D-11: Local registration starts unverified.
    - D-12: Local registration requests verification notification.
    - D-13: Unverified users are not blocked in this phase.
    - D-14: Google verified emails mark users verified on create/link.
    - D-15: Password reset changes local credentials only.
    - D-16: OAuth-only accounts do not receive reset tokens.
    - D-17: Password reset revokes active refresh tokens.
    - D-18: Invalid verification token uses `EMAIL_VERIFICATION_TOKEN_INVALID`.
    - D-19: Expired verification token uses `EMAIL_VERIFICATION_TOKEN_EXPIRED`.
    - D-20: Invalid reset token uses `PASSWORD_RESET_TOKEN_INVALID`.
    - D-21: Expired reset token uses `PASSWORD_RESET_TOKEN_EXPIRED`.
    - D-22: Invalid reset password uses `PASSWORD_RESET_PASSWORD_INVALID`.
---

<objective>
Implement Phase 04 email verification and password reset token APIs.
</objective>

<context>
@.planning/phases/04-email-verification-password-reset/04-CONTEXT.md
@.planning/phases/02-auth-context-mvp/02-CONTEXT.md
@.planning/phases/03-google-oauth-2-login/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verification state and token persistence</name>
  <action>Add email verification fields to user domain/entity/profile DTO and add hashed single-use auth action token persistence for verification and reset purposes.</action>
  <verify>Unit tests assert raw token is not stored and tokens are single-use.</verify>
</task>

<task type="auto">
  <name>Task 2: Add recovery service and notification port</name>
  <action>Add service methods for requesting verification, verifying email, requesting reset, and resetting local password. Emit raw tokens only through `EmailNotificationPort`.</action>
  <verify>Unit tests cover invalid, expired, consumed, and valid token paths.</verify>
</task>

<task type="auto">
  <name>Task 3: Wire auth APIs and existing auth flows</name>
  <action>Add four public endpoints, request verification after local registration, mark Google verified emails as verified on create/link, and revoke refresh tokens after reset.</action>
  <verify>Controller and integration tests cover endpoint status and behavior.</verify>
</task>

<task type="auto">
  <name>Task 4: Full verification</name>
  <action>Run the full Maven test suite and create a summary artifact after completion.</action>
  <verify>`mvnw.cmd test` passes.</verify>
</task>

</tasks>

<verification>
- [ ] `mvnw.cmd test` passes with Java 17.
- [ ] Public APIs do not return raw tokens.
- [ ] Integration tests capture tokens through notification port.
- [ ] Password reset revokes active refresh tokens.
</verification>

<success_criteria>
- AUTH-012, AUTH-013, and AUTH-014 are covered.
- Existing local auth and Google OAuth contracts remain unchanged.
- No real email provider is required.
</success_criteria>
