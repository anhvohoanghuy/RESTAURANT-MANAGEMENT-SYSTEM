---
phase: 16-kitchen-preparing-workflow-order-item-in-progress-status-and
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/StockReservationEntity.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/ReservationSettlementEntity.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/ReservationSettlementRepository.java
autonomous: true
requirements: [D-03, D-04, D-05]
must_haves:
  truths:
    - "(D-04) InventoryMovementType has a distinct CONSUMPTION value classified as outbound (decreases on_hand), separate from WASTE/ADJUSTMENT_OUT"
    - "(D-05) StockReservationEntity.ReservationStatus gains SETTLED; the ReservationLine embeddable and lines collection are unchanged"
    - "(D-05) StockReservationRepository exposes a PESSIMISTIC_WRITE lockByOrderId so the last-line settlement check is atomic"
    - "(D-03) ReservationSettlementEntity + repository provide a unique (orderId, orderLineId) per-line settlement guard, with existence and per-order count queries"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/ReservationSettlementEntity.java"
      provides: "Per-line settlement guard row (double-deduction prevention + last-line counting)"
      contains: "orderLineId"
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/ReservationSettlementRepository.java"
      provides: "existsByOrderIdAndOrderLineId + countByOrderId"
      contains: "existsByOrderIdAndOrderLineId"
  key_links:
    - from: "StockReservationRepository.lockByOrderId"
      to: "InventorySettlementService last-line check (16-04)"
      via: "PESSIMISTIC_WRITE lock on reservation row"
      pattern: "lockByOrderId"
    - from: "ReservationSettlementRepository"
      to: "InventorySettlementService idempotency guard (16-04)"
      via: "existsByOrderIdAndOrderLineId / countByOrderId"
      pattern: "existsByOrderIdAndOrderLineId"
---

<objective>
Establish the Inventory Context data layer for per-line settlement without touching the Phase 15 reservation model shape (D-05): add a distinct `InventoryMovementType.CONSUMPTION` for the audit deduction (D-04/WR-02), add the `SETTLED` reservation lifecycle state, add a pessimistic `lockByOrderId` for atomic last-line detection, and introduce a new sibling `ReservationSettlementEntity`/repository as the per-`(orderId, orderLineId)` double-settlement guard and last-line counter (D-03/D-05).

Purpose: Pure data/enum/repository scaffolding the settlement service (16-04) locks and writes against. Isolated in Wave 1 with zero file overlap with the order-side foundation (16-01) so both run in parallel.
Output: `InventoryMovementType.CONSUMPTION`, `ReservationStatus.SETTLED`, `StockReservationRepository.lockByOrderId`, `ReservationSettlementEntity`, `ReservationSettlementRepository`.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-preparing-workflow-order-item-in-progress-status-and/16-PATTERNS.md

<interfaces>
<!-- Existing shapes to mirror. Use directly. -->

InventoryMovementType (inventory_context.domain.model): enum {RECEIPT, ADJUSTMENT_IN, ADJUSTMENT_OUT, WASTE, STOCK_COUNT} with isInbound()/isOutbound()/isCount() helpers. Add CONSUMPTION and include it in isOutbound().

StockReservationEntity (inventory_context.infrastructure.entity): has nested enum ReservationStatus{HELD}, embeddable ReservationLine (DO NOT touch — D-05), lines collection, orderId, status, static held(orderId, Map). Only touch the enum.

StockReservationRepository: extends JpaRepository<StockReservationEntity, UUID>; existsByOrderId(UUID), findByOrderId(UUID). Add lockByOrderId.

@Lock idiom to copy — InventoryStockBalanceRepository.lockByIngredientAndLocation: `@Lock(LockModeType.PESSIMISTIC_WRITE) @Query("select ... where ...") Optional<...> lockBy...(...)`.

InventoryProcessedEventEntity (inventory_context.infrastructure.entity) — unique-constraint ledger-row shape to mirror for the new guard entity: @Entity @Table(name=..., uniqueConstraints=@UniqueConstraint(columnNames={...})), @Id @GeneratedValue(GenerationType.UUID), @Column fields, Instant timestamp.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONSUMPTION movement type, SETTLED reservation status, and lockByOrderId</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java (file being modified — isOutbound disjunction)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/StockReservationEntity.java (file being modified — touch only ReservationStatus; D-05 forbids touching ReservationLine)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java (file being modified)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryStockBalanceRepository.java (lines 18-26 — the @Lock(PESSIMISTIC_WRITE)+@Query idiom to copy)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/StockReservationEntity.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java
  </files>
  <action>
    In `InventoryMovementType` add a new constant `CONSUMPTION` and include it in the `isOutbound()` disjunction (it decreases `quantity_on_hand`, same polarity as `WASTE`/`ADJUSTMENT_OUT`) — keep it a DISTINCT value so audit/reporting can separate real kitchen consumption from manual adjustments/waste (RESEARCH A3). In `StockReservationEntity`, add `SETTLED` to the nested `ReservationStatus` enum (append after `HELD`). Do NOT modify the `ReservationLine` embeddable, the `lines` collection, or the `held(...)` factory — D-05 forbids changing the reservation model shape. In `StockReservationRepository`, add `@Lock(LockModeType.PESSIMISTIC_WRITE) @Query("select r from StockReservationEntity r where r.orderId = :orderId") Optional<StockReservationEntity> lockByOrderId(UUID orderId)`, copying the exact annotation idiom from `InventoryStockBalanceRepository.lockByIngredientAndLocation`. Add the `jakarta.persistence.LockModeType`, `org.springframework.data.jpa.repository.Lock`, and `org.springframework.data.jpa.repository.Query` imports as needed (non-wildcard).
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - `InventoryMovementType.CONSUMPTION` exists and `isOutbound()` returns true for it; it is a distinct constant from WASTE/ADJUSTMENT_OUT.
    - `ReservationStatus` has `SETTLED`; `ReservationLine`/`lines`/`held(...)` are untouched.
    - `StockReservationRepository.lockByOrderId` exists with `@Lock(PESSIMISTIC_WRITE)`; project compiles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ReservationSettlementEntity + ReservationSettlementRepository (per-line guard)</name>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryProcessedEventEntity.java (unique-constraint ledger-row shape to mirror)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java (JpaRepository interface style)
    - .planning/phases/16-kitchen-preparing-workflow-order-item-in-progress-status-and/16-PATTERNS.md (ReservationSettlementEntity pattern assignment)
  </read_first>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/ReservationSettlementEntity.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/ReservationSettlementRepository.java
  </files>
  <action>
    Create `ReservationSettlementEntity` in `inventory_context.infrastructure.entity`, mirroring `InventoryProcessedEventEntity`'s unique-constraint ledger idiom: `@Getter @Setter @Entity @Table(name = "reservation_settlements", uniqueConstraints = @UniqueConstraint(name = "uq_reservation_settlement_line", columnNames = {"order_id", "order_line_id"}))`. Fields: `@Id @GeneratedValue(strategy = GenerationType.UUID) UUID id`; `@Column(name = "order_id", nullable = false) UUID orderId`; `@Column(name = "order_line_id", nullable = false) UUID orderLineId`; `@Column(name = "settled_at", nullable = false) Instant settledAt`. The unique `(order_id, order_line_id)` constraint is the D-03 per-line double-settlement guard. Create `ReservationSettlementRepository extends JpaRepository<ReservationSettlementEntity, UUID>` with derived queries `boolean existsByOrderIdAndOrderLineId(UUID orderId, UUID orderLineId)` (double-settlement guard) and `long countByOrderId(UUID orderId)` (last-line detection — compare against event.totalLines under the reservation lock in 16-04). Use non-wildcard imports.
  </action>
  <verify>
    <automated>./mvnw -q compile</automated>
  </verify>
  <acceptance_criteria>
    - `ReservationSettlementEntity` maps to table `reservation_settlements` with a unique `(order_id, order_line_id)` constraint and a `settledAt` timestamp.
    - `ReservationSettlementRepository` exposes `existsByOrderIdAndOrderLineId` and `countByOrderId`.
    - Project compiles.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent settlement transactions → reservation + settlement-guard rows | Two lines of the same order settling concurrently must not double-deduct or both miss the SETTLED transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02a | Tampering (data integrity) | Duplicate per-line settlement | mitigate | Unique `(order_id, order_line_id)` constraint on `reservation_settlements` guarantees a line cannot be recorded/deducted twice even under concurrent delivery |
| T-16-04a | Tampering | Reservation model integrity | mitigate | `ReservationLine`/`lines` left untouched (D-05); settlement progress tracked in a NEW sibling table, not by mutating the reservation shape |
| T-16-08 | Tampering | Last-line race | mitigate | `lockByOrderId` (PESSIMISTIC_WRITE) makes the count-vs-totalLines check atomic in 16-04 |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — pure Java entity/repo/enum changes, no new dependencies |
</threat_model>

<verification>
- `./mvnw -q compile` succeeds.
- Full suite `./mvnw -q test` green at wave merge (schema additions must not break context load).
</verification>

<success_criteria>
- CONSUMPTION movement type, SETTLED reservation state, atomic `lockByOrderId`, and the per-line settlement-guard entity/repository exist and compile, unblocking 16-04 (settlement service).
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-preparing-workflow-order-item-in-progress-status-and/16-02-SUMMARY.md` when done.
</output>
