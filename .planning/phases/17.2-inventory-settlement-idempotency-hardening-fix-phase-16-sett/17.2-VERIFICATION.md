---
phase: 17.2-inventory-settlement-idempotency-hardening
status: passed
verified_at: 2026-07-09
verifier: codex
---

# Phase 17.2 Verification

## Result

PASS - 9/9 must-have truths verified.

## Evidence

- `InventoryReservationSettlementService` no longer depends on `InventoryLedgerWriter`.
- `InventoryLedgerWriter.java` and `InventoryLedgerWriterTest.java` are deleted.
- `recordProcessed(eventId)` writes `InventoryProcessedEventEntity` through `InventoryProcessedEventRepository.save(...)`.
- Processed-event recording occurs after settlement mutation work and inside the same settlement transaction.
- `DataIntegrityViolationException` from the final processed-event save propagates, allowing the settlement transaction to roll back atomically.
- Duplicate `eventId` and duplicate `(orderId, orderLineId)` guards remain in place and return before mutation.
- Scan passed: `rg -n "InventoryLedgerWriter|Propagation\\.REQUIRES_NEW" src/main/java src/test/java` returned no matches.
- Focused test passed: `mvn test -Dtest=InventoryReservationSettlementServiceTest`.
- Full regression passed: `mvn test` with `Tests run: 207, Failures: 0, Errors: 0, Skipped: 0`.

## Must-Have Truths

- D-01: Inventory settlement no longer depends on `InventoryLedgerWriter` or any `REQUIRES_NEW` idempotency-ledger insert. PASS
- D-02: The settlement eventId ledger row is written as the last durable step in the same transaction as the settlement mutation. PASS
- D-03: A concurrent duplicate ledger unique-constraint failure propagates and rolls back the whole settlement transaction instead of being swallowed. PASS
- D-04: Existing eventId pre-check and per-order-line settlement guard still skip already processed work. PASS
- D-05: `InventoryLedgerWriter.java` and its obsolete dedicated test are deleted. PASS
- D-06: Regression tests prove final same-transaction ledger insert and duplicate-ledger propagation behavior. PASS
- D-07: Phase 16 settlement behavior remains unchanged except the ledger ordering pattern. PASS
- D-08: No kitchen settle-trigger/status-changed transactional outbox was introduced in this phase. PASS
- D-09: Payment/Table Kafka producer serializer and payment-history filters were not touched in this phase. PASS

## Residual Risk

No blocking residual risk found for the scoped Phase 17.2 idempotency fix.
