---
phase: 16-inventory-reservation-settlement
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-04]
autonomous: true
requirements: [D-01, D-05]
files_modified:
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfig.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/SettleTriggerListener.java
  - src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfigTest.java
  - src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java

must_haves:
  truths:
    - "A SettleTriggerEvent consumer factory uses ErrorHandlingDeserializer → JacksonJsonDeserializer with USE_TYPE_INFO_HEADERS=false, fixed VALUE_DEFAULT_TYPE, and a TRUSTED_PACKAGES allow-list [D-01]"
    - "The listener container uses AckMode.RECORD and a DefaultErrorHandler + DeadLetterPublishingRecoverer with FixedBackOff(1000,3); only DeserializationException is non-retryable [D-01/D-05]"
    - "A NewTopic bean exists for the settle-trigger topic and its .DLT, derived from the same @Value property [D-05]"
    - "SettleTriggerListener is a thin @KafkaListener delegating to InventoryReservationSettlementService.onSettleTrigger [D-01]"
    - "SettleTriggerEvent round-trips through Jackson-3 serde with trusted packages com.example.feat1.* [D-01]"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfig.java"
      provides: "Typed consumer factory + error handler + container factory for SettleTriggerEvent [D-01/D-05]"
      contains: "settleTriggerKafkaListenerContainerFactory"
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/SettleTriggerListener.java"
      provides: "Thin Kafka listener delegating to the settlement service [D-01]"
      contains: "@KafkaListener"
  key_links:
    - from: "SettleTriggerListener"
      to: "InventoryReservationSettlementService.onSettleTrigger"
      via: "delegate call"
      pattern: "onSettleTrigger"
    - from: "SettleTriggerKafkaConsumerConfig"
      to: "DeadLetterPublishingRecoverer"
      via: "DefaultErrorHandler"
      pattern: "DeadLetterPublishingRecoverer"
    - from: "InventoryKafkaTopicConfig"
      to: "settle-trigger topic + .DLT"
      via: "NewTopic beans from one @Value property"
      pattern: "DLT_SUFFIX"
---

<objective>
Wire the Kafka boundary for the settlement consumer: a dedicated typed consumer factory / error handler / container factory for SettleTriggerEvent, the settle-trigger + DLT NewTopic beans, the thin listener delegating to the settlement service, and a serde round-trip test. Mirrors Phase 15's InventoryKafkaConsumerConfig exactly; broker-free (producer is Phase 17).

Purpose: Make the settlement service reachable via Kafka with poison-pill/type-confusion hardening and DLT routing (D-01/D-05).
Output: SettleTriggerKafkaConsumerConfig, extended InventoryKafkaTopicConfig, SettleTriggerListener, config test, serde test case.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-inventory-reservation-settlement/16-CONTEXT.md
@.planning/phases/16-inventory-reservation-settlement/16-RESEARCH.md
@.planning/phases/16-inventory-reservation-settlement/16-PATTERNS.md

<interfaces>
<!-- Consumed from earlier waves: -->
SettleTriggerEvent (Plan 01): record(eventId, eventType, occurredAt, orderId, orderLineId, totalLines), TYPE="SettleTrigger".
InventoryReservationSettlementService.onSettleTrigger(SettleTriggerEvent) (Plan 04).

<!-- Analog to mirror bean-for-bean: -->
Consumer config: src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfig.java (orderCreatedConsumerFactory lines 47-68; orderCreatedErrorHandler lines 86-96; container factory with AckMode.RECORD; inventoryDltKafkaTemplate lines 76-84 — REUSE this DLT template by @Qualifier). 16-PATTERNS.md lines 583-622.
Topic config: src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java (DLT_SUFFIX + NewTopic TopicBuilder pattern). 16-PATTERNS.md lines 626-654.
Listener: src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/OrderCreatedListener.java (thin @KafkaListener). 16-PATTERNS.md lines 539-579.
Config test: src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfigTest.java (broker-free; instantiates the config class directly, asserts deserializer props / non-retryable exceptions).
Serde test: src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java (existing round-trip cases to extend).

<!-- Discretionary names LOCKED here so Phase 17 producer matches (RESEARCH Open Q2): -->
Topic property: kitchen.events.settle-trigger-topic (default kitchen.settlement-trigger); DLT = <topic>.DLT.
Consumer group property: inventory.settlement.consumer.group-id (default inventory-settlement).
TRUSTED_PACKAGES: com.example.feat1.DDD.inventory_context.application.event (SettleTriggerEvent's package); VALUE_DEFAULT_TYPE = SettleTriggerEvent.class.getName().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettleTriggerKafkaConsumerConfig + settle-trigger/DLT topics + config test</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfig.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java,
    src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/SettleTriggerKafkaConsumerConfigTest.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfig.java (consumer factory + error handler + container factory + inventoryDltKafkaTemplate to reuse — bean-for-bean analog)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java (the file being modified; DLT_SUFFIX + NewTopic pattern)
    - src/test/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaConsumerConfigTest.java (broker-free config-test analog)
    - 16-PATTERNS.md lines 583-654 (exact wiring to mirror; TRUSTED_PACKAGES/VALUE_DEFAULT_TYPE for SettleTriggerEvent)
  </read_first>
  <action>
    Create SettleTriggerKafkaConsumerConfig as a new @Configuration class (do NOT add @EnableKafka — it is already declared on InventoryKafkaConsumerConfig; IN-03). Define three beans mirroring the orderCreated triplet but typed for SettleTriggerEvent with distinct names: settleTriggerConsumerFactory (ConsumerFactory<String, SettleTriggerEvent>), settleTriggerErrorHandler (DefaultErrorHandler), settleTriggerKafkaListenerContainerFactory (ConcurrentKafkaListenerContainerFactory<String, SettleTriggerEvent> with AckMode.RECORD). Consumer props identical to the analog: BOOTSTRAP_SERVERS from ${spring.kafka.bootstrap-servers:localhost:9092}, GROUP_ID from ${inventory.settlement.consumer.group-id:inventory-settlement}, AUTO_OFFSET_RESET=earliest, ENABLE_AUTO_COMMIT=false, key=StringDeserializer, value=ErrorHandlingDeserializer wrapping JacksonJsonDeserializer, JacksonJsonDeserializer.TRUSTED_PACKAGES=com.example.feat1.DDD.inventory_context.application.event, VALUE_DEFAULT_TYPE=SettleTriggerEvent.class.getName(), USE_TYPE_INFO_HEADERS=false. Error handler: DeadLetterPublishingRecoverer over the REUSED inventoryDltKafkaTemplate (inject by @Qualifier("inventoryDltKafkaTemplate") KafkaTemplate<String,Object>), FixedBackOff(1000L, 3L), handler.addNotRetryableExceptions(DeserializationException.class) ONLY — do NOT add the missing-reservation exception to non-retryable (RESEARCH Anti-Pattern: transient ordering races must self-heal via retry before DLT).
    Modify InventoryKafkaTopicConfig: add a constructor-injected settleTriggerTopic field from @Value("${kitchen.events.settle-trigger-topic:kitchen.settlement-trigger}") and two @Bean NewTopic methods — settleTriggerTopic() and settleTriggerDltTopic() — using TopicBuilder.name(...).partitions(1).replicas(1).build(); the DLT name MUST be settleTriggerTopic + DLT_SUFFIX (never a hardcoded divergent literal).
    Create SettleTriggerKafkaConsumerConfigTest mirroring InventoryKafkaConsumerConfigTest exactly (broker-free, instantiate the config directly): assert the consumer factory config map has ErrorHandlingDeserializer as value deserializer, VALUE_DEFAULT_TYPE = SettleTriggerEvent class name, USE_TYPE_INFO_HEADERS=false, TRUSTED_PACKAGES set; and that the error handler treats DeserializationException as non-retryable.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=SettleTriggerKafkaConsumerConfigTest test</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -q -Dtest=SettleTriggerKafkaConsumerConfigTest test` exits 0 (D-01 wiring hardening verified broker-free).
    - Config sets USE_TYPE_INFO_HEADERS=false + VALUE_DEFAULT_TYPE=SettleTriggerEvent + TRUSTED_PACKAGES (type-confusion mitigation): grep confirms all three in SettleTriggerKafkaConsumerConfig.
    - DLT topic derives from the live-topic property + DLT_SUFFIX: `grep -q "DLT_SUFFIX" src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/config/InventoryKafkaTopicConfig.java` and a settleTriggerDltTopic bean exists.
    - Only DeserializationException is non-retryable: `grep -q "addNotRetryableExceptions(DeserializationException.class)" ...SettleTriggerKafkaConsumerConfig.java` and no missing-reservation exception added there (D-05).
  </acceptance_criteria>
  <done>Typed SettleTriggerEvent consumer factory/error handler/container factory + settle-trigger and DLT topics exist and pass a broker-free config test.</done>
</task>

<task type="auto">
  <name>Task 2: SettleTriggerListener + serde round-trip case</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/SettleTriggerListener.java,
    src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/OrderCreatedListener.java (thin @KafkaListener delegate analog)
    - src/test/java/com/example/feat1/DDD/order_context/EventSerdeRoundTripTest.java (the file being modified; existing OrderCreatedEvent/OrderStockResultEvent round-trip cases)
    - src/main/java/com/example/feat1/DDD/inventory_context/application/event/SettleTriggerEvent.java (event to round-trip)
  </read_first>
  <action>
    Create SettleTriggerListener as a @Component (@RequiredArgsConstructor) in ...inventory_context.infrastructure.adapter, injecting InventoryReservationSettlementService. Add a single @KafkaListener method onSettleTrigger(SettleTriggerEvent event) with topics = "${kitchen.events.settle-trigger-topic:kitchen.settlement-trigger}", groupId = "${inventory.settlement.consumer.group-id:inventory-settlement}", containerFactory = "settleTriggerKafkaListenerContainerFactory". Body: delegate to settlementService.onSettleTrigger(event) and nothing else — zero business logic in the listener (mirror OrderCreatedListener).
    Extend EventSerdeRoundTripTest with a case that serializes a SettleTriggerEvent (with a non-null Instant occurredAt) via the Jackson-3 JacksonJsonSerializer/JacksonJsonDeserializer pair used by the existing cases and asserts the deserialized record equals the original — proving Jackson-3 handles the Instant and the record shape (D-01). Reuse the existing test's serializer/deserializer setup and trusted-packages configuration.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=EventSerdeRoundTripTest test</automated>
  </verify>
  <acceptance_criteria>
    - SettleTriggerListener delegates only: `grep -q "settlementService.onSettleTrigger" src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/adapter/SettleTriggerListener.java` and the method body has no other logic (D-01).
    - Listener references the locked topic/group property keys (kitchen.events.settle-trigger-topic, inventory.settlement.consumer.group-id) and containerFactory settleTriggerKafkaListenerContainerFactory.
    - `./mvnw -q -Dtest=EventSerdeRoundTripTest test` exits 0 with the SettleTriggerEvent round-trip case green (D-01).
    - `./mvnw -q test` full suite green.
  </acceptance_criteria>
  <done>Thin SettleTriggerListener wired to the settlement service; SettleTriggerEvent round-trips through Jackson-3 serde; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka topic (external/untrusted bytes) → consumer deserialization | The settle-trigger payload crosses from the broker into the JVM. This is the transport trust boundary; hardening (type-confusion, poison-pill) is enforced here. No HTTP endpoint exists in this phase — no authorization threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Tampering/Spoofing (type confusion) | JacksonJsonDeserializer | mitigate | USE_TYPE_INFO_HEADERS=false + fixed VALUE_DEFAULT_TYPE + TRUSTED_PACKAGES allow-list (ASVS V5, mirrors Phase 15) — D-01 |
| T-16-13 | Denial of Service (poison pill) | ErrorHandlingDeserializer | mitigate | Malformed payload wrapped as non-retryable DeserializationException → routed straight to the DLT by DeadLetterPublishingRecoverer (D-01/D-05) |
| T-16-14 | Denial of Service (partition block on missing reservation) | DefaultErrorHandler retry policy | accept (retry→DLT) | FixedBackOff(1000,3) then DLT; missing reservation intentionally left retryable so transient ordering races self-heal (RESEARCH Anti-Pattern) — D-05 |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No dependency added (RESEARCH Package Legitimacy: N/A) |
</threat_model>

<verification>
- SettleTriggerKafkaConsumerConfigTest green (broker-free wiring/hardening).
- EventSerdeRoundTripTest green including the SettleTriggerEvent case.
- Full suite `./mvnw -q test` green — phase gate.
</verification>

<success_criteria>
- Settle-trigger consumer is wired with the same hardening as Phase 15 (ErrorHandlingDeserializer + trusted packages + DLT), a thin listener delegating to the settlement service, and settle-trigger/DLT topics.
- SettleTriggerEvent round-trips through Jackson-3 serde.
</success_criteria>

<output>
Create `.planning/phases/16-inventory-reservation-settlement/16-05-SUMMARY.md` when done.
</output>
