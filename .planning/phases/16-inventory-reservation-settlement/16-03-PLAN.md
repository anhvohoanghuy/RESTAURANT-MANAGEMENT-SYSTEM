---
phase: 16-inventory-reservation-settlement
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D-02]
files_modified:
  - src/main/java/com/example/feat1/DDD/inventory_context/domain/service/RecipeRequirementResolver.java
  - src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationService.java

must_haves:
  truths:
    - "Per-target (DISH/TOPPING_OPTION) recipe-to-ingredient resolution lives in ONE shared class, not duplicated [D-02]"
    - "InventoryReservationService (Phase 15 whole-order path) resolves recipe via the extracted resolver [D-02]"
    - "The settlement service (Plan 04) can resolve a single line's recipe via the same resolver [D-02]"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/domain/service/RecipeRequirementResolver.java"
      provides: "Shared recipe→ingredient base-quantity resolver [D-02]"
      contains: "accumulate"
  key_links:
    - from: "InventoryReservationService.computeRequired"
      to: "RecipeRequirementResolver.accumulate"
      via: "constructor-injected resolver"
      pattern: "recipeRequirementResolver|RecipeRequirementResolver"
---

<objective>
Mechanically extract the private recipe-resolution logic (accumulateRecipe / computeRequired, InventoryReservationService lines ~165-222) into a new shared RecipeRequirementResolver domain service, and refactor InventoryReservationService to call it. This is the structural meaning of D-02's "reuse the exact Phase 15 recipe-resolution path" — so Plan 04's single-line settlement calls the SAME code, not a copy.

Purpose: Prevent recipe-resolution drift between the reservation and settlement paths (RESEARCH Pattern 1 / Don't-Hand-Roll).
Output: RecipeRequirementResolver + a refactored InventoryReservationService that behaves identically (Phase 15 tests stay green).
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-inventory-reservation-settlement/16-CONTEXT.md
@.planning/phases/16-inventory-reservation-settlement/16-RESEARCH.md
@.planning/phases/16-inventory-reservation-settlement/16-PATTERNS.md

<interfaces>
<!-- Extraction source. -->
Source methods (verbatim body to move): src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationService.java — computeRequired (iterates event lines) + accumulateRecipe (per-target resolution: menuRecipeCostingPort.findRecipe → ingredientRepository.findById → UnitConverter.convert → merge). Full body reproduced at 16-PATTERNS.md lines 350-395. Collaborators already injected in InventoryReservationService: MenuRecipeCostingPort menuRecipeCostingPort, IngredientRepository ingredientRepository.
Regression harness: src/test/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationServiceTest.java (must stay green — proves the extraction is behavior-preserving).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract RecipeRequirementResolver as a shared domain service</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/domain/service/RecipeRequirementResolver.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationService.java (extraction source: accumulateRecipe/computeRequired at lines 165-222; collaborators + scale/UnitConverter usage)
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/service/UnitConverter.java (existing static converter used inside accumulateRecipe)
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/snapshot/RecipeCostingSnapshot.java (recipe line shape returned by MenuRecipeCostingPort)
  </read_first>
  <action>
    Create RecipeRequirementResolver in package ...inventory_context.domain.service as a @RequiredArgsConstructor class (or @Service — match the domain-service convention; it is a stateless collaborator) constructor-injecting MenuRecipeCostingPort menuRecipeCostingPort and IngredientRepository ingredientRepository. Move the body of accumulateRecipe VERBATIM into a public method void accumulate(Map<UUID, BigDecimal> required, RecipeTargetType targetType, UUID targetId, int quantity) — same null guards, same menuRecipeCostingPort.findRecipe / ingredientRepository.findById / UnitConverter.convert(base-unit) logic, same required.merge(ingredientId, contribution, BigDecimal::add), same debug logging. Keep it a mechanical extract-method: do NOT change conversion semantics, scaling, or logging. Add a convenience method Map<UUID,BigDecimal> resolveForTarget(RecipeTargetType, UUID, int) returning a LinkedHashMap for single-target callers if helpful, but the core reusable unit is accumulate(...). Do NOT touch the WR-01/WR-02-flagged idempotency/movement code in InventoryReservationService in this task.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=InventoryReservationServiceTest test</automated>
  </verify>
  <acceptance_criteria>
    - RecipeRequirementResolver exposes a public accumulate(Map,RecipeTargetType,UUID,int) with the same body as the original accumulateRecipe (D-02).
    - The resolver injects only MenuRecipeCostingPort + IngredientRepository (no reservation/balance deps).
    - Project compiles: `./mvnw -q -Dtest=InventoryReservationServiceTest test` exits 0.
  </acceptance_criteria>
  <done>RecipeRequirementResolver exists with the extracted per-target resolution logic and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor InventoryReservationService to delegate to the resolver</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationService.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationService.java (the file being modified; computeRequired at 165-182 iterates event.lines() calling accumulateRecipe for DISH + each TOPPING_OPTION)
    - src/test/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationServiceTest.java (mock wiring — the refactor must keep these mocks/assertions valid or update them minimally to inject the resolver)
  </read_first>
  <action>
    Constructor-inject RecipeRequirementResolver into InventoryReservationService (add a private final field; @RequiredArgsConstructor already generates the constructor). Change computeRequired to iterate event.lines() and, for each line, call recipeRequirementResolver.accumulate(required, RecipeTargetType.DISH, line.dishId(), quantity) and for each selected topping recipeRequirementResolver.accumulate(required, RecipeTargetType.TOPPING_OPTION, topping.toppingOptionId(), quantity). DELETE the now-unmoved private accumulateRecipe method (its body now lives in the resolver). Remove the menuRecipeCostingPort / ingredientRepository fields ONLY if they become unused after the move; if other code in the class still uses them, keep them. Behavior must be identical — this is a pure refactor. If InventoryReservationServiceTest constructs the service directly, add the resolver mock (or a real resolver wired with the existing menu/ingredient mocks) so the test still passes. Do not alter the idempotency (WR-01) or reserve-loop code beyond wiring the resolver.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=InventoryReservationServiceTest test</automated>
  </verify>
  <acceptance_criteria>
    - computeRequired delegates to recipeRequirementResolver.accumulate (no inline recipe resolution remains): `grep -q "recipeRequirementResolver" src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationService.java` and the private accumulateRecipe method is gone.
    - `./mvnw -q -Dtest=InventoryReservationServiceTest test` exits 0 — Phase 15 reservation behavior is preserved (D-02, behavior-preserving refactor).
    - `./mvnw -q test` full suite green (no cross-context regression).
  </acceptance_criteria>
  <done>InventoryReservationService resolves recipe via the shared resolver; all Phase 15 tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (internal refactor) | No new external input path. No HTTP endpoint in this phase — no authorization threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering (of business state via logic drift) | RecipeRequirementResolver | mitigate | Single shared resolver prevents reservation/settlement recipe divergence that could over/under-deduct stock; extraction is verbatim and guarded by the unchanged Phase 15 test (D-02) |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No dependency added (RESEARCH Package Legitimacy: N/A) |
</threat_model>

<verification>
- Phase 15 reservation tests green (behavior-preserving extraction).
- Full suite `./mvnw -q test` green.
</verification>

<success_criteria>
- Recipe resolution exists once, in RecipeRequirementResolver.
- InventoryReservationService delegates to it with no behavior change.
</success_criteria>

<output>
Create `.planning/phases/16-inventory-reservation-settlement/16-03-SUMMARY.md` when done.
</output>
