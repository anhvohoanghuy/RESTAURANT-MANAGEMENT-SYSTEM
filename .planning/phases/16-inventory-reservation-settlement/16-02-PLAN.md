---
phase: 16-inventory-reservation-settlement
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D-02]
files_modified:
  - src/main/java/com/example/feat1/DDD/inventory_context/domain/port/OrderLineLookupPort.java
  - src/main/java/com/example/feat1/DDD/inventory_context/domain/snapshot/OrderLineRecipeSnapshot.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderLineRepository.java
  - src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapter.java
  - src/test/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapterTest.java

must_haves:
  truths:
    - "Inventory can read one OrderLine's (dishId, quantity, selectedToppingOptionIds) by (orderId, orderLineId) without touching order_context JPA directly [D-02]"
    - "Lookup returns a narrow OrderLineRecipeSnapshot, not the full OrderLineEntity graph [D-02]"
    - "Lookup keyed by BOTH orderId and orderLineId (defense against cross-order id collision) [D-02]"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/domain/port/OrderLineLookupPort.java"
      provides: "Cross-context read port defined in the consuming context [D-02]"
      contains: "interface OrderLineLookupPort"
    - path: "src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapter.java"
      provides: "Adapter implementing the port in the owning context [D-02]"
      contains: "implements OrderLineLookupPort"
  key_links:
    - from: "OrderLineLookupAdapter"
      to: "OrderLineRepository.findByOrder_IdAndId"
      via: "constructor-injected repository"
      pattern: "findByOrder_IdAndId"
    - from: "OrderLineLookupAdapter"
      to: "OrderLineRecipeSnapshot"
      via: "map(line -> new OrderLineRecipeSnapshot(...))"
      pattern: "OrderLineRecipeSnapshot"
---

<objective>
Add the cross-context read path that lets inventory_context re-resolve one order line's recipe by (orderId, orderLineId). Mirrors the existing MenuRecipeCostingPort/Adapter convention: the CONSUMING context (inventory) defines the port + snapshot, the OWNING context (order) implements the adapter over a new JPA repository.

Purpose: D-02 requires inventory to re-resolve recipe itself from a thin event; there is no repository to read a single OrderLine by id today (only via OrderEntity.lines).
Output: OrderLineLookupPort, OrderLineRecipeSnapshot, OrderLineRepository, OrderLineLookupAdapter + adapter test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-inventory-reservation-settlement/16-CONTEXT.md
@.planning/phases/16-inventory-reservation-settlement/16-RESEARCH.md
@.planning/phases/16-inventory-reservation-settlement/16-PATTERNS.md

<interfaces>
<!-- Analogs to copy shape from — no exploration needed. -->
Port analog: src/main/java/com/example/feat1/DDD/inventory_context/domain/port/MenuRecipeCostingPort.java (narrow interface, Optional return).
Snapshot analog: src/main/java/com/example/feat1/DDD/inventory_context/domain/snapshot/RecipeCostingSnapshot.java (record-style snapshot).
Adapter analog: src/main/java/com/example/feat1/DDD/menu_context/infrastructure/adapter/MenuRecipeCostingAdapter.java (@Component @RequiredArgsConstructor, @Transactional(readOnly=true), repository -> map -> snapshot).
Entity fields: src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderLineEntity.java (id, dishId, quantity, selectedToppings:List<OrderLineToppingSnapshot> @ElementCollection EAGER) and OrderLineToppingSnapshot.java (getToppingOptionId()). Per 16-PATTERNS.md lines 283-343.
Test style: src/test/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationServiceTest.java (Mockito mock-the-repository + AssertJ assertions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define OrderLineLookupPort and OrderLineRecipeSnapshot (inventory side)</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/domain/port/OrderLineLookupPort.java,
    src/main/java/com/example/feat1/DDD/inventory_context/domain/snapshot/OrderLineRecipeSnapshot.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/port/MenuRecipeCostingPort.java (port convention — 16-PATTERNS.md lines 251-266)
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/snapshot/RecipeCostingSnapshot.java (snapshot record convention)
  </read_first>
  <action>
    Create OrderLineRecipeSnapshot as a record in package ...inventory_context.domain.snapshot with components: UUID orderLineId, UUID dishId, int quantity, List<UUID> selectedToppingOptionIds. Keep it narrow (RESEARCH Information-Disclosure mitigation: expose only recipe-relevant fields, never the full order graph).
    Create OrderLineLookupPort as an interface in package ...inventory_context.domain.port with a single method: Optional<OrderLineRecipeSnapshot> findLine(UUID orderId, UUID orderLineId). Convention: the consuming context (inventory) owns this port + snapshot; the adapter lives in order_context (Task 2), mirroring MenuRecipeCostingPort/Adapter.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=InventoryReservationServiceTest test</automated>
  </verify>
  <acceptance_criteria>
    - OrderLineLookupPort.findLine takes both orderId and orderLineId and returns Optional<OrderLineRecipeSnapshot> (D-02).
    - OrderLineRecipeSnapshot exposes only orderLineId, dishId, quantity, selectedToppingOptionIds (no full-entity leak).
    - Compiles: `./mvnw -q -Dtest=InventoryReservationServiceTest test` exits 0.
  </acceptance_criteria>
  <done>Port interface + narrow snapshot record exist in inventory_context and compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OrderLineRepository + OrderLineLookupAdapter (order side) with test</name>
  <files>
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderLineRepository.java,
    src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapter.java,
    src/test/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapterTest.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/menu_context/infrastructure/adapter/MenuRecipeCostingAdapter.java (adapter shape — 16-PATTERNS.md lines 285-326)
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderLineEntity.java (fields: id, dishId, quantity, selectedToppings — 16-PATTERNS.md line 330)
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/entity/OrderLineToppingSnapshot.java (getToppingOptionId())
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderCartLineRepository.java (sibling repository query-method style)
    - src/test/java/com/example/feat1/DDD/inventory_context/application/InventoryReservationServiceTest.java (Mockito test style)
  </read_first>
  <behavior>
    - findLine returns a populated OrderLineRecipeSnapshot when the repository finds a line: dishId, quantity, and the mapped toppingOptionId list all match the entity.
    - findLine returns Optional.empty() when the repository returns empty (line not found).
    - The adapter delegates to findByOrder_IdAndId(orderId, orderLineId) — passing BOTH ids (defense-in-depth against a cross-order id collision).
  </behavior>
  <action>
    Create OrderLineRepository in package ...order_context.infrastructure.repository extending JpaRepository<OrderLineEntity, UUID> with Optional<OrderLineEntity> findByOrder_IdAndId(UUID orderId, UUID id). (No such repository exists today — OrderLineEntity was only reachable via OrderEntity.lines.)
    Create OrderLineLookupAdapter in package ...order_context.infrastructure.adapter: @Component @RequiredArgsConstructor implements OrderLineLookupPort; constructor-inject OrderLineRepository. Implement findLine as @Transactional(readOnly = true): call orderLineRepository.findByOrder_IdAndId(orderId, orderLineId), map the entity to OrderLineRecipeSnapshot(line.getId(), line.getDishId(), line.getQuantity(), line.getSelectedToppings().stream().map(OrderLineToppingSnapshot::getToppingOptionId).toList()). Do not modify OrderLineEntity.
    Write OrderLineLookupAdapterTest using Mockito (mock OrderLineRepository) mirroring InventoryReservationServiceTest style — assert the mapping and the empty case per the behavior block. No new test framework; JUnit5+Mockito+AssertJ already configured.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=OrderLineLookupAdapterTest test</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -q -Dtest=OrderLineLookupAdapterTest test` exits 0 with both behavior cases (mapping + empty) green (D-02).
    - Adapter uses findByOrder_IdAndId (both ids): `grep -q findByOrder_IdAndId src/main/java/com/example/feat1/DDD/order_context/infrastructure/adapter/OrderLineLookupAdapter.java`.
    - Adapter method is @Transactional(readOnly = true) and returns the narrow snapshot.
  </acceptance_criteria>
  <done>OrderLineRepository + OrderLineLookupAdapter compile; adapter test green; inventory can read a single order line as a narrow snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| order_context → inventory_context (in-process cross-context read) | Inventory reads order data via a port/adapter (anti-corruption layer), not direct JPA. Same process, same trust domain. No HTTP endpoint in this phase — no authorization threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | OrderLineLookupAdapter return shape | mitigate | Return a narrow OrderLineRecipeSnapshot (dishId, quantity, toppingOptionIds only), never the full OrderLineEntity/OrderEntity graph (RESEARCH Security Domain) |
| T-16-04 | Spoofing/Tampering | cross-order id collision | mitigate | findByOrder_IdAndId keys on BOTH orderId and orderLineId, closing a theoretical cross-order data leak (RESEARCH Pattern 2) |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No dependency added (RESEARCH Package Legitimacy: N/A) |
</threat_model>

<verification>
- `./mvnw -q test` full suite green after this plan (no regression; purely additive cross-context read path).
</verification>

<success_criteria>
- inventory_context defines OrderLineLookupPort + OrderLineRecipeSnapshot.
- order_context implements OrderLineLookupAdapter over a new OrderLineRepository.
- Adapter maps to a narrow snapshot, keyed by (orderId, orderLineId), verified by a green unit test.
</success_criteria>

<output>
Create `.planning/phases/16-inventory-reservation-settlement/16-02-SUMMARY.md` when done.
</output>
