---
phase: 16-inventory-reservation-settlement
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D-01, D-04, D-05, D-06]
files_modified:
  - src/main/java/com/example/feat1/DDD/inventory_context/application/event/SettleTriggerEvent.java
  - src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/StockReservationEntity.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryLineSettlementEntity.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryLineSettlementRepository.java
  - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java

must_haves:
  truths:
    - "A SettleTriggerEvent record exists carrying exactly (eventId, eventType, occurredAt, orderId, orderLineId, totalLines) — no ingredient amounts [D-01]"
    - "InventoryMovementType has a CONSUMPTION value classified as outbound [D-06/WR-02]"
    - "StockReservationEntity.ReservationStatus has HELD and SETTLED [D-04]"
    - "A unique (order_id, order_line_id) settlement row can be inserted and counted per order [D-04/D-05]"
    - "StockReservationEntity can be locked by orderId under PESSIMISTIC_WRITE [D-04]"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/application/event/SettleTriggerEvent.java"
      provides: "Inbound settle-trigger event contract [D-01]"
      contains: "record SettleTriggerEvent"
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryLineSettlementEntity.java"
      provides: "Per-(orderId,orderLineId) idempotency guard + last-line counter [D-04/D-05]"
      contains: "uq_inventory_line_settlement"
    - path: "src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryLineSettlementRepository.java"
      provides: "existsByOrderIdAndOrderLineId + countByOrderId [D-04/D-05]"
      exports: ["existsByOrderIdAndOrderLineId", "countByOrderId"]
  key_links:
    - from: "InventoryMovementType.CONSUMPTION"
      to: "isOutbound()"
      via: "enum classifier"
      pattern: "isOutbound.*CONSUMPTION"
    - from: "StockReservationRepository.lockByOrderId"
      to: "PESSIMISTIC_WRITE"
      via: "@Lock query method"
      pattern: "PESSIMISTIC_WRITE"
---

<objective>
Build the additive data-layer foundation for inventory settlement: the inbound event contract, two enum extensions, the new per-line settlement entity + repository, and a pessimistic reservation lookup. All additive, no behavior change to existing Phase 15 code.

Purpose: Unblock the settlement service (Plan 04) and Kafka boundary (Plan 05) with the types, columns, and queries they consume.
Output: SettleTriggerEvent record, CONSUMPTION movement type, SETTLED reservation status, InventoryLineSettlementEntity + repository, StockReservationRepository.lockByOrderId.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-inventory-reservation-settlement/16-CONTEXT.md
@.planning/phases/16-inventory-reservation-settlement/16-RESEARCH.md
@.planning/phases/16-inventory-reservation-settlement/16-PATTERNS.md

<interfaces>
<!-- Analog files to copy shape from. Read directly; no exploration. -->
Event analog: src/main/java/com/example/feat1/DDD/order_context/application/event/OrderStockResultEvent.java (record + TYPE constant style).
Ledger-row analog: src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryProcessedEventEntity.java (@Entity + named @UniqueConstraint + snake_case @Column + Lombok @Getter/@Setter).
Lock-query analog: src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryStockBalanceRepository.java (lockByIngredientAndLocation: @Lock(PESSIMISTIC_WRITE) + @Query).
Repo query-method analog: src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryProcessedEventRepository.java.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settle-trigger event contract and enum extensions</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/application/event/SettleTriggerEvent.java,
    src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/StockReservationEntity.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/application/event/OrderStockResultEvent.java (event record + *_TYPE constant convention — analog per 16-PATTERNS.md lines 38-85)
    - src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java (the file being modified; current enum + isOutbound at 16-PATTERNS.md lines 94-131)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/StockReservationEntity.java (the file being modified; ReservationStatus enum at lines 44-46, per 16-PATTERNS.md lines 137-149)
  </read_first>
  <action>
    Create SettleTriggerEvent as a Java record in package com.example.feat1.DDD.inventory_context.application.event with EXACTLY these components in order: UUID eventId, String eventType, Instant occurredAt, UUID orderId, UUID orderLineId, int totalLines. Add a public static final String TYPE = "SettleTrigger". Do NOT add any ingredient/dish/topping fields — D-01 and RESEARCH Pitfall 6 forbid carrying recipe data on the event (inventory re-resolves it itself). Mirror OrderStockResultEvent's package/import/record style.
    In InventoryMovementType add a CONSUMPTION enum constant (append after STOCK_COUNT) and include CONSUMPTION in the isOutbound() predicate alongside ADJUSTMENT_OUT and WASTE; leave isInbound()/isCount() unchanged.
    In StockReservationEntity change the ReservationStatus enum from { HELD } to { HELD, SETTLED }. Make NO other change to this entity — do not attempt to derive last-line completion from its per-ingredient lines (RESEARCH Anti-Pattern).
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=EventSerdeRoundTripTest test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "int totalLines" src/main/java/com/example/feat1/DDD/inventory_context/application/event/SettleTriggerEvent.java` and the record has no BigDecimal/ingredient field (D-01).
    - `grep -v '^ *//' src/main/java/com/example/feat1/DDD/inventory_context/domain/model/InventoryMovementType.java | grep -c CONSUMPTION` is >= 2 (declared + in isOutbound) (D-06).
    - StockReservationEntity ReservationStatus contains both HELD and SETTLED (D-04).
    - `./mvnw -q -Dtest=EventSerdeRoundTripTest test` exits 0 (existing suite still compiles/green after enum additions).
  </acceptance_criteria>
  <done>SettleTriggerEvent record + TYPE constant exist; InventoryMovementType.CONSUMPTION is outbound; ReservationStatus = { HELD, SETTLED }; project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-line settlement entity/repository and reservation pessimistic lookup</name>
  <files>
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryLineSettlementEntity.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryLineSettlementRepository.java,
    src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java
  </files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryProcessedEventEntity.java (entity + named @UniqueConstraint analog — 16-PATTERNS.md lines 155-201)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/StockReservationRepository.java (the file being modified; current methods shown above)
    - src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/repository/InventoryStockBalanceRepository.java (lockByIngredientAndLocation PESSIMISTIC_WRITE @Query analog — 16-PATTERNS.md lines 227-244)
  </read_first>
  <action>
    Create InventoryLineSettlementEntity in package ...inventory_context.infrastructure.entity: @Getter @Setter @Entity, @Table(name = "inventory_line_settlements", uniqueConstraints = @UniqueConstraint(name = "uq_inventory_line_settlement", columnNames = {"order_id", "order_line_id"})). Fields: UUID id (@Id @GeneratedValue(strategy = GenerationType.UUID)); UUID orderId (@Column(name = "order_id", nullable = false)); UUID orderLineId (@Column(name = "order_line_id", nullable = false)); Instant settledAt (@Column(name = "settled_at", nullable = false)). This ONE table serves BOTH the D-05 idempotency guard and the D-04 last-line counter — do not split into two tables.
    Create InventoryLineSettlementRepository extending JpaRepository<InventoryLineSettlementEntity, UUID> with derived queries: boolean existsByOrderIdAndOrderLineId(UUID orderId, UUID orderLineId) and long countByOrderId(UUID orderId).
    Modify StockReservationRepository: add Optional<StockReservationEntity> lockByOrderId(UUID orderId) annotated @Lock(LockModeType.PESSIMISTIC_WRITE) with @Query("select r from StockReservationEntity r where r.orderId = :orderId"). Add imports jakarta.persistence.LockModeType, org.springframework.data.jpa.repository.Lock, org.springframework.data.jpa.repository.Query. Keep existsByOrderId/findByOrderId unchanged.
  </action>
  <verify>
    <automated>./mvnw -q -Dtest=InventoryStockIntegrationTest test</automated>
  </verify>
  <acceptance_criteria>
    - InventoryLineSettlementEntity declares a named unique constraint on (order_id, order_line_id): `grep -q "uq_inventory_line_settlement" src/main/java/com/example/feat1/DDD/inventory_context/infrastructure/entity/InventoryLineSettlementEntity.java` (D-04/D-05).
    - Repository exposes both existsByOrderIdAndOrderLineId and countByOrderId (D-04/D-05).
    - StockReservationRepository.lockByOrderId is annotated PESSIMISTIC_WRITE (D-04): `grep -A2 lockByOrderId src/main/java/.../StockReservationRepository.java | grep -q PESSIMISTIC_WRITE`.
    - `./mvnw -q -Dtest=InventoryStockIntegrationTest test` exits 0 (Spring context loads with the new @Entity mapped, schema created).
  </acceptance_criteria>
  <done>New settlement entity + repository compile and map to a schema; lockByOrderId acquires a PESSIMISTIC_WRITE lock; Spring context boots green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (data layer only) | This plan adds types/columns/queries; no external input crosses a boundary here. No HTTP endpoint exists in this phase (staff endpoint is Phase 17) — no authorization threat applies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | InventoryLineSettlementEntity | mitigate | Named DB unique constraint on (order_id, order_line_id) is the durable per-line double-settlement guard consumed in Plan 04 (D-05) |
| T-16-02 | Denial of Service | lockByOrderId PESSIMISTIC_WRITE | accept | Single-row lock, acquired first in a fixed order in Plan 04; deadlock avoidance is enforced by lock ordering in Plan 04, not here |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No dependency added this plan (RESEARCH Package Legitimacy: N/A) — nothing to install |
</threat_model>

<verification>
- `./mvnw -q test` full suite green after both tasks (no regression from enum/entity additions).
- New entity mapped without breaking Hibernate schema generation (context-load test passes).
</verification>

<success_criteria>
- SettleTriggerEvent carries only (eventId, eventType, occurredAt, orderId, orderLineId, totalLines).
- CONSUMPTION is a valid outbound InventoryMovementType.
- ReservationStatus supports SETTLED.
- InventoryLineSettlementEntity/Repository and StockReservationRepository.lockByOrderId exist and compile.
</success_criteria>

<output>
Create `.planning/phases/16-inventory-reservation-settlement/16-01-SUMMARY.md` when done.
</output>
