---
phase: 20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - admin-ui/src/views/InventoryView.vue
autonomous: true
requirements: [D-01, COST-HISTORY]
must_haves:
  truths:
    - "Editing an ingredient opens the shared modal pre-filled (D-01); submit routes to PUT for edits, POST for creates"
    - "Each ingredient row exposes a 'View costs' action opening a read-only cost-history modal listing IngredientCostResponse[]"
    - "No Inventory control is role-gated — Inventory stays visible to STAFF (UI-SPEC §6)"
  artifacts:
    - path: "admin-ui/src/views/InventoryView.vue"
      provides: "Create+edit ingredient modal + read-only cost-history modal"
      contains: "editingIngredientId"
  key_links:
    - from: "admin-ui/src/views/InventoryView.vue submitIngredient"
      to: "inventoryApi.updateIngredient / inventoryApi.createIngredient"
      via: "branch on editingIngredientId"
      pattern: "inventoryApi\\.updateIngredient"
    - from: "admin-ui/src/views/InventoryView.vue cost-history modal"
      to: "inventoryApi.listCosts"
      via: "fetch on open"
      pattern: "inventoryApi\\.listCosts"
---

<objective>
Extend `InventoryView.vue`: (1) generalize the create-only ingredient modal into a shared create+edit modal per D-01, and (2) wire the existing-but-uncalled `listCosts` binding into a read-only "View costs" cost-history modal (RESEARCH Open Question 1). Inventory is ADMIN+STAFF per UI-SPEC §6 — no role gating in this view.

Purpose: Close the ingredient-edit gap and surface ingredient cost history that Phase 19 left unwired.
Output: Editable ingredients + a read-only cost-history modal, visible to both ADMIN and STAFF.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-UI-SPEC.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-PATTERNS.md

<interfaces>
<!-- Bindings from 20-01 (updateIngredient) + existing. Consume directly — do not touch modules.ts. -->
From admin-ui/src/api/modules.ts:
- `inventoryApi.updateIngredient(id, body: IngredientRequest)` (new in 20-01)
- `inventoryApi.createIngredient`, `inventoryApi.addCost`, `inventoryApi.listCosts(ingredientId)` (existing; listCosts previously uncalled)
- Types: IngredientRequest = { name; baseUnit; description?; status? }, IngredientResponse, `IngredientCostResponse = { ... unitCost, costUnit, effectiveAt/source/note/createdAt per InventoryDtos }`
Note: Inventory is ADMIN+STAFF — do NOT import or apply `isAdmin` here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize the ingredient modal to create+edit</name>
  <files>admin-ui/src/views/InventoryView.vue</files>
  <read_first>
    - admin-ui/src/views/InventoryView.vue (openIngredientModal line 88 / submitIngredient createIngredient line 101; "New ingredient" button line 262; ingredient row table-actions; messageOf usage)
    - .planning/phases/20-.../20-PATTERNS.md (§"admin-ui/src/views/InventoryView.vue — EXTEND" — same mechanical create→edit; explicit "No role gating needed")
    - .planning/phases/20-.../20-RESEARCH.md (§"Pattern 1: Shared create+edit modal")
    - .planning/phases/20-.../20-UI-SPEC.md (§1 modal contract; §6 — Inventory visible to ADMIN+STAFF, do NOT hide)
  </read_first>
  <action>
    Add `editingIngredientId = ref<string|null>(null)`; change `openIngredientModal(row?: IngredientResponse)` to seed name/baseUnit/description/status from `row` when present (else reset), setting the editing id; branch `submitIngredient` on the editing id to call `inventoryApi.updateIngredient(id, payload)` vs `inventoryApi.createIngredient(payload)`; on success reset editing id, close, `loadStock()`. Make the modal `:title` computed 'Edit ingredient'/'New ingredient'. Keep "Save changes" label in both modes. Add an "Edit" `ghost-button small` to the ingredient/stock row, positioned before the existing "Add cost" button, calling `openIngredientModal(row)`. Use `messageOf` in catches. Do NOT wrap any control in `v-if="isAdmin"` — Inventory is ADMIN+STAFF.
  </action>
  <acceptance_criteria>
    - InventoryView.vue contains `editingIngredientId`.
    - `submitIngredient` references `inventoryApi.updateIngredient`.
    - No `isAdmin`/`v-if="isAdmin"` appears in InventoryView.vue.
    - `npm --prefix admin-ui run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build</automated>
  </verify>
  <done>Ingredient modal does create AND edit via pre-fill; no role gating added.</done>
</task>

<task type="auto">
  <name>Task 2: Add a read-only "View costs" cost-history modal wiring listCosts</name>
  <files>admin-ui/src/views/InventoryView.vue</files>
  <read_first>
    - admin-ui/src/views/InventoryView.vue (openCostModal/costForm add-cost pattern lines 205-235 for the trigger-button shape; DataTable import line 4 + read-only column usage; "Add cost" button line 285)
    - .planning/phases/20-.../20-PATTERNS.md (§"Cost-history view (Open Question 1)" — "View costs" ghost-button + read-only Modal+DataTable; §"Shared Patterns" loading/error/empty)
    - .planning/phases/20-.../20-RESEARCH.md (§"Open Questions" Q1 — recommended shape; columns: unit cost, cost unit, effective at, source, note, created at)
    - .planning/phases/20-.../20-UI-SPEC.md (§"Copywriting Contract" error state; no v-html)
  </read_first>
  <action>
    Add a read-only cost-history modal distinct from the add-cost modal. State: `costHistoryModalOpen`, `costHistoryTargetName`, `costHistoryLoading`, `costHistoryError`, `costHistory = ref<IngredientCostResponse[]>([])`. Add a "View costs" `ghost-button small` next to the existing "Add cost" button (line 285) calling an `openCostHistory(row)` that sets the target name, opens the modal, and fetches `inventoryApi.listCosts(row.ingredientId)` into `costHistory` (loading/error handled with the standard three-way convention: `EmptyState tone="error"` + Retry on failure, `.skeleton-table` while loading, `DataTable` otherwise). Modal titled "Cost history — {ingredientName}" renders a read-only `DataTable` (no form, no submit) with columns Unit cost (`formatMoney`), Cost unit, Effective at (`formatDateTime`), Source, Note, Created at (`formatDateTime`) — column keys mapping to IngredientCostResponse fields. All cells via `{{ }}` interpolation (no `v-html`). Use `messageOf` for the error. This is not role-gated.
  </action>
  <acceptance_criteria>
    - InventoryView.vue references `inventoryApi.listCosts`.
    - A cost-history modal exists separate from the add-cost modal (distinct open state).
    - Cost-history render uses the error/loading/DataTable convention and no `v-html`.
    - `npm --prefix admin-ui run build` exits 0 and `npm --prefix admin-ui run test` green.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build && npm --prefix admin-ui run test</automated>
  </verify>
  <done>Ingredient cost history is viewable via a read-only modal wired to listCosts; visible to STAFF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InventoryView → inventoryApi → /admin/inventory/** (ADMIN+STAFF) | No client role gating; server enforces the ADMIN+STAFF matcher. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2004-01 | Information Disclosure (XSS) | cost-history source/note strings in DataTable | mitigate | Render all cost-history cells via `{{ }}` interpolation (auto-escaped); no `v-html`. |
| T-2004-02 | Elevation of Privilege | (none — Inventory is ADMIN+STAFF) | accept | No gating is required or added; correctly leaving Inventory visible to STAFF matches SecurityConfig's `/admin/inventory/**`→ADMIN+STAFF rule (UI-SPEC §6). |
</threat_model>

<verification>
- `npm --prefix admin-ui run build` clean; `npm --prefix admin-ui run test` green.
- Manual smoke: both ADMIN and STAFF can edit ingredients and open the cost-history modal.
</verification>

<success_criteria>
Ingredient create+edit via shared modal (D-01); read-only cost-history modal wired to listCosts; zero role gating in InventoryView (stays visible to STAFF).
</success_criteria>

<output>
Create `.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-04-SUMMARY.md` when done.
</output>
