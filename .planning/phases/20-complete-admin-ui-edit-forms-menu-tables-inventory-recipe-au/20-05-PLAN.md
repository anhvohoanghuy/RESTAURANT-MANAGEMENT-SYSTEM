---
phase: 20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au
plan: 05
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - admin-ui/src/lib/recipe.ts
  - admin-ui/src/lib/recipe.test.ts
  - admin-ui/src/views/RecipeView.vue
autonomous: true
requirements: [D-02, RECIPE]
must_haves:
  truths:
    - "A dedicated recipe-builder screen (D-02) lets a user add/remove ingredient rows (ingredient picker + quantity + unit) for a dish"
    - "The saved RecipeRequest payload sends BOTH ingredientId AND the resolved ingredient display-name for every line (RESEARCH trap)"
    - "sortOrder is auto-assigned by row index; unit auto-fills from the selected ingredient's baseUnit"
    - "After a successful save the recipe cost panel refreshes from GET /admin/menu/recipes/cost"
  artifacts:
    - path: "admin-ui/src/lib/recipe.ts"
      provides: "Pure toRecipeRequest(dishId, dishName, rows, ingredients) mapping resolving ingredient name from id"
      contains: "toRecipeRequest"
    - path: "admin-ui/src/lib/recipe.test.ts"
      provides: "RECIPE-01 coverage — payload includes both ingredientId and ingredient name"
      contains: "ingredient"
    - path: "admin-ui/src/views/RecipeView.vue"
      provides: "Repeatable-row recipe builder + cost panel (D-02 dedicated screen)"
      contains: "addRow"
  key_links:
    - from: "admin-ui/src/views/RecipeView.vue saveRecipe"
      to: "menuApi.upsertRecipe"
      via: "toRecipeRequest(...) payload"
      pattern: "menuApi\\.upsertRecipe"
    - from: "admin-ui/src/views/RecipeView.vue cost panel"
      to: "menuApi.recipeCost"
      via: "refresh after save"
      pattern: "menuApi\\.recipeCost"
    - from: "admin-ui/src/lib/recipe.ts toRecipeRequest line mapping"
      to: "RecipeLine.ingredient (display name)"
      via: "resolve from ingredients by ingredientId"
      pattern: "ingredient:"
---

<objective>
Build the D-02 dedicated recipe-builder screen — the one net-new design in this phase (a repeatable ingredient-row array, which has NO analog anywhere in `admin-ui/src`). Extract the row→payload transform into a pure, unit-tested `lib/recipe.ts` function first (contract-first), so the RecipeRequest.Line dual-field trap (both `ingredientId` AND `ingredient` name) is verified without mounting a component. Then build `RecipeView.vue` on that contract with a recipe cost panel.

Purpose: Recipe authoring is the deliberate D-02 exception to the shared modal (nested repeatable rows too complex for a modal).
Output: A routed recipe-builder view + pure transform util + its test.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-UI-SPEC.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-PATTERNS.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-RESEARCH.md

<interfaces>
<!-- Bindings + types from 20-01 (already merged). Consume directly — do not touch modules.ts. -->
From admin-ui/src/api/modules.ts:
- `menuApi.getRecipe(targetType, targetId)` → RecipeResponse (retyped in 20-01), `menuApi.upsertRecipe(body: RecipeRequest)`, `menuApi.recipeCost(targetType, targetId)` → RecipeCostResponse (new in 20-01)
- `inventoryApi.listIngredients()` → IngredientResponse[] (existing; each has ingredientId, name, baseUnit)
- Types: `RecipeLine = { ingredientId; ingredient; quantity; unit; sortOrder? }`, `RecipeRequest = { targetType: RecipeTargetType; targetId; name; lines: RecipeLine[] }`, RecipeResponse, RecipeCostResponse = { totalCost; fullyCosted; lines: RecipeCostLineResponse[] }
From admin-ui/src/lib/format.ts: `formatMoney`, `messageOf`
Route: this view mounts at `/menu/dishes/:dishId/recipe` (route added in 20-06); read `dishId` from `useRoute().params`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure toRecipeRequest transform + test (RECIPE-01)</name>
  <files>admin-ui/src/lib/recipe.ts, admin-ui/src/lib/recipe.test.ts</files>
  <read_first>
    - admin-ui/src/lib/format.ts (the small pure individually-exported-function shape to imitate)
    - .planning/phases/20-.../20-RESEARCH.md (§"Pattern 2: Recipe builder repeatable rows" — the row→payload mapping; §"Pitfall 2" — Line needs BOTH ingredientId AND the display-name ingredient, resolved from the loaded ingredient list)
    - .planning/phases/20-.../20-PATTERNS.md (§"admin-ui/src/lib/recipe.ts" — pure-function extraction rationale for testability)
  </read_first>
  <behavior>
    - toRecipeRequest('dish-1', 'Pho', [{ingredientId:'i-1',quantity:2,unit:'kg'}], [{ingredientId:'i-1',name:'Beef',baseUnit:'kg'}]) returns { targetType:'DISH', targetId:'dish-1', name:'Pho', lines:[{ ingredientId:'i-1', ingredient:'Beef', quantity:2, unit:'kg', sortOrder:0 }] }.
    - The resolved `ingredient` (display name) is looked up from the ingredients array by ingredientId — present on every line.
    - sortOrder is the row's zero-based index.
    - A row whose ingredientId is not found in the ingredients array resolves `ingredient` to '' (empty string, not undefined).
    - Multiple rows produce ascending sortOrder 0,1,2…
  </behavior>
  <action>
    Create `admin-ui/src/lib/recipe.ts` exporting a pure `toRecipeRequest(dishId: string, dishName: string, rows: RecipeRowForm[], ingredients: IngredientResponse[]): RecipeRequest` where `RecipeRowForm = { ingredientId: string; quantity: number; unit: string }`. For each row, resolve `ingredient` (display name) by finding the ingredient with matching `ingredientId` in `ingredients` (`?.name ?? ''`), and set `sortOrder` to the array index — producing `RecipeLine[]` where EVERY line carries both `ingredientId` and `ingredient` (the RESEARCH Pitfall 2 trap). Import RecipeRequest/RecipeLine/IngredientResponse types from `../api/modules`. Also export a `RecipeRowForm` type. Create `admin-ui/src/lib/recipe.test.ts` covering the five behaviors above with plain vitest assertions (no component mount).
  </action>
  <acceptance_criteria>
    - `admin-ui/src/lib/recipe.ts` exports `toRecipeRequest` and `RecipeRowForm`.
    - Every produced line object has both an `ingredientId` and an `ingredient` key.
    - `admin-ui/src/lib/recipe.test.ts` asserts the resolved display name and index-based sortOrder.
    - `npm --prefix admin-ui run test` exits 0 with the new recipe test file counted.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run test</automated>
  </verify>
  <done>Pure transform resolves ingredient name from id and auto-indexes sortOrder; RECIPE-01 covered without a live backend or mounted component.</done>
</task>

<task type="auto">
  <name>Task 2: Build RecipeView.vue repeatable-row builder + cost panel</name>
  <files>admin-ui/src/views/RecipeView.vue</files>
  <read_first>
    - admin-ui/src/views/MenuView.vue and admin-ui/src/views/InventoryView.vue (page-heading/eyebrow/panel chrome + the three-way loading/error/empty convention to reuse)
    - admin-ui/src/views/InventoryView.vue (onMovementIngredientChange lines 169-174 + the `<select @change>` idiom — the per-row ingredient-select auto-fill-unit sub-pattern to reuse per row)
    - admin-ui/src/lib/recipe.ts (toRecipeRequest, created in Task 1 — the save payload contract)
    - .planning/phases/20-.../20-UI-SPEC.md (§2 recipe builder — full interaction contract: page heading copy, repeatable row tokens, empty/loading/error states, cost panel columns, "Save recipe"/"Back to menu" copy, ADMIN-only route)
    - .planning/phases/20-.../20-PATTERNS.md (§"admin-ui/src/views/RecipeView.vue (NEW)" — NO analog for the row-array mechanic; reuse RESEARCH Pattern 2 directly; reuse .tag-chip tokens, no new radius/padding)
  </read_first>
  <action>
    Create `admin-ui/src/views/RecipeView.vue`. Read `dishId` from `useRoute().params.dishId` and resolve the dish name (from the loaded recipe response `name`, or pass/fetch as needed). Page heading: eyebrow "Admin module" → h2 `Recipe — {dishName}` → paragraph per UI-SPEC §2; a `ghost-button` "Back to menu" → `router.push('/menu')`. On mount: fetch existing recipe via `menuApi.getRecipe('DISH', dishId)` (pre-fill `rows` from its lines: ingredientId/quantity/unit) and ingredient options via `inventoryApi.listIngredients()`. Loading → `.skeleton-table`; error → `EmptyState tone="error"` + Retry; empty (0 rows) → `EmptyState tone="empty"` title "No ingredients yet" body per UI-SPEC (no retry). Repeatable rows: `rows = ref<RecipeRowForm[]>([])`; each row is a flex container reusing the `.tag-chip` token set (padding 8px 10px, border 1px solid var(--border), border-radius 6px, gap 12px — NO new tokens) containing: ingredient `<select v-model @change="onIngredientChange(index)">` (options from listIngredients, auto-fills that row's `unit` from the selected ingredient's baseUnit), quantity `<input type="number" step="0.01" min="0">`, unit `<input>` (editable), and a remove `icon-button` (`X`, aria-label "Remove ingredient row"). `addRow()` pushes `{ ingredientId:'', quantity:0, unit:'' }`; `removeRow(index)` splices. Do NOT expose sortOrder as a field — it's auto-assigned by `toRecipeRequest`. Coarse validation: one `.form-error` "Every ingredient row needs an ingredient, quantity, and unit." if any row is incomplete at submit. `saveRecipe()` builds the payload via `toRecipeRequest(dishId, dishName, rows.value, ingredients.value)` (guaranteeing both ingredientId + ingredient name), calls `menuApi.upsertRecipe(payload)`, then refreshes the cost panel via `menuApi.recipeCost('DISH', dishId)`. Cost panel: a separate `.panel` "Recipe cost" showing Total (`formatMoney(totalCost)`), a `StatusBadge` (success/warning) labelled "Fully costed"/"Partial" from `fullyCosted`, and a `DataTable` of cost lines (Ingredient, Quantity, Unit, Line cost) — refreshed only after a successful save, not live. Page-level footer (not modal): `primary-button` "Save recipe" (disabled + "Saving…" while pending) + `ghost-button` "Back to menu". Use `messageOf` in catches; interpolate all text via `{{ }}` (no v-html).
  </action>
  <acceptance_criteria>
    - `admin-ui/src/views/RecipeView.vue` exists and contains `addRow` and `removeRow` over a `ref` array.
    - `saveRecipe` calls `menuApi.upsertRecipe` with the output of `toRecipeRequest` (imported from `../lib/recipe`).
    - Cost panel calls `menuApi.recipeCost` after save.
    - The empty state uses the exact "No ingredients yet" copy.
    - `npm --prefix admin-ui run build` exits 0 (vue-tsc typecheck) and `npm --prefix admin-ui run test` green.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build && npm --prefix admin-ui run test</automated>
  </verify>
  <done>Dedicated recipe builder with add/remove rows, auto-fill unit, index sortOrder, dual-field payload, and post-save cost panel; reuses existing tokens only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RecipeView → menuApi → /admin/menu/recipes (ADMIN) | Client route/button gating is UX only; the ADMIN-only route guard is added in 20-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2005-01 | Tampering | malformed RecipeRequest payload (missing ingredient name) | mitigate | The pure `toRecipeRequest` (unit-tested) always resolves and includes the `ingredient` display name for every line; RecipeView builds the payload ONLY through it, never inline — closing the Pitfall 2 dual-field trap. |
| T-2005-02 | Elevation of Privilege | STAFF deep-linking the recipe route | accept (guarded in 20-06) | The route-level `adminOnly` guard (added in 20-06) redirects STAFF to `/menu`; a forced API call still 403s server-side. Client gating is UX only (UI-SPEC §6). |
| T-2005-03 | Information Disclosure (XSS) | ingredient/cost names rendered in rows/DataTable | mitigate | All rendered via `{{ }}` interpolation; no `v-html`. |
</threat_model>

<verification>
- `npm --prefix admin-ui run build` clean; `npm --prefix admin-ui run test` green (recipe.test.ts + existing suite).
- Manual smoke: add/remove ingredient rows, save, cost panel refreshes; payload (devtools/network) shows both ingredientId and ingredient name per line.
</verification>

<success_criteria>
Dedicated recipe-builder screen (D-02) with repeatable rows, auto-fill unit, index-based sortOrder, dual-field payload guaranteed by a unit-tested pure transform, and a post-save cost panel — composed entirely from existing tokens/primitives.
</success_criteria>

<output>
Create `.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-05-SUMMARY.md` when done.
</output>
