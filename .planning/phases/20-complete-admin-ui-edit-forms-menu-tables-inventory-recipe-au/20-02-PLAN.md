---
phase: 20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - admin-ui/src/views/MenuView.vue
autonomous: true
requirements: [D-01, TOPPING, ROLE-GATE]
must_haves:
  truths:
    - "Editing a category or dish opens the same modal pre-filled (D-01 shared create+edit); submit routes to PUT for edits, POST for creates"
    - "Clicking a category chip's text opens edit mode; the × still archives (unchanged)"
    - "Topping groups/options are managed in-view (create group, create option, archive option); the deferred placeholder panel is removed"
    - "Each dish row exposes a Recipe entry button navigating to the recipe route"
    - "All Menu catalog CRUD + topping + Recipe controls are hidden for STAFF (v-if=isAdmin), never disabled"
  artifacts:
    - path: "admin-ui/src/views/MenuView.vue"
      provides: "Create+edit category/dish modals, topping management section, recipe entry, role-gated controls"
      contains: "editingCategoryId"
  key_links:
    - from: "admin-ui/src/views/MenuView.vue submitCategory/submitDish"
      to: "menuApi.updateCategory / menuApi.updateDish"
      via: "branch on editing{Entity}Id"
      pattern: "menuApi\\.update(Category|Dish)"
    - from: "admin-ui/src/views/MenuView.vue topping controls"
      to: "menuApi.createToppingGroup / createToppingOption / archiveToppingOption"
      via: "modal + chip archive"
      pattern: "menuApi\\.(createToppingGroup|createToppingOption|archiveToppingOption)"
    - from: "admin-ui/src/views/MenuView.vue dish row Recipe button"
      to: "route name 'recipe'"
      via: "router.push({ name: 'recipe', params: { dishId } })"
      pattern: "name: 'recipe'"
---

<objective>
Extend `MenuView.vue` to close three Menu gaps: (1) generalize the existing create-only category/dish modals into shared create+edit modals per D-01, (2) replace the deferred topping placeholder panel with first-class topping group/option management, and (3) add a per-dish Recipe entry point — all behind `v-if="isAdmin"` role gating per UI-SPEC §6.

Purpose: Menu catalog is ADMIN-only; this delivers the edit + topping + recipe-entry affordances that Phase 19 left as placeholders.
Output: A fully editable, role-gated MenuView with topping management and a recipe route entry.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-UI-SPEC.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-PATTERNS.md

<interfaces>
<!-- Bindings created in 20-01 (already merged). Consume directly — do not touch modules.ts. -->
From admin-ui/src/api/modules.ts:
- `menuApi.updateCategory(id, body: CategoryRequest)`, `menuApi.updateDish(id, body: DishRequest)` (new in 20-01)
- `menuApi.createToppingGroup(body: ToppingGroupRequest)`, `menuApi.createToppingOption(body: ToppingOptionRequest)`, `menuApi.archiveToppingOption(id)` (existing, previously uncalled)
- `menuApi.createCategory`, `menuApi.createDish`, `menuApi.archiveCategory`, `menuApi.archiveDish` (existing)
- Types: CategoryRequest/CategoryResponse, DishRequest/DishResponse, ToppingGroupRequest, ToppingOptionRequest, MenuStatus
From admin-ui/src/stores/auth.ts:
- `import { authState, isAdmin } from '../stores/auth'` — `isAdmin` computed (new in 20-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize category/dish modals to create+edit, add role gating + recipe entry</name>
  <files>admin-ui/src/views/MenuView.vue</files>
  <read_first>
    - admin-ui/src/views/MenuView.vue (create-only category modal openCategoryModal line 117 / submitCategory; dish modal openDishModal line 159; category chip × line 245; dish row table-actions line 277; requestArchive line 200; messageOf import line 19)
    - .planning/phases/20-.../20-PATTERNS.md (§"admin-ui/src/views/MenuView.vue — EXTEND" — Pattern 1 seed-on-row + branch-submit; chip text-vs-× distinction; role-gating list of exact controls)
    - .planning/phases/20-.../20-RESEARCH.md (§"Pattern 1: Shared create+edit modal" — full worked example incl. computed title)
    - .planning/phases/20-.../20-UI-SPEC.md (§1 shared create+edit modal contract; §6 role matrix — Menu catalog CRUD is ADMIN-only "Hide" for STAFF)
  </read_first>
  <action>
    Import `authState, isAdmin` from `../stores/auth`. For category: add `editingCategoryId = ref<string|null>(null)`; change `openCategoryModal(row?: CategoryResponse)` to seed name/description/sortOrder/status from `row` when present (else reset to defaults) and set `editingCategoryId.value = row?.id ?? null`; branch `submitCategory()` — when `editingCategoryId.value` is set call `menuApi.updateCategory(id, payload)`, else `menuApi.createCategory(payload)`; on success reset the editing id, close, `loadMenu()`. Make the modal `:title` a computed `editingCategoryId.value ? 'Edit category' : 'New category'`. Keep the primary button label "Save changes" in BOTH modes (UI-SPEC copy). Use `messageOf(caught, fallback)` for the catch (replace the older inline `caught instanceof ApiError` idiom). Apply the identical create→edit generalization to the dish modal (`editingDishId`, `openDishModal(row?)`, branch `submitDish` on updateDish vs createDish, computed title 'Edit dish'/'New dish'). Category-chip edit: wrap the chip's text node in a clickable span/button calling `openCategoryModal(category)`; leave the existing archive `<button>` (× → requestArchive) unchanged (UI-SPEC §1 chip note). Dish row: add an "Edit" `ghost-button small` BEFORE the existing "Archive" button (line 278) calling `openDishModal(row)`, and a "Recipe" `ghost-button small` after Archive calling `router.push({ name: 'recipe', params: { dishId: row.id } })` (import `useRouter`). Role gating: wrap "New category" (line 240), "New dish" (line 261), the category chip edit-span + × (line 245), and dish row Edit/Archive/Recipe buttons in `v-if="isAdmin"`. Do NOT gate anything else in this view.
  </action>
  <acceptance_criteria>
    - MenuView.vue contains `editingCategoryId` and `editingDishId` refs.
    - `submitCategory`/`submitDish` reference `menuApi.updateCategory`/`menuApi.updateDish`.
    - Dish row contains a Recipe button with `name: 'recipe'`.
    - "New category"/"New dish"/dish-row action buttons are wrapped in `v-if="isAdmin"`.
    - `npm --prefix admin-ui run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build</automated>
  </verify>
  <done>Category + dish modals do create AND edit via pre-fill + branch-submit; controls hidden for STAFF; recipe entry wired.</done>
</task>

<task type="auto">
  <name>Task 2: Replace deferred placeholder with topping group/option management section</name>
  <files>admin-ui/src/views/MenuView.vue</files>
  <read_first>
    - admin-ui/src/views/MenuView.vue (the `.notice-panel` placeholder at lines 305-314 to DELETE; the category-chip archive idiom lines 242-250 to imitate for option chips; existing Modal usage lines 316+)
    - .planning/phases/20-.../20-UI-SPEC.md (§3 topping management — create+archive only, no PUT/no group archive; exact modal fields + copy; §"Copywriting Contract" for archive confirm copy)
    - .planning/phases/20-.../20-PATTERNS.md (§"Topping management section" — reuse category-chip archive + PaymentsView toolbar-opens-modal shape)
    - .planning/phases/20-.../20-RESEARCH.md (§"Anti-Patterns" — do NOT fabricate a PUT or group-archive endpoint; only createToppingGroup/createToppingOption/archiveToppingOption exist)
  </read_first>
  <action>
    Delete the `.notice-panel` placeholder block (lines 305-314, the "dedicated editor UI is deferred…" copy) entirely — do not leave it beside the new UI. Add a "Topping groups & options" `.panel` section iterating the existing `menu.categories[].dishes[].toppingGroups[]` tree already returned by the loaded menu (no new fetch). Per dish: header with dish name + `ghost-button small` "New group" opening a `Modal` titled "New topping group" (fields: name, minSelections number, maxSelections number, sortOrder number) → `menuApi.createToppingGroup({ dishId/targetId per ToppingGroupRequest, ... })` then `loadMenu()`. Each group renders as a nested sub-block (reuse the `.tag-chip` border/radius/padding tokens — no new values) with header = group name + a `StatusBadge tone="info"` showing "{minSelections}–{maxSelections} selections" + `ghost-button small` "New option" opening a `Modal` "New topping option" (fields: name, additionalPrice number step 0.01, status select ACTIVE/INACTIVE/ARCHIVED, sortOrder number) → `menuApi.createToppingOption(...)`. Options render as a `.tag-list` of `.tag-chip`s labelled "{name} (+{formatMoney(additionalPrice)})" each with an × wired to a ConfirmDialog (archive copy from UI-SPEC Copywriting Contract) → `menuApi.archiveToppingOption(id)`. Empty dish (zero groups): inline `.field-hint` "No topping groups yet." (not a full EmptyState). Do NOT add any update/PUT control and do NOT add a group-archive control (no backend). Wrap ALL topping create/archive controls in `v-if="isAdmin"` (Menu catalog is ADMIN-only). Use `messageOf` in every catch. Match ToppingGroupRequest/ToppingOptionRequest field names exactly from modules.ts.
  </action>
  <acceptance_criteria>
    - The "dedicated editor UI is deferred" placeholder string is removed from MenuView.vue (grep returns no match).
    - MenuView.vue references `menuApi.createToppingGroup`, `menuApi.createToppingOption`, and `menuApi.archiveToppingOption`.
    - No `updateTopping*` or topping-group-archive call is referenced.
    - Topping controls wrapped in `v-if="isAdmin"`.
    - `npm --prefix admin-ui run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build && npm --prefix admin-ui run test</automated>
  </verify>
  <done>Topping groups/options are managed in-view (create+archive only); placeholder gone; ADMIN-gated; build+tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MenuView controls → menuApi → /admin/menu/** | ADMIN-only backend surface; client hiding is UX only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2002-01 | Elevation of Privilege | `v-if="isAdmin"` gating on Menu CRUD | accept (UX only) | Client hide is NOT a security boundary. A STAFF user who forces the request still gets 403 from SecurityConfig's `/admin/**`→ADMIN matcher, surfaced via the existing ApiError → `.form-error` path. Documented non-goal (UI-SPEC §6). |
| T-2002-02 | Tampering (XSS) | topping/option/category names rendered in chips | mitigate | Render all names via Vue `{{ }}` interpolation (auto-escaped); never `v-html`. |
</threat_model>

<verification>
- `npm --prefix admin-ui run build` clean; `npm --prefix admin-ui run test` green (existing suite unaffected — no view-test precedent added here).
- Manual (smoke, per VALIDATION §Manual): log in as STAFF → Menu CRUD/topping/Recipe controls hidden; as ADMIN → edit modal pre-fills, topping create/archive works, Recipe button navigates.
</verification>

<success_criteria>
Category/dish edit via shared modal (D-01); topping groups/options managed in-view (create+archive only); placeholder removed; recipe entry wired; all Menu catalog controls hidden for STAFF.
</success_criteria>

<output>
Create `.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-02-SUMMARY.md` when done.
</output>
