---
phase: 20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - admin-ui/src/views/TablesView.vue
autonomous: true
requirements: [D-01, RESV-STATUS, ROLE-GATE]
must_haves:
  truths:
    - "Editing a dining area or table opens the shared modal pre-filled (D-01); submit routes to PUT for edits, POST for creates"
    - "The reservation row's action opens a status selector offering CONFIRMED/NO_SHOW/COMPLETED/CANCELLED (not hardcoded CANCELLED)"
    - "CANCELLED and NO_SHOW transitions escalate through a danger ConfirmDialog; CONFIRMED/COMPLETED apply immediately"
    - "Area/table catalog controls are hidden for STAFF (v-if=isAdmin); reservations/occupancy/session controls stay visible to STAFF"
  artifacts:
    - path: "admin-ui/src/views/TablesView.vue"
      provides: "Create+edit area/table modals, full reservation status modal, role-gated catalog controls"
      contains: "editingAreaId"
  key_links:
    - from: "admin-ui/src/views/TablesView.vue submitArea/submitTable"
      to: "tablesApi.updateArea / tablesApi.updateTable"
      via: "branch on editing{Entity}Id"
      pattern: "tablesApi\\.update(Area|Table)"
    - from: "admin-ui/src/views/TablesView.vue reservation status modal"
      to: "tablesApi.updateReservationStatus"
      via: "selected nextStatus (not literal 'CANCELLED')"
      pattern: "updateReservationStatus\\("
---

<objective>
Extend `TablesView.vue`: (1) generalize the create-only area/table modals into shared create+edit modals per D-01, and (2) replace the hardcoded `status: 'CANCELLED'` reservation action (Pitfall 1) with a full 4-status selector modal — behind role gating that hides ONLY the ADMIN-only area/table catalog controls while keeping reservation/occupancy/session actions visible to STAFF (UI-SPEC §6).

Purpose: Close the Tables catalog-edit gap and the reservation full-status gap Phase 19 left minimal.
Output: Editable areas/tables + full reservation status transitions, correctly role-scoped.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-UI-SPEC.md
@.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-PATTERNS.md

<interfaces>
<!-- Bindings created in 20-01 (already merged). Consume directly — do not touch modules.ts. -->
From admin-ui/src/api/modules.ts:
- `tablesApi.updateArea(id, body: DiningAreaRequest)`, `tablesApi.updateTable(id, body: DiningTableRequest)` (new in 20-01)
- `tablesApi.updateReservationStatus(reservationId, body: UpdateReservationStatusRequest)` (existing, currently called with hardcoded CANCELLED)
- `tablesApi.createArea`, `tablesApi.createTable`, `tablesApi.archiveArea` (existing)
- Types: `ReservationStatus = 'PENDING'|'CONFIRMED'|'SEATED'|'CANCELLED'|'NO_SHOW'|'COMPLETED'`; `UpdateReservationStatusRequest = { status: ReservationStatus; note?: string }`; DiningAreaRequest/Response, DiningTableRequest/Response
From admin-ui/src/stores/auth.ts:
- `import { isAdmin } from '../stores/auth'` — standalone computed; do NOT also import `authState` (unused → fails `vue-tsc noUnusedLocals`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize area/table modals to create+edit and role-gate catalog controls</name>
  <files>admin-ui/src/views/TablesView.vue</files>
  <read_first>
    - admin-ui/src/views/TablesView.vue (openAreaModal line 146, openTableModal line 192, "New area" line 365, "New table" line 378; area × / table row action buttons; messageOf usage)
    - .planning/phases/20-.../20-PATTERNS.md (§"admin-ui/src/views/TablesView.vue — EXTEND" — same mechanical create→edit pattern as Menu; role-gating list; do NOT gate reservations/occupancy/session)
    - .planning/phases/20-.../20-RESEARCH.md (§"Pattern 1: Shared create+edit modal")
    - .planning/phases/20-.../20-UI-SPEC.md (§1 modal contract; §6 — Table catalog CRUD ADMIN-only, Table operations ADMIN+STAFF visible)
  </read_first>
  <action>
    Import `isAdmin` (only — NOT `authState`, which would be unused and fail `vue-tsc noUnusedLocals`) from `../stores/auth`. Add `editingAreaId`/`editingTableId` refs; change `openAreaModal(row?: DiningAreaResponse)` and `openTableModal(row?: DiningTableResponse)` to seed every form field from `row` when present (else reset), setting the editing id; branch `submitArea`/`submitTable` on the editing id to call `tablesApi.updateArea(id, payload)` / `tablesApi.updateTable(id, payload)` vs the existing create calls; on success reset editing id, close, reload. Make each modal `:title` computed ('Edit dining area'/'New area', 'Edit table'/'New table' per UI-SPEC §1). Keep "Save changes" label in both modes. Add an "Edit" `ghost-button small` before the existing Archive action on the table row; for the dining-area chips, wire the chip text-click to `openAreaModal(area)` while the × stays archive (same idiom as Menu category chips). Use `messageOf` in catches. Role gating: wrap "New area" + area × + area-edit-click, "New table", and table row Edit/Archive in `v-if="isAdmin"`. Do NOT wrap the Reservations panel, Occupancy panel, or any session-modal action — those are ADMIN+STAFF (UI-SPEC §6).
  </action>
  <acceptance_criteria>
    - TablesView.vue contains `editingAreaId` and `editingTableId`.
    - `submitArea`/`submitTable` reference `tablesApi.updateArea`/`tablesApi.updateTable`.
    - "New area"/"New table"/table-row catalog buttons wrapped in `v-if="isAdmin"`; reservation/occupancy/session controls are NOT wrapped.
    - `npm --prefix admin-ui run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build</automated>
  </verify>
  <done>Area/table edit via shared modal; only catalog controls hidden for STAFF; table-ops untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded CANCELLED with a full reservation status selector modal</name>
  <files>admin-ui/src/views/TablesView.vue</files>
  <read_first>
    - admin-ui/src/views/TablesView.vue (cancelReservation lines 325-334 to DELETE; the "Cancel" row button line 425; sessionModal reactive-object + computed-title shape lines 68-137 to imitate; existing ConfirmDialog usage lines ~645-654)
    - .planning/phases/20-.../20-UI-SPEC.md (§5 full reservation status control — 4 target statuses excluding current, optional note, confirm escalation for CANCELLED/NO_SHOW; §"Copywriting Contract" for the exact cancel/no-show confirm copy)
    - .planning/phases/20-.../20-RESEARCH.md (§"Pitfall 1"; §"Anti-Patterns" — do NOT offer PENDING or SEATED as selectable options)
  </read_first>
  <action>
    Delete `cancelReservation()` (lines 325-334) entirely — do not extend it. Add a `reservationStatusModal` reactive object `{ open, reservationId, currentStatus, nextStatus, note, saving, error }` following the `sessionModal` single-reactive-object + shared-Modal + computed-title shape (but single-mode, simpler). Rename the reservation row's "Cancel" `ghost-button small` (line 425) to "Status" and change its `@click` to open the modal seeded with the row's current status. Modal titled "Update reservation status" contains a `<select v-model="reservationStatusModal.nextStatus">` listing exactly the 4 target statuses CONFIRMED / NO_SHOW / COMPLETED / CANCELLED, EXCLUDING the row's current status (do NOT list PENDING or SEATED — those are reached via creation/seat flows per RESEARCH Anti-Patterns), plus an optional `note` `<input>` (maps to UpdateReservationStatusRequest.note). Footer: `ghost-button` "Cancel" + `primary-button` "Update status". On submit: if nextStatus is CONFIRMED or COMPLETED, call `tablesApi.updateReservationStatus(reservationId, { status, note })` immediately, update the reservation in the local list, close. If nextStatus is CANCELLED or NO_SHOW, first open a danger `ConfirmDialog` (reuse existing pattern; copy verbatim from UI-SPEC Copywriting Contract — "Cancel reservation"/confirm "Cancel reservation" and "Mark no-show"/confirm "Mark no-show") and only fire the API call on confirm. Use `messageOf` in catches. This modal is NOT role-gated (reservations are ADMIN+STAFF).
  </action>
  <acceptance_criteria>
    - The `cancelReservation` function name no longer exists in TablesView.vue (grep returns no match).
    - `updateReservationStatus` is called with a variable status (no literal `status: 'CANCELLED'` remains).
    - The status `<select>` offers CONFIRMED, NO_SHOW, COMPLETED, CANCELLED and never PENDING/SEATED.
    - CANCELLED/NO_SHOW paths route through a ConfirmDialog before the API call.
    - `npm --prefix admin-ui run build` exits 0 and `npm --prefix admin-ui run test` green.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix admin-ui run build && npm --prefix admin-ui run test</automated>
  </verify>
  <done>Reservation status fully selectable (4 targets) with danger confirmation on CANCELLED/NO_SHOW; hardcoded CANCELLED removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TablesView catalog controls → tablesApi → /admin/tables/** (ADMIN) | Client hide is UX only. |
| TablesView reservation/session controls → /admin/tables/reservations|sessions/** (ADMIN+STAFF) | Not gated client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2003-01 | Elevation of Privilege | `v-if="isAdmin"` on area/table CRUD | accept (UX only) | Server SecurityConfig enforces `/admin/tables/areas|{id}`→ADMIN; STAFF forcing the call gets 403 via existing ApiError path. Reservation endpoints correctly left ungated (ADMIN+STAFF). |
| T-2003-02 | Tampering | wrong reservation status transition | mitigate | Selector excludes the current status and never offers PENDING/SEATED; destructive CANCELLED/NO_SHOW require an explicit ConfirmDialog before firing. |
</threat_model>

<verification>
- `npm --prefix admin-ui run build` clean; `npm --prefix admin-ui run test` green.
- Manual smoke: STAFF sees no area/table CRUD but full reservation Status modal; ADMIN sees both; CANCELLED prompts confirm.
</verification>

<success_criteria>
Area/table create+edit via shared modal (D-01); reservation status modal offers all 4 target transitions with danger confirmation; catalog controls hidden for STAFF, reservations visible to STAFF.
</success_criteria>

<output>
Create `.planning/phases/20-complete-admin-ui-edit-forms-menu-tables-inventory-recipe-au/20-03-SUMMARY.md` when done.
</output>
