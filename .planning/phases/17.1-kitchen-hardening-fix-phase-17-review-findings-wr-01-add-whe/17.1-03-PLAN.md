---
phase: 17.1-kitchen-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/order_context/application/OrderLedgerWriter.java
  - src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java
  - src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java
autonomous: true
requirements: [K-WR-03, I-WR-01]
must_haves:
  truths:
    - "When the derived target status has an unknown fulfillment rank, the projection logs a warning and skips (fail-closed) instead of overwriting a lower/unknown current status"
    - "A pre-CONFIRMED order (rank unknown, e.g. PENDING_CONFIRMATION/SUBMITTED) is never jumped past CONFIRMED by a kitchen snapshot"
    - "The order-side idempotency ledger insert runs in its own REQUIRES_NEW transaction, shared by the projection and confirmation handlers"
  artifacts:
    - path: "src/main/java/com/example/feat1/DDD/order_context/application/OrderLedgerWriter.java"
      provides: "REQUIRES_NEW ledger writer for order processed-events"
      contains: "Propagation.REQUIRES_NEW"
    - path: "src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java"
      provides: "fail-closed rank guard + delegated ledger write"
      contains: "currentRank"
  key_links:
    - from: "KitchenStatusProjectionService.onTicketStatusChanged"
      to: "OrderLedgerWriter.tryInsert"
      via: "REQUIRES_NEW ledger insert"
      pattern: "ledgerWriter.tryInsert"
---

<objective>
Make KitchenStatusProjectionService fail-closed on an unknown fulfillment rank
(K-WR-03) and create the order-context REQUIRES_NEW ledger writer (I-WR-01, order side)
that this handler uses now and OrderConfirmationService adopts in Wave 2.

Purpose: The current rank guard uses getOrDefault(status, -1) for BOTH current and
target rank. If the current order status is not in FULFILLMENT_RANK (e.g. it is still
PENDING_CONFIRMATION/SUBMITTED, rank -1), any fulfillment status (rank >= 0) passes the
`targetRank <= currentRank` check and overwrites it, skipping CONFIRMED. Fail-closed
must reject an unknown current rank. Separately the inline saveAndFlush+catch is the
same rollback-only anti-pattern fixed elsewhere.
Output: OrderLedgerWriter bean (shared); fail-closed guard with warning log.
</objective>

<execution_context>
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/chinhlq/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-kitchen-hardening-fix-phase-17-review-findings-wr-01-add-whe/17.1-CONTEXT.md
@.planning/phases/17.1-kitchen-hardening-fix-phase-17-review-findings-wr-01-add-whe/17.1-RESEARCH.md
@src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryLedgerWriter.java

<interfaces>
<!-- Extracted contracts — use directly. -->

KitchenStatusProjectionService: CONSUMER_NAME = "kitchen-status-projection"; FULFILLMENT_RANK =
Map.of(CONFIRMED,0, PREPARING,1, READY,2, SERVED,3, COMPLETED,4). Current guard (lines ~95-99):
int targetRank = FULFILLMENT_RANK.getOrDefault(target, -1); int currentRank =
FULFILLMENT_RANK.getOrDefault(order.getStatus(), -1); if (targetRank <= currentRank) return;
order.setStatus(target). Inline idempotency: existsByEventIdAndConsumerName pre-check then
try{ saveAndFlush(OrderProcessedEventEntity) }catch(DIVE){return;}.

OrderProcessedEventRepository: JpaRepository<OrderProcessedEventEntity, UUID>; boolean
existsByEventIdAndConsumerName(UUID, String). OrderProcessedEventEntity setters:
setEventId(UUID), setConsumerName(String), setProcessedAt(Instant).

InventoryLedgerWriter (pattern to mirror): @Component; @Transactional(propagation =
Propagation.REQUIRES_NEW) boolean tryInsert(UUID eventId, String consumerName). MUST be a
distinct bean, not a private method.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrderLedgerWriter (REQUIRES_NEW) and delegate the projection ledger write</name>
  <files>src/main/java/com/example/feat1/DDD/order_context/application/OrderLedgerWriter.java, src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java</files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/inventory_context/application/InventoryLedgerWriter.java
    - src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java
    - src/main/java/com/example/feat1/DDD/order_context/infrastructure/repository/OrderProcessedEventRepository.java
  </read_first>
  <action>
    Create OrderLedgerWriter as a @Component @RequiredArgsConstructor in package
    com.example.feat1.DDD.order_context.application, mirroring InventoryLedgerWriter: inject
    OrderProcessedEventRepository; @Transactional(propagation = Propagation.REQUIRES_NEW) public
    boolean tryInsert(UUID eventId, String consumerName) that builds OrderProcessedEventEntity
    (setEventId/setConsumerName/setProcessedAt(Instant.now())), saveAndFlush, returns true, catches
    DataIntegrityViolationException → log.debug + return false. In KitchenStatusProjectionService,
    inject OrderLedgerWriter and REPLACE the inline try/catch saveAndFlush block: keep the
    existsByEventIdAndConsumerName fast pre-check, then
    `if (!ledgerWriter.tryInsert(event.eventId(), CONSUMER_NAME)) return;`. Remove now-unused
    DataIntegrityViolationException import if unused. (OrderLedgerWriter is a new file consumed
    again by OrderConfirmationService in plan 17.1-06.)
  </action>
  <verify>
    <automated>./mvnw -q test -Dtest=KitchenStatusProjectionServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - OrderLedgerWriter.java exists, @Component, with `@Transactional(propagation = Propagation.REQUIRES_NEW)` on tryInsert.
    - KitchenStatusProjectionService calls `ledgerWriter.tryInsert(...)`; `grep -c 'processedEventRepository.saveAndFlush' KitchenStatusProjectionService.java` returns 0.
    - Existing projection idempotency tests (replay + concurrent duplicate) still pass.
  </acceptance_criteria>
  <done>Order-context ledger insert isolated in a shared REQUIRES_NEW writer bean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed rank guard on unknown fulfillment rank (K-WR-03)</name>
  <files>src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java, src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java</files>
  <read_first>
    - src/main/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionService.java
    - src/test/java/com/example/feat1/DDD/order_context/application/KitchenStatusProjectionServiceTest.java
    - 17.1-RESEARCH.md "K-WR-03 fail-closed rank guard" and "Anti-Patterns to Avoid"
  </read_first>
  <behavior>
    - Order currently PENDING_CONFIRMATION (rank absent) + snapshot deriving PREPARING → status is NOT changed (fail-closed) and a warning is logged once.
    - Order currently CONFIRMED (rank 0) + snapshot deriving PREPARING (rank 1) → status advances to PREPARING (normal forward transition still works).
    - Order currently READY (rank 2) + snapshot deriving PREPARING (rank 1) → no change (non-advancing).
  </behavior>
  <action>
    Replace the rank guard so an unknown CURRENT rank is fail-closed. Compute
    int currentRank = FULFILLMENT_RANK.getOrDefault(order.getStatus(), -1) and
    int targetRank = FULFILLMENT_RANK.getOrDefault(target, -1). Change the guard to:
    `if (currentRank < 0 || targetRank <= currentRank) { if (currentRank < 0) log.warn(...unknown
    fulfillment rank for order status {} — skipping projection to {}...); return; }`. Then
    order.setStatus(target). Add a private static SLF4J Logger. Do not alter deriveTargetStatus or
    the REJECTED-terminal guard. The order only leaves an unknown/pre-CONFIRMED status through its
    own confirmation path, never via this projection.
  </action>
  <verify>
    <automated>./mvnw -q test -Dtest=KitchenStatusProjectionServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - Guard includes `currentRank < 0` fail-closed branch; `grep -c 'currentRank < 0' KitchenStatusProjectionService.java` returns 1.
    - New test: current status PENDING_CONFIRMATION + items yielding PREPARING → order.setStatus is never called with PREPARING and log.warn fires.
    - New test: current status CONFIRMED + items yielding PREPARING → order.setStatus(PREPARING) applied (forward transition preserved).
    - New test: current status READY + items yielding PREPARING → no status change.
  </acceptance_criteria>
  <done>Unknown current rank fail-closes (log + skip); CONFIRMED cannot be jumped past.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kafka redelivery/reordering → projection | out-of-order snapshots must not regress or skip order status |
| ledger insert → business transaction | inner violation must not poison outer tx |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.1-07 | Tampering (state jump) | KitchenStatusProjectionService fail-open rank guard | mitigate | fail-closed on currentRank < 0 — unknown status never overwritten, CONFIRMED never skipped |
| T-17.1-08 | Denial of Service (rollback-only) | inline saveAndFlush+catch in projection tx | mitigate | REQUIRES_NEW OrderLedgerWriter isolates ledger insert |
| T-17.1-09 | Information disclosure | fail-closed warn log | accept | logs status enum + orderId only |
| T-17.1-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `./mvnw -q test -Dtest=KitchenStatusProjectionServiceTest` green including fail-closed cases.
- Wave-merge gate: full `./mvnw test` stays green.
</verification>

<success_criteria>
- Fail-closed guard rejects unknown current rank with a warning (K-WR-03).
- OrderLedgerWriter created and used by the projection (I-WR-01 order side, shared with plan 06).
</success_criteria>

<output>
Create `.planning/phases/17.1-kitchen-hardening-fix-phase-17-review-findings-wr-01-add-whe/17.1-03-SUMMARY.md` when done.
</output>
