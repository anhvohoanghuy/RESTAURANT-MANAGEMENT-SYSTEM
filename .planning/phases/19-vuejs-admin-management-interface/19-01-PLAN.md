---
phase: 19-vuejs-admin-management-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - admin-ui/package.json
  - admin-ui/vite.config.ts
  - admin-ui/src/main.ts
  - admin-ui/src/App.vue
  - admin-ui/src/router/index.ts
  - admin-ui/src/stores/auth.ts
  - admin-ui/src/api/client.ts
  - admin-ui/src/layouts/AdminLayout.vue
  - admin-ui/src/views/LoginView.vue
  - admin-ui/src/views/OverviewView.vue
autonomous: true
requirements: [ADMIN-UI-001, ADMIN-UI-002, ADMIN-UI-004]
must_haves:
  truths:
    - "Vue 3 + Vite + TypeScript app exists in admin-ui/"
    - "Unauthenticated users are routed to /login"
    - "Authenticated admin shell has sidebar modules for Overview, Menu, Tables, Inventory, Orders, Kitchen, Payments"
    - "Shared API client attaches Bearer token and handles JSON errors consistently"
---

<objective>
Create the frontend foundation for the admin management UI: Vite/Vue/TypeScript scaffold, auth store, router guards, shared API client, login screen, and dense admin shell with module navigation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite Vue TypeScript app</name>
  <action>Create `admin-ui/` with Vue 3, TypeScript, Vite, vue-router, @lucide/vue, Vitest, and npm scripts for dev/build/test.</action>
  <verify><automated>npm --prefix admin-ui run build</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Add auth store, API client, and router guard</name>
  <action>Implement token persistence, login/logout helpers, shared fetch wrapper, and route guard for authenticated admin routes.</action>
  <verify><automated>npm --prefix admin-ui run test</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Build login view and admin shell</name>
  <action>Implement `LoginView`, `AdminLayout`, sidebar navigation, top bar session controls, and `OverviewView` with operational summary placeholders tied to module navigation.</action>
  <verify><manual>Open Vite dev server, confirm `/login` renders and protected routes redirect when logged out.</manual></verify>
</task>

</tasks>

<verification>
- `npm --prefix admin-ui run build`
- `npm --prefix admin-ui run test`
- Manual browser check for `/login` and `/`
</verification>

<success_criteria>
- Vue app builds successfully.
- Login flow calls `/auth/login` through the shared API client.
- Authenticated layout has stable admin navigation and responsive shell.
- Unauthenticated routes redirect to login.
</success_criteria>
