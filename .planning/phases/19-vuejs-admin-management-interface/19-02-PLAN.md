---
phase: 19-vuejs-admin-management-interface
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - admin-ui/src/api/modules.ts
  - admin-ui/src/components/DataTable.vue
  - admin-ui/src/components/EmptyState.vue
  - admin-ui/src/components/StatusBadge.vue
  - admin-ui/src/views/MenuView.vue
  - admin-ui/src/views/TablesView.vue
  - admin-ui/src/views/InventoryView.vue
  - admin-ui/src/views/PaymentsView.vue
  - admin-ui/src/views/KitchenView.vue
  - admin-ui/src/views/OrdersView.vue
autonomous: true
requirements: [ADMIN-UI-002, ADMIN-UI-003, ADMIN-UI-006]
must_haves:
  truths:
    - "Each admin module has a real route and view"
    - "Available backend endpoints are called through module API functions"
    - "Known backend gaps are surfaced as disabled/gap states, not mocked as complete"
---

<objective>
Build the admin module views and reusable operational UI primitives for menu, tables, inventory, payments, kitchen, and orders.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable admin primitives</name>
  <action>Create compact data table, status badge, empty/error state, toolbar, and form/action button patterns following `19-UI-SPEC.md`.</action>
  <verify><automated>npm --prefix admin-ui run build</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Wire module API functions</name>
  <action>Add typed API wrappers for menu, tables/table operations, inventory/stock, payments/refunds, kitchen board, and admin cancellation endpoints.</action>
  <verify><automated>npm --prefix admin-ui run test</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Build module pages</name>
  <action>Implement module pages with loading/error/empty states, dense tables, and action affordances for existing backend capabilities.</action>
  <verify><manual>Run against backend and smoke load each module route.</manual></verify>
</task>

</tasks>

<verification>
- `npm --prefix admin-ui run build`
- `npm --prefix admin-ui run test`
- Manual route smoke: `/menu`, `/tables`, `/inventory`, `/payments`, `/kitchen`, `/orders`
</verification>

<success_criteria>
- All primary module routes render inside the admin shell.
- API calls use the shared client.
- Unsupported filters/actions are clearly marked as gaps.
</success_criteria>
