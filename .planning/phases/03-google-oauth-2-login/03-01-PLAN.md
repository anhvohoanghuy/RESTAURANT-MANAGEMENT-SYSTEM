---
phase: 03-google-oauth-2-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/auth/application/dto/GoogleLoginRequest.java
  - src/main/java/com/example/feat1/DDD/auth/application/auth_service/auth_provider/GoogleAuthProvider.java
  - src/main/java/com/example/feat1/DDD/auth/application/auth_service/google/GoogleIdTokenVerifier.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/google/SpringGoogleIdTokenVerifier.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/presentation/AuthController.java
  - src/main/java/com/example/feat1/DDD/identity_context/domain/repository/user/IUserDomainRepository.java
  - src/main/java/com/example/feat1/DDD/identity_context/domain/repository/user/UserDomainRepository.java
  - src/main/java/com/example/feat1/DDD/identity_context/infastructure/repository/IUserRepository.java
  - src/main/resources/application.properties
  - src/test/java/com/example/feat1/DDD/auth/application/auth_service/auth_provider/GoogleAuthProviderTest.java
  - src/test/java/com/example/feat1/DDD/auth/infrastructure/presentation/AuthControllerTest.java
  - src/test/java/com/example/feat1/DDD/auth/integration/AuthFlowIntegrationTest.java
autonomous: true
requirements: [AUTH-011]
user_setup:
  - Configure real Google OAuth client IDs with `GOOGLE_OAUTH_CLIENT_IDS` before using `/auth/google` outside tests.
must_haves:
  truths:
    - `POST /auth/google` accepts `{ "idToken": "..." }` and returns the same token response shape as local login.
    - Google ID tokens are verified before any user or credential is created.
    - Existing local users are auto-linked only for Google-authoritative email.
    - Google access and refresh tokens are not stored.
  artifacts:
    - Google login request DTO exists.
    - Google verifier abstraction and production verifier exist.
    - Unit and integration tests cover the Google login flow.
  key_links:
    - `AuthController` routes `/auth/google` to `AuthService` with `AuthType.GOOGLE`.
    - `GoogleAuthProvider` uses the verifier, identity repositories, credential repository, role repository, and `TokenSerivce`.
---

<objective>
Implement Phase 03 Google OAuth 2 ID-token login.

Purpose: Add social login without changing the existing internal JWT/refresh-token lifecycle.
Output: Working `/auth/google` endpoint, verifier abstraction, account creation/linking behavior, and tests.
</objective>

<execution_context>
@C:/Users/chinh/IdeaProjects/feat1/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chinh/IdeaProjects/feat1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-google-oauth-2-login/03-CONTEXT.md
@.planning/phases/02-auth-context-mvp/02-CONTEXT.md
@src/main/java/com/example/feat1/DDD/auth/application/auth_service/auth_provider/GoogleAuthProvider.java
@src/main/java/com/example/feat1/DDD/auth/infrastructure/presentation/AuthController.java
@src/main/java/com/example/feat1/DDD/auth/TokenSerivce.java
@src/main/java/com/example/feat1/DDD/identity_context/domain/repository/user/IUserDomainRepository.java
@src/main/java/com/example/feat1/DDD/identity_context/domain/repository/credential/ICredentialDomainRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google login API contract</name>
  <files>AuthController.java, GoogleLoginRequest.java, AuthRequest.java</files>
  <read_first>03-CONTEXT.md, AuthController.java</read_first>
  <action>Add `POST /auth/google` accepting `{ idToken }`. Map it to `AuthRequest(AuthType.GOOGLE, null, null, idToken)` and return `AuthService.login(...)`. Add a public request DTO. Keep local `/auth/login`, `/auth/register`, refresh, and logout unchanged.</action>
  <verify>Focused controller test for mapping.</verify>
  <acceptance_criteria>
    - `/auth/google` exists.
    - Missing blank id token can be rejected by provider with `GOOGLE_TOKEN_REQUIRED`.
  </acceptance_criteria>
  <done>Controller mapping test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Google verifier abstraction</name>
  <files>GoogleIdTokenVerifier.java, GoogleUserInfo.java, SpringGoogleIdTokenVerifier.java, application.properties</files>
  <read_first>03-CONTEXT.md</read_first>
  <action>Create an application-level verifier interface returning normalized Google claims. Production implementation must verify issuer, expiry, signature via Google's JWK set, audience membership from `google.oauth.client-ids`, `sub`, email, and `email_verified`. Use stable `AppException` codes for invalid and unverified tokens. Do not use Google tokeninfo in production.</action>
  <verify>Maven compile and provider tests with mocked verifier.</verify>
  <acceptance_criteria>
    - Config supports multiple client IDs.
    - Tests do not call Google network.
  </acceptance_criteria>
  <done>Verifier code compiles and is replaceable in tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Google account login, create, and link behavior</name>
  <files>GoogleAuthProvider.java, user/credential repositories, role repository usage</files>
  <read_first>03-CONTEXT.md, LocalAuthProvider.java, RegisterUserUseCase.java</read_first>
  <action>Replace the placeholder Google provider. If Google credential exists, load that user with roles and issue tokens. If absent and no user exists for email, create user with role `USER` and save Google credential provider user id as Google `sub`. If local user exists for email, link only when email ends with `@gmail.com` or `hd` claim exists, then issue tokens. Reject non-linkable existing emails with `GOOGLE_EMAIL_NOT_LINKABLE`.</action>
  <verify>GoogleAuthProvider unit tests.</verify>
  <acceptance_criteria>
    - New users receive `USER`.
    - Existing Google credential login works.
    - Auto-link policy follows D-12 and D-13.
  </acceptance_criteria>
  <done>Provider tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Add integration coverage</name>
  <files>AuthFlowIntegrationTest.java, GoogleAuthProviderTest.java, AuthControllerTest.java</files>
  <read_first>03-CONTEXT.md, existing auth tests</read_first>
  <action>Add tests from the Phase 03 test plan. Integration tests should use a mocked verifier and refresh-token cache, assert `/auth/google` returns backend token pair, creates/links expected records, and makes `/users/me` work with the returned access token.</action>
  <verify>`mvnw.cmd test` with Java 17.</verify>
  <acceptance_criteria>
    - No real Google or Redis dependency in tests.
    - Full Maven test suite passes.
  </acceptance_criteria>
  <done>All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvnw.cmd test` passes with Java 17.
- [ ] `/auth/google` is covered at controller and integration levels.
- [ ] Google provider unit tests cover create, existing credential, auto-link, rejected link, missing/invalid/unverified token.
</verification>

<success_criteria>
- `AUTH-011` is implemented.
- Existing local auth tests still pass.
- Google login does not store Google access/refresh tokens.
- Google verifier is production-capable and mockable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-google-oauth-2-login/03-01-SUMMARY.md`.
</output>
