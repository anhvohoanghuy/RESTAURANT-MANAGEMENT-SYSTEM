---
phase: 02-auth-context-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/feat1/DDD/auth/TokenSerivce.java
  - src/main/java/com/example/feat1/DDD/auth/application/AuthService.java
  - src/main/java/com/example/feat1/DDD/auth/application/auth_service/auth_provider/LocalAuthProvider.java
  - src/main/java/com/example/feat1/DDD/auth/application/auth_service/jwt/JwtProvider.java
  - src/main/java/com/example/feat1/DDD/auth/application/auth_service/refresh/RefreshTokenCache.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/cache/RedisRefreshTokenCache.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/entity/RefreshTokenEntity.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/presentation/AuthController.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/security/SecurityConfig.java
  - src/main/java/com/example/feat1/DDD/auth/infrastructure/security/JwtAuthenticationFilter.java
  - src/main/java/com/example/feat1/DDD/identity_context/application/RoleSeedInitializer.java
  - src/main/java/com/example/feat1/DDD/identity_context/application/usecase/RegisterUserUseCase.java
  - src/main/java/com/example/feat1/DDD/identity_context/infastructure/presentation/UserController.java
  - src/main/java/com/example/feat1/common/exception/**
  - src/main/resources/application.properties
  - docker-compose.yml
  - src/test/java/com/example/feat1/DDD/auth/**
  - src/test/java/com/example/feat1/DDD/identity_context/**
autonomous: true
requirements: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, AUTH-007, AUTH-008, AUTH-009, AUTH-010]
requirements_addressed: [AUTH-001, AUTH-002, AUTH-003, AUTH-004, AUTH-005, AUTH-006, AUTH-007, AUTH-008, AUTH-009, AUTH-010]
user_setup:
  - Start Redis for local/dev with Docker before exercising the real refresh-token cache path.
must_haves:
  truths:
    - D-01: Support multi-session refresh tokens.
    - D-02: Configure Redis through Docker for local/dev usage.
    - D-03: Keep the database refresh-token table as source of truth and repopulate Redis on valid cache miss.
    - D-04: Treat Redis outages as degraded cache behavior, not authority failure.
    - D-05: Rotate refresh tokens on every successful `/auth/refresh`.
    - D-06: Logout revokes the submitted refresh token in Redis and database state.
    - D-07: Refresh-token reuse revokes all active refresh tokens for the user.
    - D-08: Refresh-token database state uses soft revocation.
    - D-09: Use `/auth/**` as the canonical auth API prefix.
    - D-10: Public registration is `POST /auth/register`.
    - D-11: Authenticated profile is `GET /users/me`.
    - D-12: Public registration always assigns `USER`.
    - D-13: Public registration must not trust client-supplied roles.
    - D-14: Use a global application exception contract.
    - D-15: Error responses use `{ code, message, timestamp }`.
    - D-16: Auth failures map to consistent status/code bodies.
    - D-17: Keep cleanup limited to auth correctness and tests.
    - D-18: Refresh and logout use `{ "refreshToken": "..." }` request bodies.
    - D-19: Login uses `{ "username": "...", "password": "..." }` and defaults to local auth server-side.
    - D-20: Login and refresh responses include token type and expiration metadata.
    - D-21: Logout returns `204 No Content` and is idempotent for token state.
    - D-22: Registration auto-logs in and returns the token response shape.
    - D-23: Local login uses username as the identifier.
    - D-24: Seed `USER` and `ADMIN` roles at startup if missing.
    - D-25: Access tokens include subject, roles, permissions, iat, exp, jti, and tokenType.
    - D-26: Refresh tokens do not need permissions.
    - D-27: Refresh-token reuse returns HTTP `401` with code `REFRESH_TOKEN_REUSED`.
  artifacts:
    - Local auth controller DTOs exist for register, login, refresh, and logout.
    - Refresh-token persistence and Redis cache abstraction exist.
    - Role seeding creates `USER` and `ADMIN` if missing.
    - Self/profile endpoint returns a safe DTO without password hashes.
    - Focused auth tests and HTTP integration tests cover the MVP flow.
  key_links:
    - `AuthController` routes local public auth APIs to `RegisterUserUseCase` and `AuthService`.
    - `TokenSerivce` issues token pairs, persists refresh tokens, rotates refresh tokens, and handles logout.
    - `SecurityConfig` permits `/auth/**` and `GET /menus/public`, protects `/admin/**`, and protects `/users/**`.
---

<objective>
Implement Phase 02 Auth Context MVP.

Purpose: stabilize local registration, username/password login, JWT access tokens, refresh-token lifecycle, logout revocation, profile access, and route protection.
Output: working local auth API, database-backed refresh-token source of truth, Redis cache fallback behavior, global auth error contract, and focused tests.
</objective>

<execution_context>
@C:/Users/chinh/IdeaProjects/feat1/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chinh/IdeaProjects/feat1/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-auth-context-mvp/02-CONTEXT.md
@.planning/phases/01-menu-context/01-CONTEXT.md
@src/main/java/com/example/feat1/DDD/auth/infrastructure/security/SecurityConfig.java
@src/main/java/com/example/feat1/DDD/auth/infrastructure/security/JwtAuthenticationFilter.java
@src/main/java/com/example/feat1/DDD/auth/application/auth_service/jwt/JwtProvider.java
@src/main/java/com/example/feat1/DDD/auth/TokenSerivce.java
@src/main/java/com/example/feat1/DDD/auth/infrastructure/presentation/AuthController.java
@src/main/java/com/example/feat1/DDD/identity_context/application/usecase/RegisterUserUseCase.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize public local auth API</name>
  <files>AuthController.java, RegisterLocalRequest.java, LoginRequest.java, RefreshTokenRequest.java, AuthRequest.java, AuthResponse.java, RegisterUserUseCase.java</files>
  <read_first>02-CONTEXT.md, AuthController.java, RegisterUserUseCase.java</read_first>
  <action>Add or verify the public API contract for `POST /auth/register`, `POST /auth/login`, `POST /auth/refresh`, and `POST /auth/logout`. Registration must create user, local credential, and default `USER` role atomically, must not trust client-supplied roles, and must auto-login with the same response shape as login. Local login must use username/password and keep `AuthType.LOCAL` internal to the server.</action>
  <verify>Controller tests assert request mapping and response shape; integration test covers register then login.</verify>
  <acceptance_criteria>
    - `/auth/register` returns `201` with the token pair.
    - `/auth/login` returns `200` with the token pair for valid local credentials.
    - Public request bodies do not require `authType`.
    - Response shape is `{ accessToken, refreshToken, tokenType, accessExpiresIn, refreshExpiresIn }`.
  </acceptance_criteria>
  <done>Local register/login tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JWT and refresh-token lifecycle</name>
  <files>JwtProvider.java, TokenType.java, JwtProperties.java, TokenSerivce.java, RefreshTokenEntity.java, IRefreshTokenRepository.java, RefreshTokenCache.java, RedisRefreshTokenCache.java, RedisConfig.java, application.properties, docker-compose.yml</files>
  <read_first>02-CONTEXT.md, JwtProvider.java, TokenSerivce.java, RefreshTokenEntity.java</read_first>
  <action>Ensure access tokens include subject, roles, permissions, `iat`, `exp`, `jti`, and `tokenType`. Persist refresh tokens in a dedicated database table as the source of truth. Use Redis as a cache only: on cache miss, validate against the database and repopulate Redis when valid. Rotate refresh tokens on successful refresh, revoke the previous token with soft revocation metadata, and return a new token pair. On reuse of a revoked/rotated token, revoke all active refresh tokens for that user.</action>
  <verify>Unit tests cover generation, type validation, cache miss DB fallback, rotation, expiry, revoked token reuse, and all-session revocation.</verify>
  <acceptance_criteria>
    - Refresh token validation checks JWT validity, token type, stored state, owning user, and expiry.
    - Redis absence alone does not invalidate a valid database token.
    - Rotation stores the new refresh token and revokes the old one.
    - Reuse returns `REFRESH_TOKEN_REUSED` with HTTP `401`.
  </acceptance_criteria>
  <done>Refresh-token lifecycle tests pass without requiring a real Redis server.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize security, profile, roles, and errors</name>
  <files>SecurityConfig.java, JwtAuthenticationFilter.java, CustomUserDetailsService.java, UserController.java, UserProfileResponse.java, RoleSeedInitializer.java, common/exception/**</files>
  <read_first>02-CONTEXT.md, SecurityConfig.java, JwtAuthenticationFilter.java, UserController.java</read_first>
  <action>Keep Spring Security stateless. Permit `/auth/**` and `GET /menus/public`; enforce `ADMIN` on `/admin/**`; enforce `USER` or `ADMIN` on `/users/**`. Add or verify `GET /users/me` returns only safe identity and roles. Seed `USER` and `ADMIN` roles at startup if missing. Route application and security failures through the global `{ code, message, timestamp }` error contract.</action>
  <verify>Controller/security tests cover unauthenticated profile, forbidden access, authenticated `/users/me`, and global error body fields.</verify>
  <acceptance_criteria>
    - `/users/me` does not expose credential or password hash data.
    - Invalid credentials, invalid/expired/revoked/reused refresh tokens, unauthenticated requests, and forbidden requests have stable codes.
    - Role-protected routes remain protected after auth changes.
  </acceptance_criteria>
  <done>Security/profile/error tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused integration coverage and finish docs</name>
  <files>AuthFlowIntegrationTest.java, TokenSerivceTest.java, AuthControllerTest.java, UserControllerTest.java, src/test/resources/application.properties, .planning/phases/02-auth-context-mvp/02-01-SUMMARY.md</files>
  <read_first>02-CONTEXT.md, existing auth tests</read_first>
  <action>Add focused tests for the full local auth flow: register, login, refresh rotation, logout revocation, reused refresh token all-session revocation, `/users/me`, unauthenticated access, forbidden access, and JWT claim expectations. Use mocked Redis cache where appropriate so the suite is deterministic. After implementation, write the Phase 02 summary with changed files, verification command, and any deviations.</action>
  <verify>`mvnw.cmd test` with Java 17.</verify>
  <acceptance_criteria>
    - Tests cover AUTH-001 through AUTH-010.
    - No test requires a real Redis instance.
    - Full Maven test suite passes.
  </acceptance_criteria>
  <done>All tests pass and `02-01-SUMMARY.md` exists.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvnw.cmd test` passes with Java 17.
- [ ] Register/login/refresh/logout integration flow passes through HTTP.
- [ ] Refresh-token reuse test proves all active sessions for the user are revoked.
- [ ] `/users/me` works with a valid access token and omits password hashes.
- [ ] Auth/security failures return `{ code, message, timestamp }`.
- [ ] Redis is treated as cache only; database remains source of truth.
</verification>

<success_criteria>
- `AUTH-001` through `AUTH-010` are implemented and covered.
- Local auth and protected route behavior are stable enough for later OAuth providers to reuse.
- Refresh-token lifecycle follows the multi-session, rotate-on-refresh, reuse-detection policy from `02-CONTEXT.md`.
- Phase 01 public menu behavior remains unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-context-mvp/02-01-SUMMARY.md`.
</output>
